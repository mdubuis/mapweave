# Mapweave — vision & roadmap

For a hands-on walkthrough of running the app and trying out what's built so far, see `LAUNCH.md`.

A much larger follow-up migration (Postgres/PostGIS backend, server-side generation, Leaflet rendering, wiki-integrated map view) is planned and tracked separately in `MIGRATION.md` — phases 1-4 below are complete and stable; the migration is new, additional work on top.

This fork of Azgaar's Fantasy Map Generator is being extended into a worldbuilding platform. FMG's own conventions (CONTEXT.md, CLAUDE.md/AGENTS.md, docs/) still apply to everything under `src/` — this file only adds the layer above them. Three pillars:

1. **Map** — the existing FMG procedural engine (Voronoi, hydrology, climate, cultures), kept as a reliable black box. Do not rewrite the generation pipeline; build on top of it.
2. **Lore wiki** — Markdown files with YAML frontmatter, wikilink-style cross-references (`[[entity]]`), and a resolved link graph for navigation.
3. **Bidirectional link** — map entities (burgs, states, markers) link to their wiki page; wiki pages link back to their map location.
4. *(later)* **Timeline** — multiple eras of the same world, with entities/borders/settlements changing between eras without duplicating everything.

## Gating rule

Phases run in order. As of 2026-09-13 the user has authorized working through phases autonomously without stopping for per-phase or per-step confirmation — proceed and log decisions here as they're made, report progress at natural checkpoints, but don't block on approval. The one exception: the Phase 4 temporal data model is flagged as the most structurally significant decision in the project (per the original brief) and deserves a clear callout even under this autonomy grant, so a bad call there doesn't get compounded silently.

## Phases

- **Phase 0 — Setup** (this file, `.claude/` config, workflow). Status: **done.**
- **Phase 1 — Repo cleanup**: classify what's essential to generation/rendering vs. optional (Electron build, Nix, misc scripts/docs) vs. dead; delete and verify `npm run build` + local run still work. Status: **done** — see decisions log for what was removed/kept.
- **Phase 2 — Wiki engine**: Markdown schema for lore entities + frontmatter (reserves a `map_ref` field for phase 3), wikilink resolution, relation graph, minimal read/edit UI. Status: **done** — see decisions log.
- **Phase 3 — Map ↔ wiki linking**: extend map popups (burgs/states/markers) with a link to the matching wiki page via `map_ref`; reverse link from wiki to map (check FMG's URL params first — may already support centering/zooming). Missing wiki page → offer to create one, never crash. Status: **done** — see decisions log.
- **Phase 4 — Timeline**: propose a temporal-validity data model (map side and wiki side) for approval before implementing — this is the most structural decision in the project. Then handle entity changes between eras (destroyed city, moved border, faction appearing/disappearing) without needless duplication, and an era-selector UI that filters both map and wiki. Status: **done** — see decisions log.
- **Phase 5 — Scenario module**: quest/objective tracking, stat blocks on any entity, weighted encounter tables with a roll widget, and a per-session campaign log — for writers/GMs prepping and running a game on top of the world, layered on the existing wiki entity model rather than a parallel system. Status: **done** — see decisions log.
- **Phase 6 — Unified navigation, multi-tab pages, burg-scoped sub-maps**: true single-DOM merge of map + wiki + board (shadow DOM, deliberately reopening the "no shared DOM" call made in `MIGRATION.md` Phase 4 once a de-risking mechanism existed for it — see `MIGRATION.md`), a multi-tab page model (wiki/map/board tabs on one entity), burg-scoped detail-map generation with terrain and culture/state inheritance from the parent world, and inline (not overlay-only) map embedding for a page's own map tab. Five internally-gated sub-phases, tracked as "Phase 6 Phase 1"–"Phase 6 Phase 5" in the decisions log below. Status: **done** — see decisions log. Known deferred gaps carried forward, not silently dropped: a DB-connected world's seed/width/height isn't wired into the map engine's own load, and a page's map tab can't yet load a *different* database world than whichever one the engine currently has loaded without a full page reload.

## End-of-phase reporting

Each phase closes with: what changed, what was validated (build/lint/tests run and their result), open questions, and an explicit request to proceed. No phase is silently continued.

## Decisions log

- 2026-09-13 — Project setup: added `.claude/settings.json` permission allowlist for `npm run dev/build/lint/test` (not `test:e2e`, per CONTEXT.md's "never run Playwright automatically"); added `.claude/agents/repo-archaeologist.md` for Phase 1 classification; added `.claude/commands/phase-checkpoint.md` for end-of-phase reports.
- 2026-09-13 — User dropped the per-phase approval gate: proceed autonomously through phases, report progress, don't block on confirmation. Flagged exception: still surface the Phase 4 temporal model clearly rather than silently deciding it.
- 2026-09-13 — Initialized git (repo had none) with a baseline commit before any deletion, so cleanup is reversible. Added remote `origin` → `git@github.com:mdubuis/mapweave.git`; pushing periodically going forward.
- 2026-09-13 — **Phase 1 cleanup done.** Removed via `repo-archaeologist` audit + judgment: Electron desktop packaging (`electron/`, `electron-builder.yml`, `build/icon.png`, related scripts/workflows/deps) and Nix packaging (`nix/`, `flake.*`, its workflow) — out of scope for a web-only map+wiki tool; Docker/Netlify alternate deploy paths (unused, GH Pages `deploy.yml` is the real one); upstream-only automation that can't run on a fork (`board-reconcile`, `theme-classify`, `sync-wiki` scripts+workflows); OSS community boilerplate (`CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, issue/discussion templates, `FUNDING.yml`, `copilot-instructions.md`); dead files (`.serena/`, `pw.q.config.ts`, `scripts/convert-style-presets.mjs`); historical changelogs (`docs/updates/`); the "Ask FMG" desktop-app download offer (`src/services/app-offer.ts` + its wiring in `about-tab.ts`/`versioning.ts`/`services/index.ts`) since it pointed at Azgaar's own release feed and installer artifacts that no longer exist for this fork.
  - **Kept deliberately**: actual FMG editing features named as removal candidates in the audit (battle simulator, GIS export, Ollama text generation, military forces/journeys) — removing them cleanly would require touching the 9K-line `src/index.html` monolith that CONTEXT.md explicitly flags as risky to restructure, and they're not clearly out of scope for a worldbuilding tool. Also kept `docs/wiki/*.md` (pure reference docs, zero build/bundle cost). Also left the internal `isElectron()`/`window.electron` runtime feature-detection in `platform.ts`/`shell.ts`/`assistant.ts`/`io/cloud.ts` alone — with no Electron shell anymore it deterministically returns false, which is correct web behavior, not a bug; ripping it out everywhere was judged as unnecessary refactor beyond cleanup scope.
  - **Verified**: `npm install` (lockfile resync), `npm run build`, `npm run lint`, `npm run test` (975/975 passing), and a local `npm run dev` smoke check all pass after cleanup.
  - **Not yet rebranded**: README.md, `about-tab.ts` community links (Discord/Reddit/Patreon/upstream changelog wiki) still describe upstream Azgaar/FMG — left untouched since that's a branding decision, not a cleanup one.
- 2026-09-13 — **Phase 2 wiki engine done.** Schema documented in `wiki/SCHEMA.md`: one Markdown file per entity anywhere under `wiki/`, filename = permanent slug, YAML frontmatter (`title`, `type` — free-form, not an enum — `summary`, `tags`, `aliases`, `relations: {name: slug}` for typed graph edges, and the reserved `map_ref: {kind, id, name}` for Phase 3). `[[wikilinks]]`/`[[slug|display]]` resolve case-insensitively against slugs/titles/aliases; unresolved links render as a distinct "broken" link instead of crashing, with a `#/new?title=…` affordance to create the page.
  - **No new npm dependencies** (CONTEXT.md rule): hand-rolled a narrow YAML-lite frontmatter parser (`src/wiki/frontmatter.ts`) and a minimal Markdown renderer covering only headings/paragraphs/bold/italic/code/lists/quotes/hr/links (`src/wiki/markdown.ts`) — not full YAML or CommonMark, documented as such in SCHEMA.md.
  - **Isolated from the risky monolith**: the wiki UI is a second Vite entry point (`src/wiki.html` + `src/wiki-main.ts` + `src/wiki.css`), not wired into `src/index.html` at all. `vite.config.ts` now builds both entries; `npm run build` confirmed both `dist/index.html` and `dist/wiki.html` are emitted.
  - Content is bundled from `wiki/**/*.md` via `import.meta.glob` for zero-setup browsing. Editing is progressive enhancement via the File System Access API (`src/wiki/editor.ts`, Chromium-only, feature-detected): "Open wiki folder" grants live read/write access to the same files, including creating a new page from a broken link. Firefox/Safari users get read-only browsing of the bundled snapshot with no crash, just a disabled button.
  - Graph view uses `d3-force` (already a dependency) in a full-screen SVG overlay, click-to-navigate.
  - Seed content: 4 example entities across place/character/faction/event under `wiki/` demonstrating frontmatter, wikilinks, relations, and one intentionally broken link.
  - **Verified**: `tsc --noEmit`, `npm run lint`, `npm run build` (both entries), `npm run test` (1001/1001 — added unit tests for frontmatter parsing, markdown rendering, wikilink extraction, and graph building). The interactive UI (open-folder/edit/save/graph click) was **not** exercised in a real browser — doing so would mean driving Playwright, which CONTEXT.md says never to run automatically. Recommend opening `/wiki.html` by hand once to click through it.
  - Found and worked around a pre-existing test-infra footgun while writing tests: `test-setup.ts` stubs the global `Node` as a plain (non-callable) object, which breaks `expect(...).toContain(...)` under the plain `node` vitest environment (existing files that use `toContain` all opt into `@vitest-environment jsdom` to avoid it). Used `.includes(...).toBe(true)` instead rather than touching shared test infra.
- 2026-09-13 — **Phase 3 map ↔ wiki linking done.** Researched how FMG wires entity clicks first (no `index.html` edits needed anywhere in this phase — every popup is built from a TS template string): burg clicks open `burg-editor.ts`'s dialog, marker clicks open `markers-editor.ts`'s, and state details live only in `states-editor.ts`'s table (there is no single-click state popup in FMG itself). Also found FMG's existing `Notes`/`NoteRef {type, id}` system (`src/generators/notes.ts`) is structurally the same idea as `map_ref` — same entity-kind vocabulary (burg/state/marker/province/religion/river/...).
  - **Map → wiki**: new shared helper `src/wiki/map-link.ts` scans the bundled `wiki/**/*.md` snapshot (same one `wiki.html` browses) for a matching `map_ref {kind, id}` and returns either the existing page's link or a prefilled "create this page" link. Wired in next to each editor's existing `Notes` button/icon, reusing the app's own `icon-link-ext` external-link glyph: `burg-editor.ts` (`#burgBottom`), `markers-editor.ts` (`#markerBottom`), `states-editor.ts` (per-row `data-col="wiki"`, following its existing click-delegation pattern — this one needed a matching entry added to the table's `columns` array and to its "neutrals" placeholder row template, or the grid layout would misalign).
  - **Wiki → map**: reuses FMG's *existing* URL params natively — no `url-params.ts` changes needed. Burgs jump via `?burg=<id>&scale=8`; everything else (state/province/religion/marker/river) via the already-generic `?cell=<id>&scale=8`, which is why `map_ref` gained an optional `cell` field (the entity's associated cell id — a state/province/religion's `center`, a marker's own `cell`) required for every kind except `burg`.
  - **Missing-page handling**: clicking the wiki-link icon for an unlinked entity opens `wiki.html#/new?title=...&mapKind=...&mapId=...&mapCell=...` — the wiki app's existing "create from broken link" flow, extended to prefill `map_ref` automatically so the new page comes pre-linked instead of needing a second manual step.
  - **Verified**: `tsc --noEmit`, `npm run lint`, `npm run build`, `npm run test` (1007/1007, +6 tests for `map-link.ts` using the seed `old-port.md` burg link as a real fixture). Clicking through the actual dialogs in a browser was not exercised (would mean driving Playwright — see Phase 2's note on the same policy).
  - **Known limitation, documented in SCHEMA.md**: the map app's reverse lookup reads the *build-time* bundled wiki snapshot, so a page linked live via the wiki's "Open wiki folder" editor won't show up on the map side until the dev server HMR-reloads it (automatic in `npm run dev`) or the app is rebuilt (production).
- 2026-09-13 — **Phase 4 timeline done**, per the user's explicit choice among three proposed models: **full `.map` snapshot per era** (one complete file per era, reusing FMG's own save/load — no engine changes, no diffing machinery to build or maintain), plus a **lightweight override layer** on the wiki side so most entity data isn't duplicated per era.
  - **Map side**: an "era" is a `type: era` wiki entity (`order`, `map_file`) — timeline metadata lives in the wiki, not a separate registry. `src/services/era-switcher.ts` reads it, injects a small floating dropdown (pure JS, zero `index.html` edits — same reasoning as Phase 3: nothing about the risky monolith needed to change), and switching eras just navigates to `?maplink=<absolute URL to that era's public/maps/*.map>` — FMG's own existing load path, not a new one. Seed eras (`wiki/eras/founding.md`, `post-war.md`) ship without real `.map` files (`public/maps/README.md` explains why: a legitimate one means actually building that era in the app, not something to fabricate) — selecting them shows FMG's normal "invalid link" message, which is correct, not broken.
  - **Wiki side**: `map_ref` became era-keyed (`Record<era, MapRef>`) instead of a single object — unavoidable once ids are only meaningful within one era's map file; a project with no eras defined still nests everything under one implicit `default` key (`DEFAULT_ERA`) so the shape never forks in two directions. A new `eras: {era: {field: override}}` block shallow-merges onto the base frontmatter per era (`resolveEntityForEra` in the new `src/wiki/eras.ts`) — only the fields that actually change get repeated, not the whole entity; the Markdown body stays a single "current" description (in-prose history sections work fine already, no new mechanism needed for that). `isEntityInEra` decides what belongs in an era's view: map-linked there, override there, or era-agnostic (no `map_ref`/`eras` at all — most characters/factions).
  - **Wiki UI**: an era `<select>` above the entity list (hidden entirely when no eras are defined) filters the sidebar and the graph view, and resolves the displayed entity through `resolveEntityForEra`. Era is sticky UI state seeded from `?era=` but not reset by ordinary wikilink navigation (a route's URL rarely carries era explicitly); arriving from a map-created page's `mapEra` param adopts that era as the browsing context.
  - **Bug found and fixed while building this**: the hand-rolled frontmatter YAML-lite parser's key regex didn't allow hyphens (`/^([A-Za-z0-9_]+):/`), so an era slug like `post-war` used as a nested YAML key silently failed to parse. Fixed the regex and added a regression test — this would otherwise have silently dropped every hyphenated era's data.
  - **Verified**: `tsc --noEmit`, `npm run lint`, `npm run build` (both entries, era-switcher code-splits into its own shared chunk with the wiki content loader), `npm run test` (1018/1018 — added `eras.ts` tests and updated `entities`/`frontmatter`/`map-link` tests for the new era-keyed shapes). No `index.html` edits in this phase either. Not exercised in a real browser (same Playwright policy as Phases 2–3).
  - **Not built**: per-field history/diff summaries between two eras' full map JSON (mentioned as a possible future nicety when this model was proposed) — out of scope unless asked for.
- 2026-09-13 — **Map UI style refresh + wiki menu built into the map layer**, per explicit request. User chose "full reskin" over two narrower options after being shown the risk trade-off (dozens of dialogs, no way to click through all of them without running Playwright, which the project bars running automatically).
  - **Reskin approach**: rather than editing each of the dozens of individual editor dialogs (high risk, impossible to verify visually one by one), targeted the small set of *shared chrome rules* every dialog already inherits from in `public/index.css` — `.ui-widget.ui-widget-content` (dialog frame), `.ui-widget-header`/titlebar close button, generic `button`/`input`/`select`/`textarea`, `#options` (main panel, previously had a hardcoded purple border unrelated to the app's own theme system), and the bottom `#tooltip` hint bar (was a jarring red gradient banner, now a neutral dark one). Added five CSS custom properties (`--radius`, `--radius-sm`, `--shadow`, `--shadow-sm`, `--transition`) to `:root` so the whole look stays consistent and re-tunable from one place. Added a `:focus-visible` ring using the app's own theme-color variable (`--header`) — an accessibility gap that didn't exist before.
  - **Deliberately not touched**: any SVG map-rendering CSS (fills/strokes/filters/layer rules — index.css is ~2700 lines and most of it is map rendering, not UI chrome), the vendored `public/libs/jquery-ui.css` file itself, `index.html`'s structure, and the existing theme-color derivation math in `options-tab.ts` (tuning HSL lightness/saturation deltas blind, without visual feedback, was judged too likely to break contrast somewhere across the whole app).
  - **One real bug caught before it shipped**: an early draft added `overflow: hidden` to the dialog frame for cleanly clipped rounded corners — reverted after checking that several dialogs (`emblems-editor.ts`, `production-overview.ts`, `production-chains.ts`, `goods-distribution-editor.ts`, `view-3d.ts`) are `resizable: true`, and jQuery UI's resize handles are positioned partly outside the frame's own bounds; hiding overflow would have silently broken dragging them to resize.
  - **Wiki menu in the map layer**: `src/services/wiki-panel.ts` (parallel to `era-switcher.ts` — same "inject via JS, zero `index.html` edits" approach) adds a floating circular button (top-right, `icon-sitemap`) that slides in a right-hand panel with an `<iframe src="./wiki.html">`, lazy-loaded on first open. Wiki browsing/editing no longer requires knowing to type a different URL.
  - **Bug fixed while wiring tooltips for the two new floating widgets**: the app's `data-tip` hint-bar system only listens on a fixed list of container ids (`src/components/tooltips.ts`), scanned once at module load — adding the new widgets' ids to that list looked like the obvious fix but would have silently done nothing, since that scan runs before the widgets exist (they're injected later, on `DOMContentLoaded`, same as `era-switcher.ts` already was). Wired each widget's own `mousemove` listener to the exported `showDataTip` instead, decoupled from that startup-order assumption.
  - **Verified**: `tsc --noEmit`, `npm run lint`, `npm run build`, `npm run test` (1018/1018 — no new tests, since this is CSS/DOM-injection with no pure-function logic to unit test); manually confirmed brace-balance on the edited stylesheet. **Not verified**: actual visual appearance in a browser — no dialog was clicked through, per the project's Playwright policy. A manual pass is genuinely warranted here before calling this finished.
- 2026-09-19 — **Phase 5 scenario module done**, scoped with the user first (content: quests + NPC stats + encounter tables + session log; integration: extend the existing wiki entity model, not a separate tool; usage: in-app consultation/editing now, export explicitly deferred) via a planned research pass before writing any code — see the approved plan for the full design rationale.
  - **No new entity "kinds", just conventions on the existing model**: `WikiFrontmatter` (`src/wiki/types.ts`) gains `status`/`hook`/`objectives`/`resolution`/`stats`/`statBlockSystem`/`table`/`number`/`date`, all optional and usable on any `type`, coerced the same lenient way (`src/wiki/entities.ts`'s `toFrontmatter`) as every existing field — no schema validation added, consistent with the rest of the wiki. A stat block is deliberately *not* a separate `npc` type: any entity (typically `character`) can carry `stats`, so a lore-only character and a fight-ready NPC stay cross-referenceable through the same `relations`/wikilinks.
  - **Worked inside the frontmatter parser's real limits, not around them**: the YAML-lite parser (`src/wiki/frontmatter.ts`) has no object-list support (`- key: value` per item) and a latent inline-comment bug, confirmed by reading it before designing anything. Weighted encounter entries and quest objectives are therefore both plain string lists with a light convention — `"Nx entry"` (weight, default 1) and `"[x] "/"[ ] "` (done/pending) — parsed app-side by a new `src/wiki/scenario.ts` (`parseWeightedEntry`/`rollEncounter`/`parseObjective`), not by extending the shared parser.
  - **First stateful widget in the wiki app**: the encounter-table "Roll" button (`mountEncounterRoller` in `wiki-main.ts`) is the first interactive, non-persisted widget anywhere in this app — confirmed via research that nothing like it (a button that computes an ephemeral result rather than navigating or writing a file) existed before. Owns its last-roll result in a closure, re-renders only its own container, touches neither the route nor any file.
  - **Two new routed views** (`#/quests`, `#/sessions`) follow the existing hand-rolled hash-router's exact pattern (`Route` union, `currentRoute()`, `render()` dispatch) — quests board grouped by `status` in a fixed reading order (open → active → complete → abandoned → other, not alphabetical, so the board reads as a workflow); sessions list chronologically by `number`. Session log is deliberately one file per session (matching the existing "one file per entity" convention already used for `era`), not a single growing document — distinct from `eras`: an era is the world's own history, a session is what a group actually played.
  - **Creating a new page of these types** reuses the existing `#/new` flow and its `createEntity()` (`src/wiki/editor.ts`) unchanged in structure, just with a type-specific starter frontmatter block (`status: open` + an empty objective for a quest, an empty `table` for an encounter table, `number`/`date` for a session) so a freshly created page already shows the expected shape instead of a bare stub.
  - **Seed content**: `wiki/quests/the-salt-tithe.md`, `wiki/encounters/delta-crossing.md`, `wiki/sessions/session-{1,2}.md`, plus a `stats` block added to the existing `wiki/characters/mira-thorne.md` — all cross-linked into the existing Old Port/Silver Compact/Salt War seed world rather than living in isolation, so the new views have real, connected content to demonstrate against.
  - **Verified**: `tsc --noEmit` propre, lint propre, 1056/1056 tests passent (`src/wiki/scenario.test.ts` new, 11 tests; `src/wiki/entities.test.ts` extended, +4 tests for the new field coercion), `npm run build` (both entry points, `dist/index.html` and `dist/wiki.html`, confirmed present).
  - **Not built, explicitly out of scope for this pass**: PDF/printable export (deferred per the user's own stated priority — consultation first); structured per-field editing (the editor stays the existing raw textarea); zod-style strict schema validation on wiki frontmatter (kept at the same lenient-coercion rigor as the rest of the wiki, not a new precedent).
  - **Reste à vérifier par un humain** (le projet ne lance jamais de vrai navigateur automatiquement) : ouvrir `wiki.html`, cliquer les liens "Quests"/"Sessions" dans l'en-tête, ouvrir la page de Mira Thorne (bloc stats), la quête "The Salt Tithe" (statut + checklist d'objectifs), la table de rencontre "Delta Crossing" (bouton Roll, plusieurs tirages), et créer une nouvelle page de chaque nouveau type via `#/new` pour confirmer le gabarit pré-rempli.
- 2026-09-20 — **"View Map" ouvre maintenant la carte réellement connectée à la base, pas une carte indépendante.** Demandé par l'utilisateur ; recherche préalable a confirmé que Postgres ne stocke pas un format que `map.html` peut recharger tel quel (couches GeoJSON + `map_topology.pack` brut, pas un `.map` prêt à charger) — reconstruire un export fidèle à 100% (y compris les éditions manuelles post-génération) est le travail explicitement reporté à la Phase 6 dans `MIGRATION.md`, pas quelque chose à improviser ici. Option choisie avec l'utilisateur (clarifiée via question) : régénérer côté client depuis le seed — fidèle seulement si la carte n'a jamais été éditée à la main après génération, écart accepté.
  - `src/wiki/db-entities.ts` gagne `fetchConnectedMapInfo(apiBase, mapId)` (lit `GET /api/maps/:id`, extrait `seed`/`facts.graph.{width,height}`) ; `wiki-main.ts` le stocke dans `connectedMap` à chaque connexion DB (auto ou manuelle) et construit l'URL de l'iframe carte (`mapFrameSrc()`) avec `?seed=&width=&height=` — mécanisme déjà supporté nativement par `map.html` (`checkLoadParameters`/`setSeed`), aucun nouveau code moteur côté client. L'iframe recharge automatiquement si la carte connectée change entre deux ouvertures du panneau (`mapFrameKey`), sans recharger inutilement si c'est toujours la même.
  - **Vrai bug corrigé en cours de route** : `POST /api/maps/generate` (génération serveur, Phase 3) n'a jamais persisté `width`/`height` dans `facts` — seul l'import navigateur avec un export "Minimal JSON" le faisait. Corrigé dans `server/src/routes/maps.mjs` en construisant un `settingsExport` minimal à partir de `packExport.info` (déjà retourné par `generateMap`, voir `pack-to-json.ts`) avant l'import Postgres. Sans ça, toute carte générée côté serveur n'aurait régénéré qu'à la taille par défaut (1280×800), pas sa vraie taille.
  - **Vérifié** : `tsc --noEmit`, lint, 1065/1065 tests, build (les deux points d'entrée). Pas de test serveur ajouté (aucun n'existe dans `server/`, vérifications manuelles seulement, cohérent avec l'existant).
- 2026-09-20 — **Zoom plus fluide avec beaucoup de burgs/marqueurs.** Signalé par l'utilisateur (saccades pendant le geste, à-coup au relâchement, empire avec le nombre d'icônes). Cause réelle trouvée en lisant le code : `refreshSizeForZoom()` dans `burg-icon-layer.ts` et `marker-layer.ts` appelait `marker.setIcon(...)` pour **chaque** icône à chaque changement de niveau de zoom — `setIcon` ne modifie pas un attribut, il détruit et reconstruit tout le nœud DOM (chaîne HTML → parse → retrait de l'ancien nœud → insertion du nouveau), pour potentiellement des centaines d'icônes. `marker-layer.ts` reconstruisait en plus une `Map` complète depuis `pack.markers` à chaque appel, alors que les données ne changent pas entre deux niveaux de zoom.
  - **Corrigé par mutation en place, pas par réécriture d'architecture** : nouvelle fonction exportée `updateIconSize(marker, size)` dans chaque fichier, qui modifie directement les attributs qui dépendent réellement de la taille sur le nœud DOM déjà rendu (largeur/hauteur/marge du div, `viewBox`/largeur/hauteur du svg, `font-size` du `<g>` et `x`/`y` du `<use>` pour les burgs) — reproduit exactement le calcul interne de Leaflet (`Icon._setIconStyles`) sans détruire/recréer le nœud. Repli automatique sur l'ancien `setIcon()` si l'icône n'a pas encore de nœud rendu (`marker.getElement()` retourne `undefined`). `marker-layer.ts` gagne aussi `markerData`, une Map peuplée une fois par `update()`, pour ne plus reconstruire `pack.markers` en Map à chaque zoom.
  - **Décision explicite avec l'utilisateur** : correctif appliqué aux deux fichiers avec tests de régression (option choisie sur trois proposées, l'alternative la plus prudente n'aurait corrigé que le gaspillage de `marker-layer.ts` sans toucher au coût `setIcon()`), car c'est du code moteur délicat (historique de bugs de positionnement pendant la migration Leaflet, voir plus haut) qu'il n'est pas possible de vérifier visuellement dans un vrai navigateur depuis cette session.
  - **Tests de régression** (`burg-icon-layer.test.ts`, `marker-layer.test.ts`, nouveaux, avec un vrai `L.Marker`/`L.map` en jsdom, pas de mock) : comparent l'état DOM obtenu par mutation en place à celui d'une icône reconstruite à neuf à la même taille — doivent être strictement identiques (attributs svg, style du div). Couvrent aussi le repli sur `setIcon()` quand l'icône n'est pas encore montée.
  - **Non touché** : le coût par frame pendant le geste continu (ex. `applyLabelsZoomSize()` qui redimensionne tous les labels à chaque frame de zoom si `resizeOnZoom` est actif) — c'est probablement un choix produit délibéré (le texte grossit visiblement pendant le geste, pas seulement au relâchement), pas un pur gaspillage à supprimer sans le confirmer avec l'utilisateur.
  - **Vérifié** : `tsc --noEmit`, lint, 1070/1070 tests (5 nouveaux), build.
- 2026-09-20 — **Navigation carte↔wiki repensée + nettoyage des références Azgaar**, planifiés et validés avec l'utilisateur (voir le plan approuvé) après avoir sorti explicitement une troisième demande — faire vivre le wiki dans Postgres — vers une future phase à part (aucune table wiki, aucune API d'écriture, aucun outil de migration de schéma n'existent aujourd'hui ; question de modèle de données non triviale avec le système d'ères).
  - **Plus d'auto-connexion silencieuse "à la carte la plus récente"** : `src/wiki-main.ts` gagne une route `#/choose-world` (choix explicite d'un monde), persisté dans `localStorage` (`mapweave.lastMapId`) — au chargement, `connectToPersistedWorld()` reconnecte silencieusement au monde persisté s'il existe encore, sinon redirige vers `#/choose-world` (seulement si l'utilisateur est sur la route par défaut — un lien direct vers une entité n'est jamais interrompu). Le couple `db-indicator`/`connect-db-btn` (bouton vers une vue plein écran qui effaçait la page en cours) est remplacé par un contrôle "monde courant" dans la sidebar ouvrant un petit menu déroulant, pour changer de monde sans perdre sa place.
  - **Nettoyage Azgaar/FMG** : suppression complète du bouton/widget "Assistant d'aide" (`help-assistant.ts` + `services/help/*.ts` + `services/assistant.ts`, ~8 fichiers) puisqu'il dépendait du vrai serveur backend d'Azgaar (`ask.azgaarsfmg.com`, aucun équivalent Mapweave) — décision explicite de l'utilisateur d'accepter la perte de cette fonctionnalité plutôt que de la laisser pointer vers un serveur qui n'est pas le nôtre. Retrait de la texture 3D hébergée chez Azgaar (fallback: couleur de fond unie). Retrait du tag Google Analytics d'Azgaar (G-VJL3J26W7R) — pas seulement cosmétique, ça envoyait les données d'usage de ce fork vers son compte GA. Nettoyage des dialogues About/Changelog, du manifest PWA, du titre/meta de `map.html`, des liens "tutoriel wiki upstream" dispersés dans une dizaine de contrôleurs, et de `README.md` (réécrit, l'ancienne section "Desktop app" décrivait de toute façon un packaging Electron/Nix déjà supprimé en Phase 1).
  - **Laissé intentionnellement** : la mention Azgaar dans `LICENSE` (exigée par MIT) ; les liens vers cartographyassets.com et l'intégration Armoria (armoria.github.io) dans l'éditeur d'emblèmes — outils tiers réellement utiles, pas de l'auto-promotion, aucun équivalent Mapweave à substituer ; les métadonnées XML de provenance CC0 dans `public/charges/*.svg` (invisibles dans l'app) ; les commentaires de code internes ("FMG" comme raccourci pour le moteur hérité) ; `docs/wiki/*.md` (déjà une décision de la Phase 1).
  - **Trouvé en cours de route, pas traité, signalé à l'utilisateur** : l'intégration Dropbox (`public/dropbox.html` + ~8 fichiers) authentifie via un Client ID Dropbox qui semble appartenir au compte développeur d'Azgaar (`pdr9ae64ip0qno4`) — fonctionnellement ça marche encore (OAuth normal), mais l'écran de consentement Dropbox affiche le nom de SON app, pas Mapweave. Pas retiré unilatéralement : découvert après la question posée à l'utilisateur sur les deux dépendances fonctionnelles connues (texture 3D, assistant d'aide), donc hors du périmètre déjà approuvé — nécessite une décision séparée.
  - **Vérifié** : `tsc --noEmit`, lint, 1016/1016 tests (4 fichiers de test supprimés avec leur sujet), build (les deux points d'entrée).
- 2026-09-20 — **Passe UI inspirée de LegendKeeper** (concurrent direct), planifiée et validée avec l'utilisateur après une recherche textuelle du site (pas de navigateur visuel disponible dans cette session — pas de capture d'écran possible, seulement le contenu marketing). Trois axes livrés maintenant sur les quatre proposés ; le quatrième (sous-cartes imbriquées) sort de ce passage vers sa propre phase — aucune table/champ/architecture ne supporte une hiérarchie de cartes aujourd'hui (confirmé par recherche : `maps` est strictement plat, un seul `pack`/une seule instance `L.Map` par page), ce serait un vrai morceau d'architecture (nouvelle FK `parent_map_id`, flux de génération scopé à un burg), pas une passe UI.
  - **Passe visuelle** : `src/wiki.css` importe les mêmes tokens que `public/index.css` (`--radius`, `--shadow`, `--transition`) — le wiki n'avait jusqu'ici aucune ombre ni transition, contrastant avec la carte déjà repolie cette session. Police d'affichage "Cinzel" (déjà dans le catalogue de polices de labels de carte, jamais chargée côté wiki) sur les titres, corps de lecture inchangé (Georgia, déjà bon). Contraste `--bg`/`--bg-alt` corrigé (les deux étaient presque identiques).
  - **Contenu secret (mode MJ/joueur)** : nouveau champ `secret: true` (page entière) et convention de bloc `:::secret ... :::` dans le corps (section partielle) — `src/wiki/secrets.ts`, nouveau module pur testé, pré-traite le texte brut avant `renderMarkdown` plutôt que de toucher au moteur de rendu ligne-par-ligne. Bascule "GM view"/"Player view" dans la sidebar (`viewMode`, persisté en `localStorage`, même famille que le sélecteur d'ère). Explicitement **pas** un vrai contrôle d'accès — Mapweave n'a aucun compte/authentification ; c'est un filtre d'affichage local que le MJ bascule avant de partager son écran.
  - **Liens automatiques** : un nom/alias d'entité mentionné en texte libre se lie tout seul, sans `[[crochets]]`. `src/wiki/entities.ts` gagne `buildAutoLinkNames` (titres+alias, triés du plus long au plus court, longueur minimale de 4 caractères pour éviter qu'un titre court/mot courant ne lie tout le wiki) ; `src/wiki/markdown.ts`'s `renderInline` gagne une passe qui ne scanne que le texte pas déjà à l'intérieur d'un tag `<a>`/`<code>` déjà émis — un `[[wikilink]]` explicite garde toujours la priorité, jamais re-lié.
  - **Vérifié** : `tsc --noEmit`, lint, 1032/1032 tests (16 nouveaux : `secrets.test.ts`, `buildAutoLinkNames`/`secret` dans `entities.test.ts`, auto-link dans `markdown.test.ts`), build (les deux points d'entrée).
- 2026-09-21 — **Recherche plein texte, imbrication de pages, modèles réutilisables** — deuxième morceau de l'inventaire LegendKeeper, après que l'utilisateur a confirmé que la collaboration temps réel/partage public reste hors périmètre (conflit avec l'architecture mono-utilisateur locale). Le reste (vraie timeline/calendrier, éditeur riche, tableau blanc) reste à planifier séparément, un morceau à la fois — les deux derniers sont chacun un sous-système à eux seuls.
  - **Recherche plein texte** : `renderSidebar` scanne maintenant `entity.body` en plus du titre/tags (`entity.body` était déjà 100% en mémoire, aucune I/O ajoutée). Détail de correction important : en mode joueur, le texte scanné passe par `stripSecrets` — sinon un match dans un bloc `:::secret``` révélerait son existence via la recherche même si la page reste par ailleurs visible. Un match trouvé uniquement dans le corps affiche un court extrait (`extractSnippet`, `src/wiki/entities.ts`, testé) sous le lien. L'input de recherche est débounced (150ms) pour éviter de rescanner tous les corps à chaque frappe.
  - **Imbrication de pages** : nouveau champ `parent?: string` (slug) sur `WikiFrontmatter` — portée volontairement limitée au **même type** (une place ne s'imbrique que sous une autre place), un parent d'un autre type ou inexistant dégrade proprement en entrée à plat plutôt que d'être une erreur. `buildEntityTree` (`src/wiki/entities.ts`, testé) construit l'arbre par groupe de type et se protège explicitement contre un cycle accidentel (A parent de B, B parent de A) en rendant tous les membres du cycle racines plutôt que de les faire disparaître silencieusement de la liste ou de boucler indéfiniment — un vrai piège de correction identifié et testé, pas juste un cas théorique. Pas de nouveau sélecteur "choisir le parent" dans les formulaires — `parent` s'édite comme `relations`/`aliases`, via l'éditeur brut existant.
  - **Modèles de page réutilisables** : `src/wiki/frontmatter-patch.ts`'s `patchFrontmatterType` est généralisé en `patchFrontmatterField(raw, key, value)` (fin wrapper conservé pour compat) — capture d'un modèle **verbatim** (texte brut complet, aucun parsing/ré-émission) et application via une seule ligne ciblée (`title:`) écrasée, exactement le même pattern déjà établi que pour `type:`. Stockage initial en `localStorage` (voir correction ci-dessous — remplacé par des fichiers le jour même). "Save as template…" dans l'éditeur, sélecteur "Template:" filtré par type + bouton de suppression dans le formulaire de création.
  - **Vérifié** : `tsc --noEmit`, lint, 1044/1044 tests (12 nouveaux), build (les deux points d'entrée).
- 2026-09-21 — **Correction : les modèles de page passent de `localStorage` à de vrais fichiers.** L'utilisateur a déposé un export réel d'un projet LegendKeeper (dossier `LK/`, hors dépôt) — pour la première fois cette session, du vrai contenu LK à lire directement plutôt que du texte marketing. Sa page tutoriel "Template Tavern" confirme : "Any page you drag to the Templates tab will become a re-usable template" — un modèle LK est une vraie page, stockée comme un fichier, synchronisée avec le reste du projet. La limite `localStorage` du jour même (pas de synchronisation avec le dossier wiki) était donc un vrai écart, pas un détail mineur — corrigé après confirmation explicite de l'utilisateur.
  - Les modèles vivent maintenant sous `wiki/templates/` comme des fichiers `.md` ordinaires (même forme qu'une entité : frontmatter + corps) — `src/wiki/editor.ts` gagne `loadTemplates`/`saveTemplate`/`deleteTemplate`. Ce dossier est exclu de la liste d'entités réelles (le glob bundlé `entities.ts` ET le parcours `walkMarkdownFiles` d'`editor.ts` l'ignorent tous les deux) pour qu'un modèle n'apparaisse jamais comme une page dans la sidebar/recherche/graphe.
  - Conséquence assumée : comme la sauvegarde d'un modèle utilise maintenant la même API File System Access que l'édition normale, elle nécessite un dossier wiki ouvert (`dirHandle`) — déjà le cas dans tous les points d'entrée existants (le bouton n'est atteignable que depuis l'éditeur, lui-même déjà gated sur `dirHandle`), donc aucun nouveau garde-fou nécessaire.
  - Pas de nouveau test — aucun test n'existe pour `editor.ts` (File System Access API non mocké nulle part dans ce repo), cohérent avec l'existant (`loadFromDirectory`/`createEntity` ne sont pas non plus testés).
  - **Vérifié** : `tsc --noEmit`, lint, 1044/1044 tests (aucune régression), build (les deux points d'entrée).
- 2026-09-21 — **Vue timeline d'événements**, sur instruction de continuer le reste du backlog LegendKeeper de façon autonome sans repasser par une confirmation à chaque morceau (l'utilisateur a explicitement dit de continuer jusqu'à épuiser la liste). Lecture directe de la page tutoriel "Temple of Time" de l'export LK fourni par l'utilisateur : la vraie fonctionnalité LK est bien plus grosse (3 vues — liste/Gantt/calendrier —, vrai calcul calendaire avec calendriers personnalisés, fusion de plusieurs timelines). Portée retenue pour ce passage : une timeline chronologique réelle, liée aux pages de lore, groupée par catégorie — le cœur de valeur — le reste (calcul calendaire, Gantt, calendrier mensuel, fusion) explicitement hors périmètre et documenté comme tel dans `wiki/SCHEMA.md`.
  - **Aucun nouveau champ de tri** : `order` (déjà là, jusqu'ici documenté "era-type entities only") est généralisé aux entités `event` — même concept ("position dans une séquence"), pas besoin d'un nouveau champ. `date` (déjà là pour `session-log`) est pareillement généralisé, toujours texte libre jamais parsé. Un seul champ réellement nouveau : `group?: string` (catégorie d'affichage pour le regroupement dans la vue).
  - **`renderTimelineView`** (`src/wiki-main.ts`) suit exactement le pattern déjà établi par `renderQuestBoardView`/`renderSessionLogView` — nouvelle route `#/timeline`, même style de tri avec repli (sans `order` → trié par titre, en dernier), regroupement par `group` avec repli "Other" pour les entités sans groupe.
  - **Vrai bug trouvé et corrigé en cours de route** : `visibleEntitiesOfType` (partagée par quêtes/sessions/timeline) ne filtrait pas du tout les entités `secret: true` en mode joueur — un oubli pré-existant à la fonctionnalité "secrets" de la veille, jamais remarqué avant. Corrigé dans la fonction partagée, bénéficie donc aussi aux quêtes et sessions, pas seulement à la nouvelle vue timeline.
  - **Vérifié** : `tsc --noEmit`, lint, 1046/1046 tests (2 nouveaux), build (les deux points d'entrée).
- 2026-09-21 — **Barre d'outils markdown + raccourcis clavier pour l'éditeur wiki**, cinquième morceau du backlog LegendKeeper. Vrai fork architectural trouvé en cours de recherche : Quill (bibliothèque WYSIWYG) est déjà une dépendance de production (utilisée par `notes-rich-text.ts` côté carte, zéro coût de nouvelle dépendance), mais c'est un éditeur **HTML** alors que le wiki stocke du **Markdown** — l'utiliser directement aurait demandé de changer le format de stockage du wiki (casse `wiki/SCHEMA.md`, les diffs Git, la compatibilité externe) ou d'écrire un convertisseur HTML→Markdown sur mesure inexistant à ce jour. Question posée à l'utilisateur avec les trois options réelles (barre d'outils sur markdown / Quill+HTML / Quill+nouveau convertisseur) — choix explicite de la première, la seule sans risque de conversion/perte de données.
  - **`src/wiki/markdown-edit-commands.ts`** (nouveau module pur, testé) : `applyMarkdownCommand(command, {value, start, end})` — entoure la sélection (gras/italique/code/lien/wikilink, avec texte substituable pré-sélectionné si rien n'est sélectionné) ou préfixe chaque ligne concernée avec bascule (titre/liste à puces/liste numérotée/citation — une deuxième application retire le préfixe plutôt que de le doubler). Aucune manipulation DOM dans ce module — juste des chaînes de caractères et des positions, câblé à un vrai textarea dans `wiki-main.ts`.
  - Barre d'outils au-dessus du textarea de l'éditeur + raccourcis Ctrl/Cmd+B/I/K (gras/italique/lien) — une affectation programmatique de `textarea.value` ne déclenche pas l'événement `input` existant, donc chaque commande appelle `updatePreview()` à la main après coup.
  - **Vérifié** : `tsc --noEmit`, lint, 1062/1062 tests (16 nouveaux), build (les deux points d'entrée).
- 2026-09-21 — **Tableau blanc (`type: board`)**, dernier morceau du backlog LegendKeeper. Recherche préalable (2 agents Explore) nuancée par rapport à l'hypothèse initiale ("sous-système complet, rien à réutiliser") : la carte a déjà une instance Leaflet en `L.CRS.Simple` (coordonnées plates) — un second canevas indépendant réutilise directement ce pattern ; le mécanisme "élément positionné en x/y, déplaçable au glisser" existe déjà pour les marqueurs (`marker-layer.ts`), seule l'apparence (icônes SVG de jeu) est spécifique ; l'extraction image→data-URI existe déjà dans `notes-rich-text.ts`, juste couplée à Quill. Deux questions de portée posées à l'utilisateur avant d'écrire le plan : connecteurs simples inclus (oui, lignes droites `L.polyline`, pas de courbes/flèches) et cartes-page reliant vers une entité existante incluses (oui).
  - **Stockage confirmé comme le bon choix, pas un contournement** : le parseur YAML-lite frontmatter ne supporte aucune liste d'objets structurés (vérifié en lisant `frontmatter.ts`) — impossible d'y faire tenir un tableau de cartes. `src/wiki/board.ts` (nouveau module pur, testé, même convention que `secrets.ts`) stocke `{items, connectors}` en JSON dans un bloc balisé ` ```board ` à l'intérieur du corps Markdown — `extractBoardData`/`replaceBoardBlock` (édition texte ciblée, pas de reparse+réécriture) + `replaceBoardBlockInRaw` pour le flux de sauvegarde (capture verbatim du bloc frontmatter, même technique que `map-ref-patch.ts`).
  - **`src/renderers/leaflet/board-canvas.ts`** (nouveau) : une seconde instance `L.Map` indépendante du singleton `leaflet-map.ts` (même `LinearSimpleCRS`, sans `mountLegacySvg`), un `L.marker`+`divIcon` par carte (image/texte/lien-de-page), glisser-déposer natif Leaflet, redimensionnement par poignée de coin (pointer events bruts, pas lié au zoom contrairement à `marker-layer.ts`), connecteurs en `L.polyline` repositionnés en direct pendant le glisser d'une extrémité.
  - **`renderEntityView`** (`wiki-main.ts`) gagne une branche `type === "board"` qui remplace entièrement `<article class="entity-body">` par un conteneur de canevas — le seul cas de remplacement total du corps (`isEncounterTable` ne fait qu'ajouter une section, jamais remplacer).
  - **Fuite mémoire potentielle identifiée et corrigée avant qu'elle n'existe** : une instance Leaflet vivante (écouteurs `document`) survivrait à un `content.innerHTML = ...` qui détache son conteneur sans jamais appeler `.remove()`. Nouveau `activeBoardHandle`/`teardownBoard()` au niveau module, appelé au sommet de `render()` (tout changement de route), `renderEntityView()` et `renderEditorView()` (le seul chemin qui quitte une vue entité sans repasser par `render()`) plutôt que de toucher chacun des ~12 sites qui écrivent dans `#content`.
  - **Sauvegarde explicite, pas automatique au glisser** : confirmé par la recherche — aucune sauvegarde automatique n'existe nulle part dans ce projet. Le bouton "Save" du board sérialise l'état en mémoire et appelle `saveEntity` existant, même modèle que l'éditeur normal.
  - **Portée explicitement exclue** : dessin à main levée (aucune infrastructure existante, un vrai sous-projet à part), formes géométriques, couleurs de sticky-note, flèches/courbes stylées sur les connecteurs.
  - **Vérifié** : `tsc --noEmit`, lint, 1077/1077 tests (15 nouveaux dans `board.test.ts`), build (les deux points d'entrée). Pas de vérification navigateur réelle (pas d'outil de navigateur visuel disponible dans cette session, politique Playwright du projet) — reste à vérifier par l'utilisateur : créer une page `type: board`, ajouter une image et une carte texte, les déplacer/redimensionner, les relier, sauvegarder, recharger et confirmer que tout est repositionné à l'identique.
- 2026-09-22 — **Retrait complet de l'intégration Dropbox**, décision explicite de l'utilisateur après confirmation (question posée directement : garder tel quel / retirer / attendre un Client ID Mapweave — l'utilisateur a choisi le retrait, cohérent avec le nettoyage Azgaar déjà fait le 2026-09-20). Inventaire précis fait par recherche avant toute suppression (l'estimation initiale "~8 fichiers" était sous-évaluée) : 3 fichiers entièrement Dropbox supprimés (`public/dropbox.html` — page de callback OAuth popup, `public/libs/dropbox-sdk.min.js` — SDK vendored, `src/services/io/cloud.ts` — provider Dropbox complet avec le Client ID `pdr9ae64ip0qno4` en dur) ; 7 fichiers partiellement édités pour retirer uniquement leur portion Dropbox (`src/services/index.ts`, `src/services/io/save.ts`, `src/services/io/load.ts`, `src/components/hotkeys.ts`, `src/components/shell.ts`, `src/components/options/io-panes.ts`, `src/types/global.ts`, `src/map.html`) — le reste de chacun (sauvegarde machine/storage, chargement par URL, raccourcis clavier non liés, dialogues Save/Export/Load) reste intact.
  - **`copyLinkToClipboard`/le lien partageable retirés aussi** : vérifié par grep que `#sharableLink`/`#sharableLinkContainer` n'étaient peuplés que par la fonctionnalité de lien Dropbox — pas une fonctionnalité générique de partage, donc supprimés avec elle plutôt que laissés comme code mort.
  - **Un vrai raccourci clavier corrigé en cours de route** : `hotkeys.ts` bloquait Ctrl+C par défaut du navigateur (`event.preventDefault()`) uniquement pour laisser la place au raccourci "sauver vers Dropbox" — supprimé avec lui, Ctrl+C redevient le copier-coller normal du navigateur.
  - **`docs/wiki/*.md` explicitement non touchés** malgré plusieurs mentions de Dropbox (tutoriels, FAQ, changelog) — cohérent avec la décision de Phase 1 de traiter ce dossier comme la doc upstream Azgaar figée, jamais activement maintenue par ce fork. Deux docs d'architecture (`architecture.md`, `lazy-loading.md`) corrigés en revanche : ils utilisaient `CloudStorage`/`Cloud.providers.dropbox` comme exemple illustratif d'un pattern générique (façade de méthode pour un module exposant des données imbriquées) — reformulés sans référence à un module qui n'existe plus, et la liste des modules `Services` remise à jour avec ce qui existe réellement aujourd'hui (elle mentionnait aussi un `Installation` qui n'a jamais existé dans le registre actuel — dérive pré-existante, corrigée au passage puisque la liste était de toute façon en cours de retouche).
  - **Vérifié** : `tsc --noEmit`, lint, 1077/1077 tests (aucun test ne référençait Dropbox), build (les deux points d'entrée, warning de taille de chunk pré-existant inchangé).
- 2026-09-22 — **Phase 6 (unification carte/wiki/tableau) démarrée : Phase 1, volet vérifiable sans navigateur.** Plan approuvé après recherche (5 agents) et confirmation explicite de l'utilisateur sur les quatre choix les plus ambitieux : vraie fusion mono-DOM (pas juste une coquille sur iframes), modèle de pages multi-onglets dès maintenant, génération de sous-cartes vraiment scopée à la zone du burg (héritage de terrain), et un seul plan combiné. Détail complet dans `/home/mdubuis/.claude/plans/silly-questing-church.md`. La Phase 1 (spike de dérisquage, avant tout le reste) a deux volets : ce qui est vérifiable sans navigateur réel (fait maintenant) et ce qui exige un vrai rendu visuel/interactif (`jQuery UI` ouvre/se positionne correctement, D3 dessine, Leaflet monte sans le bug de double-transform) — explicitement pas quelque chose que cette session peut vérifier elle-même (pas d'outil de navigateur visuel, politique du projet de ne jamais lancer Playwright automatiquement), donc pas prétendu vérifié.
  - **Audit réel des collisions d'id** (`scripts/audit-id-collisions.mjs`, nouveau, testé dans `scripts/audit-id-collisions.test.mjs`, exposé via `npm run audit-ids`) : approx retenue via le vrai graphe d'imports de chaque point d'entrée (`src/main.ts` côté carte, `src/wiki-main.ts` côté wiki) plutôt qu'une heuristique par dossier. Résultat réel, pas une estimation : 722 ids côté carte, 59 côté wiki, **3 collisions seulement** (`leaflet-root`, `legacyPane`, `map`), toutes les trois tracées au même fichier déjà partagé (`src/components/leaflet-map.ts`, importé côté wiki uniquement pour la constante `LinearSimpleCRS` par `board-canvas.ts` — le code qui crée réellement ces ids, `getLeafletMap()`/`ensureContainer()`, n'est jamais appelé côté wiki aujourd'hui, donc ce sont des faux positifs de code mort, pas de vraies collisions vivantes). Le risque \"quasi garanti\" documenté dans MIGRATION.md s'avère donc bien plus petit en pratique qu'en pire cas. Vérification complémentaire (grep du binaire vendored `jquery-ui.min.js`) : le seul id fixe généré en interne par jQuery UI est un compteur `ui-id-N` partagé entre tous ses widgets — catégorie de risque réelle en principe mais non couverte par l'audit source (bibliothèque vendored, pas notre code), et actuellement sans objet puisque seul le côté carte charge jQuery UI.
  - **Mécanisme de repli shadow DOM construit et testé réellement** (`src/services/shadow-dom-bridge.ts`, 12 tests dans `shadow-dom-bridge.test.ts` avec un vrai jsdom, `// @vitest-environment jsdom`) : patch de `Document.prototype.getElementById`/`querySelector` pour retomber sur un shadow root enregistré seulement en cas d'échec du DOM clair — vérifié avec un vrai `attachShadow`/`ShadowRoot.getElementById` de jsdom, pas simulé. Décision de périmètre : seul un sélecteur `#id` nu retombe sur le shadow pour `querySelector` (pas les sélecteurs composés/de classe) — combiner DOM clair + shadow pour un sélecteur générique est sémantiquement ambigu et rien dans le code légué identifié par la recherche n'en a besoin. Fonctions de reset exportées pour l'isolation des tests, restaurant les vraies méthodes du prototype plutôt que de simplement basculer un flag (bug réel trouvé et corrigé en écrivant le module : un reset qui ne restaure pas l'original aurait double-enrobé le patch à une deuxième installation).
  - **Vrai bug trouvé en écrivant l'audit, pas théorique** : le garde d'entrée CLI du script (`import.meta.url === file://${process.argv[1]}`) ne correspondait jamais et le script s'exécutait silencieusement en no-op — le chemin du dépôt contient des caractères accentués (`Téléchargements`), encodés en pourcentage dans `import.meta.url` mais pas dans `process.argv[1]`. Corrigé avec `fileURLToPath` + comparaison de chemins résolus. Cassé uniquement découvert en exécutant réellement le script, pas par les tests seuls (le fixture de test utilise maintenant volontairement un chemin accentué pour éviter une régression silencieuse).
  - **Reste à faire avant que la Phase 1 puisse être considérée terminée** : construire le vrai élément personnalisé hébergeant le moteur carte légué réel (pas un jouet) dans un shadow root, la surcharge `$.ui.dialog.prototype.options.appendTo`, le correctif shadow-aware de `leaflet-map.ts`'s `ensureContainer()`, l'injection CSS dans le shadow root, et le chargeur séquentiel de scripts classiques — puis la vérification visuelle réelle par l'utilisateur (dialogue jQuery UI, rendu D3, montage Leaflet) avant que la porte de la Phase 1 puisse être considérée franchie.
  - **Vérifié** : `tsc --noEmit`, lint, 1089/1089 tests (12 nouveaux dans `shadow-dom-bridge.test.ts`, 8 nouveaux dans `audit-id-collisions.test.mjs`), `npm run test:scripts` (13/13), build (les deux points d'entrée, inchangés — aucun changement de bundling dans ce volet).
- 2026-09-22 — **Phase 6 Phase 1, suite : la vraie page de spike, écrite jusqu'à la limite de ce qui est vérifiable sans navigateur réel.** Après confirmation explicite de l'utilisateur ("j'écris le code, tu vérifies dans le navigateur") sur la façon de continuer malgré l'absence d'outil de navigateur visuel dans cette session.
  - **`src/spike/map-in-shadow.html` + `.ts`** (nouveau, dev-only, exclu du build — absent de `vite.config.ts`'s `rollupOptions.input`, atteignable seulement via `npm run dev` → `/spike/map-in-shadow.html`) : construit le mécanisme complet du plan — récupère le **vrai** `map.html` par `fetch()` (pas un sous-ensemble recopié à la main, pour ne jamais dériver silencieusement de la vraie page), injecte son contenu `<body>` dans un shadow root, charge les 6 scripts classiques synchrones puis les 7 différés dans le même ordre que `map.html`, redirige `$.ui.dialog.prototype.options.appendTo` vers le shadow root, reproduit les imports à effet de bord de `main.ts` et appelle le vrai `boot()` — pas un jouet.
  - **`src/components/leaflet-map.ts`'s `ensureContainer()` corrigé pour de vrai** (pas seulement dans le spike) : appelait `document.body.prepend(container)` directement, une écriture que le monkeypatch de `shadow-dom-bridge.ts` ne peut pas intercepter (il ne couvre que les lectures). Nouvelle fonction `getPrimaryMountRoot()` exportée par `shadow-dom-bridge.ts` (retourne le dernier shadow root enregistré, sinon `document.body` — donc sans effet tant qu'aucun shadow root n'est enregistré, ce qui est le cas partout sauf dans le spike).
  - **Deux vrais bugs trouvés et corrigés avant de committer, pas après** : (1) `shadow-dom-bridge.ts` capturait `Document.prototype.getElementById`/`querySelector` **au chargement du module**, pas à l'installation — cassait silencieusement 8 fichiers de test (`ReferenceError: Document is not defined`) dès que `leaflet-map.ts` (qui importe maintenant `getPrimaryMountRoot`) était atteint depuis l'environnement de test "node" par défaut (sans vrai DOM, voir `test-setup.ts`) ; corrigé en capturant paresseusement dans `installShadowDomBridge()`. (2) Le spike utilisait des chemins absolus codés en dur (`fetch("/map.html")`, `script.src = "/libs/..."`) — l'app est servie sous un préfixe (`/Fantasy-Map-Generator/` en dev, voir `vite.config.ts`'s `base`), donc ces requêtes auraient fait un vrai 404 dans un vrai navigateur ; trouvé en testant réellement les deux formes d'URL au `curl` (racine du domaine vs. préfixée) plutôt qu'en supposant, corrigé avec `import.meta.env.BASE_URL`.
  - **Vérifié réellement, pas supposé** : `tsc --noEmit`, lint, 1093/1093 tests, build (le spike est bien absent de `dist/`, confirmé). `npm run dev` lancé pour de vrai, chaque ressource que le spike demande au runtime vérifiée par `curl` avec le bon préfixe `/Fantasy-Map-Generator/` (page spike, script spike, `map.html`, les CSS, `libs/jquery-3.1.1.min.js`, `components/lifecycle.ts`) — tout répond 200, et la sortie JS transformée par Vite confirme que `import.meta.env.BASE_URL` et les alias `@/` sont bien résolus même pour cette page non déclarée comme point d'entrée.
  - **Ce que ceci ne prouve toujours pas, et qui reste pour l'utilisateur** : que la page s'affiche et se comporte correctement dans un vrai navigateur — un dialogue jQuery UI qui s'ouvre/se positionne/se style correctement à l'intérieur du shadow root, D3 qui dessine, Leaflet qui monte sans le bug de double-transform. C'est une vérification visuelle/interactive que cette session ne peut pas faire elle-même. La porte de la Phase 1 reste ouverte tant que ce retour n'est pas fait.
  - **Porte de la Phase 1 franchie** : l'utilisateur a ouvert `/spike/map-in-shadow.html` dans un vrai navigateur et confirmé que la checklist de vérification manuelle passe. Le mécanisme shadow DOM + repli DOM clair est donc validé de bout en bout, pas seulement sur les parties vérifiables sans navigateur. Passage à la Phase 2 (modèle de pages multi-onglets, indépendant de la Phase 1 par conception) autorisé ; la Phase 3 (vraie fusion mono-DOM appliquée en production) devient atteignable une fois la Phase 2 terminée.
- 2026-09-23 — **Phase 6 Phase 2 : modèle de pages multi-onglets.** Une page peut désormais bundler plusieurs onglets typés (wiki/board/map) derrière une seule entrée de sidebar, comme le vrai modèle LegendKeeper. Corrigé une vraie erreur dans le plan approuvé en cours de route, pas une nouvelle question ouverte pour l'utilisateur : le plan disait `tabs?: PageTab[]` (un tableau d'objets), mais le parseur YAML-lite (`frontmatter.ts`) ne peut représenter aucun tableau d'objets — vérifié en le relisant avant d'implémenter. `map_ref` résout déjà exactement ce problème en étant `Record<era, MapRef>` (clé de chaîne, pas un tableau) ; `tabs` suit le même schéma : `PageTabs = Record<string, PageTabMeta>`, la clé étant l'id d'onglet plutôt qu'un champ interne.
  - **`src/wiki/tabs.ts`** (nouveau module pur, testé, 12 tests) : stockage du contenu d'un onglet en bloc balisé ` ```<kind>:<tabId> `, généralisant board.ts's convention ` ```board ` sans jamais la modifier — un onglet board (` ```board:tab-2 `) et une page `type: board` classique (` ```board `) ne peuvent jamais entrer en collision, testé explicitement dans les deux sens.
  - **`src/wiki/board.ts` étendu, pas réécrit** : `extractBoardData`/`replaceBoardBlock`/`replaceBoardBlockInRaw` (déjà livrés, déjà utilisés en production) restent identiques au bit près — seule la logique de parsing JSON interne a été factorée dans un `parseBoardJson` privé, réutilisé par les nouvelles fonctions `extractBoardDataForTab`/`replaceBoardBlockForTab`/`replaceBoardBlockForTabInRaw`. Risque de régression sur la fonctionnalité board déjà livrée quasi nul par construction : 24/24 tests de `board.test.ts` toujours verts après coup, dont les 15 déjà existants avant ce chantier.
  - **`renderEntityView` (`wiki-main.ts`) gagne un second chemin de rendu, pas une réécriture du premier** : `hasExplicitTabs = Boolean(frontmatter.tabs && Object.keys(frontmatter.tabs).length)` — une entité sans `tabs` (toutes celles créées avant ce champ, et le cas normal pour une page wiki simple) emprunte exactement le chemin `isBoard`/`isEncounterTable` déjà existant, inchangé. Seule une page qui déclare `tabs` explicitement atteint la nouvelle barre d'onglets (`#/entity/<slug>?tab=<id>`, nouveau `route.tab` dans le routeur hash) et le nouveau dispatch par type d'onglet actif. Onglet `board` : réutilise `mountBoard`/`board-canvas.ts` tel quel, juste avec un `tabId` optionnel qui change quelles fonctions de stockage sont appelées. Onglet `map` : implémentation intérimaire assumée — un bouton "Open map" qui ouvre le panneau glissant déjà existant (`openMapPanel()`), pas l'incorporation inline complète qui est explicitement le travail de la Phase 3.
  - **`buildPageTree` : vraiment la même fonction, pas une nouvelle implémentation** — en relisant `buildEntityTree`, son algorithme ne regarde jamais `frontmatter.type` en interne ; la contrainte "même type seulement" documentée dans son commentaire n'a toujours été qu'une convention d'appel (les appelants pré-groupent par type avant d'appeler). `buildPageTree(entities)` est donc littéralement `buildEntityTree(entities)` appelée avec la liste complète — zéro nouvelle logique de cycle à tester, 100% du code déjà validé réutilisé tel quel. Pas encore câblée dans la sidebar (`renderSidebar` reste groupée par type comme avant) — cette refonte de la sidebar en arbre unique multi-types appartient à la Phase 3 ("la coquille de navigation persistante"), pas à ce morceau-ci.
  - **Vérifié** : `tsc --noEmit`, lint, 1121/1121 tests (28 nouveaux : `tabs.test.ts` 12, extensions de `board.test.ts` 9, extensions de `entities.test.ts` 7), build (les deux points d'entrée, inchangés). Pas de vérification navigateur réelle de la nouvelle UI d'onglets — reste à vérifier par l'utilisateur : créer une page avec un `tabs:` déclarant au moins un onglet wiki et un onglet board, confirmer que la barre d'onglets apparaît, que naviguer entre onglets via l'URL fonctionne, et que sauvegarder le contenu d'un onglet board ne touche ni le corps wiki ni un autre onglet.
- 2026-09-23 — **Phase 6 Phase 3 : vraie fusion mono-point-d'entrée, appliquée en production.** Suite au choix explicite de l'utilisateur (fusion réelle, pas juste une coquille par-dessus des iframes) — `vite.config.ts`'s `rollupOptions.input` n'a plus qu'une seule entrée (`index.html`) ; `map.html` n'est plus jamais servi comme document, seul son contenu est embarqué (voir ci-dessous). C'est le chantier le plus risqué de toute la session : remplace un mécanisme qui marchait (deux apps + iframes) par un tout nouveau, sans pouvoir le vérifier visuellement.
  - **`src/services/map-engine-host.ts`** (nouveau) : promotion du spike de la Phase 1 en module de production — API `mountMapEngine(container)`, démarrage une seule fois par chargement de page (rebooter deux fois doublerait les écouteurs `document`-level d'`initShell()`), réutilisation de la même instance en la déplaçant entre conteneurs plutôt qu'en la recréant. Le corps réel de `map.html` est désormais importé via `?raw` (`import mapHtmlRaw from "@/map.html?raw"`) et non plus récupéré par `fetch()` comme le spike le faisait — vérifié concrètement que `fetch()` aurait échoué en production : un vrai build Vite n'émet un fichier HTML que s'il est déclaré comme point d'entrée, et `map.html` ne l'est plus (confirmé en construisant sans lui : `dist/` ne contient alors que `index.html`). Ce pattern `?raw` sur un fichier `.html` existait déjà ailleurs dans ce dépôt (`auto-update.test.ts`), pas une invention de cette session.
  - **`src/index.html`/`wiki.css`** : `#map-panel`'s iframe devient un simple conteneur de montage (`#map-panel-mount`) — le panneau reste l'overlay plein écran déjà existant (changement de technologie interne, pas de refonte visuelle pour ce passage). `src/services/wiki-panel.ts` (bouton flottant "ouvrir le wiki" injecté dans l'ancien `map.html` autonome) supprimé entièrement — devenu sans objet, le wiki est désormais toujours la coquille, jamais quelque chose que la carte doit faire apparaître.
  - **`era-switcher.ts` adapté, pas réécrit** : ne s'auto-démarre plus sur son propre `DOMContentLoaded` (n'aurait plus de sens — la carte n'est plus son propre document avec son propre événement de chargement) ; appelé explicitement une fois par `map-engine-host.ts` juste après le vrai `boot()`. Bug réel trouvé en l'adaptant : ses styles étaient injectés dans `document.head`, qui ne pénètre jamais un shadow root (encapsulation CSS du Shadow DOM) — corrigé pour les injecter dans `getPrimaryMountRoot()`, comme l'élément lui-même.
  - **Pont "Place on map" reconverti de postMessage vers un bus d'événements interne** : forcé par la suppression de l'iframe elle-même (`sendToMapFrame`/`handlePlacementMessage` référençaient l'élément iframe par id — l'auraient fait planter, pas juste dormir, contrairement à ce qui avait été supposé au moment de reporter ce chantier). **Vrai piège de correction trouvé et évité, pas théorique** : `PLACEMENT_CANCEL` est bidirectionnel (le wiki demande l'arrêt, la carte confirme aussi l'arrêt de son propre chef) — dispatcher les deux directions sur le même `window` aurait fait que l'émission sortante du wiki redéclenche immédiatement son propre écouteur entrant, un écho same-window qui n'existait pas quand ça traversait une vraie frontière de fenêtre. Corrigé structurellement avec deux `EventTarget` dédiés par direction (`wikiToMapBus`/`mapToWikiBus`, `src/wiki/map-bridge.ts`) plutôt que par une convention fragile.
  - **Aucune réinvention du passage de paramètres d'URL** : le système de paramètres de FMG (`?burg=`, `?cell=`, `?seed=`, `?maplink=`) lit `location.search` (vraie query string), tandis que le routeur du wiki lit `location.hash` — les deux coexistent indépendamment sur une seule URL. `mapViewHref()` construit donc `?burg=5&scale=8#/map` : `url-params.ts` n'a pas eu besoin d'un seul changement. Compromis assumé et documenté dans le code : comme changer la query string déclenche toujours un vrai rechargement de page (contrairement au hash seul), cliquer "View on map" depuis une page wiki reste fonctionnel mais recharge toute la page au lieu d'être instantané — accepté pour ce passage plutôt que de construire un mécanisme de focus sans rechargement.
  - **Vrai bug de taille de bundle trouvé et corrigé avant de committer** : premier essai avec un `import { mountMapEngine } from "@/services/map-engine-host"` statique en haut de `wiki-main.ts` — le chunk wiki est passé de ~40 Ko à 838 Ko, parce que l'import `?raw` de `map.html` (le corps HTML complet) se retrouvait embarqué dans le chunk *eager* de tout le monde. Corrigé avec un `await import("@/services/map-engine-host")` dynamique au seul site d'appel (`renderMapView`). Vérifié ensuite avec le vrai chiffre de comparaison (pas une intuition) : `git stash` pour reconstruire exactement au commit de la Phase 2, chunk wiki alors à 42,40 Ko — mais `index.html` précédemment en `modulepreload` d'un chunk partagé "map-bridge" de ~236 Ko (Leaflet, partagé entre l'usage board du wiki et l'ancien moteur carte). Total avant ≈ 278 Ko eager, total maintenant : un seul chunk de 283,79 Ko — poids quasi identique, juste consolidé en un seul fichier puisqu'il n'y a plus de seconde entrée à qui le partager. Pas une régression, une fausse alerte initiale corrigée par une vraie mesure.
  - **Portée explicitement reportée, pas oubliée** : le chargement du monde connecté à la base (`connectedMap.seed/width/height`, ex-`mapFrameSrc()`) n'est pas encore branché dans `mountMapEngine` — nécessite un serveur Postgres local, chemin secondaire ; documenté comme lacune connue dans le code (`renderMapView`'s doc comment) plutôt que silencieusement perdu.
  - **Vérifié** : `tsc --noEmit`, lint, 1121/1121 tests, build (une seule entrée, `dist/map.html` bien absent, confirmé), audit id-collisions réexécuté sur l'état réel (5 collisions, toutes tracées à des modules désormais légitimement partagés). `npm run dev` lancé pour de vrai : chaque ressource que `map-engine-host.ts` demande au runtime (les 6 scripts classiques synchrones, les CSS, et les 14 modules de la séquence de boot répliquée) vérifiée par `curl` — tout répond 200. **Ce qui reste non vérifié, et ne peut pas l'être depuis cette session** : le rendu/comportement réel dans un navigateur — bouton "View Map" affiche la carte, un dialogue jQuery UI (éditeur de burg) s'ouvre correctement dans le shadow root, la carte se dessine, l'era switcher apparaît et fonctionne, fermer la carte revient au wiki, le flux "Place on map" complet (placer puis sauvegarder le `map_ref`), l'onglet carte d'une page multi-onglets, et qu'un retour/aller sur la route carte réutilise la même instance sans la rebooter. C'est le chantier le plus important à vérifier visuellement de toute la session.
- 2026-09-23 — **Page d'atterrissage : choix explicite "monde existant" / "créer un nouveau", qui atterrit sur les réglages de génération.** Demande directe de l'utilisateur, construite sur la Phase 3 (a besoin du moteur carte fusionné pour ouvrir ses réglages inline).
  - **Réutilise `renderChooseWorldView` existant plutôt que d'inventer un nouvel écran** : c'était déjà l'écran vers lequel `connectToPersistedWorld()` envoie tout visiteur sans monde persisté — exactement "la page d'atterrissage" décrite par l'utilisateur. Ajout d'un bouton "+ Create a new world…" (`CREATE_WORLD_HTML`, lien `href="#/map?new=1"`) au-dessus de la liste des mondes existants, dans les trois branches (chargement, chargée, erreur réseau) — visible même hors-ligne, puisque créer un monde est la génération côté client du moteur existant, qui ne touche jamais le serveur API.
  - **Vrai bug évité avant qu'il n'existe, pas trouvé après coup** : le nouveau bouton partage la classe `.world-list-item` avec les boutons de mondes existants pour un style cohérent, mais le gestionnaire de clic délégué (`querySelectorAll(".world-list-item")` → `loadDatabaseEntities(Number(button.dataset.mapId))`) s'y serait attaché aussi — un clic aurait appelé `loadDatabaseEntities(NaN)` puisque c'est un `<a href>` sans `data-map-id`. Corrigé en scopant le sélecteur à `button.world-list-item` (le nouveau lien est un `<a>`, pas un `<button>`) avant même de tester, en relisant le code plutôt qu'en le découvrant à l'usage.
  - **`services/map-engine-host.ts` gagne `openGenerationSettings()`** : supprime l'overlay "no map yet" (`#generateIdleState`, qui s'affiche toujours sur un tout premier démarrage) au lieu de le laisser empilé par-dessus le panneau de réglages, puis appelle `showOptions()` (`components/options/options-panel.ts`, déjà chargé par la séquence de boot, importé dynamiquement ici) et simule un clic sur `#optionsTab` (confirmé dans `map.html` : c'est l'onglet "Options" avec le data-tip \"Click to change generation and UI options\" — pas l'onglet Layers, actif par défaut). Même technique `elementById(...).click()` que `load.ts` utilise déjà pour sélectionner un onglet par programme (`ensureEl("styleTab").click()`) — réutilisée, pas inventée. Fonctionne à travers le shadow root grâce au monkeypatch de la Phase 1 (`document.getElementById` retombe sur le shadow root enregistré), donc aucune modification supplémentaire nécessaire pour que `ensureEl`/`document.getElementById("optionsTab")` trouvent l'élément réel.
  - **Route étendue, pas dupliquée** : `{ view: "map"; newWorld?: boolean }`, parsé depuis `#/map?new=1` — même convention que `?tab=` sur les pages multi-onglets (Phase 2). `renderMapView(newWorld?)` appelle `openGenerationSettings()` seulement quand demandé, après que `mountMapEngine()` se soit résolu.
  - **Vrai gap trouvé en relisant `connectToPersistedWorld()`, pas théorique** : un visiteur hors-ligne (ou serveur API injoignable) sur sa toute première visite ne passait JAMAIS par `#/choose-world` — le `catch` du fetch échoué tombait directement sur `renderHomeView()` (l'accueil wiki générique), sans jamais montrer le choix monde-existant/nouveau-monde, même si créer un monde ne dépend d'aucun serveur. Corrigé en séparant la détection \"première visite\" de la réussite du fetch : le redirect vers `#/choose-world` se déclenche maintenant sur \"pas de monde connecté\" (succès sans correspondance OU échec du fetch) ET \"première visite\", au lieu de dépendre implicitement du succès du fetch.
  - **Vérifié** : `tsc --noEmit`, lint, 1121/1121 tests (aucun nouveau — pas de précédent de tests unitaires directs sur `wiki-main.ts` dans toute la session, cohérent avec l'existant), build (chunks wiki/map-engine-host quasi inchangés, pas de régression de taille). `npm run dev` : les modules `components/options/options-panel.ts` et le nouvel export `openGenerationSettings` vérifiés résolubles par le serveur de dev. **Non vérifié visuellement** (comme tout ce qui touche le moteur carte fusionné) : que cliquer "+ Create a new world" affiche bien le panneau de réglages sur l'onglet Options plutôt que l'état idle, et que l'onglet sélectionné est visuellement correct.
- 2026-09-23 — **Phase 6 Phase 4 (démarrée) : héritage de terrain pour les sous-cartes de burg, validé contre des données réelles.** Sur instruction explicite de l'utilisateur de continuer malgré la Phase 3 pas encore vérifiée visuellement. Contrairement à la Phase 3 (impossible à vérifier sans navigateur), cette pièce est vérifiable de bout en bout sans navigateur — vrai Postgres local disponible (7 mondes réels déjà dedans), donc vérifiée pour de vrai, pas seulement par lecture de code.
  - **Migration de schéma non destructive sur une vraie base avec de vraies données** : `docker compose down -v` (suggéré par `server/README.md` pour appliquer un changement de `schema.sql`) aurait effacé les 7 mondes déjà importés — refusé, remplacé par des `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` appliqués directement sur la base en cours d'exécution (`maps.parent_map_id`/`parent_burg_id`, `map_burgs.child_map_id`, tous nullables), vérifié que les 7 mondes existaient toujours après (`SELECT count(*) FROM maps` avant/après). `schema.sql` mis à jour en parallèle pour les futures installations fraîches.
  - **Le crochet dans le pipeline de génération, pas une réécriture** : `GenerationContext` (`generation-pipeline.ts`) gagne un `parentHeights?: Uint8Array` optionnel ; seule l'étape `"heightmap"` change, avec un embranchement conditionnel vers la nouvelle `HeightmapGenerator.fromParentSlice(graph, parentHeights)` — sinon exactement le chemin existant, `HeightmapGenerator.generate()`, inchangé. Confirmé en lisant `Grid.prepare()` que passer un graph déjà construit ne fait que réutiliser ses points/sa topologie (aucune régénération, aucun nouveau tirage du PRNG) — la seule vraie inconnue du plan (comment obtenir un vrai terrain hérité sans dupliquer 30+ étapes du pipeline à la main) se résout donc par une seule condition sur une seule étape existante.
  - **`server/src/generation/detail-map.ts`** (nouveau) : requête PostGIS (`ST_Intersects` + `ST_MakeEnvelope`, réutilise l'index GIST déjà présent sur `map_cells.geom`) pour extraire les cellules du monde parent dans un rectangle centré sur le burg ancre ; ré-échantillonnage au plus proche voisin (le point de chaque cellule de la nouvelle grille est projeté dans l'espace du parent, la hauteur de la cellule parent la plus proche est prise directement — pas de lissage/interpolation, choix assumé pour une première version qui doit être correcte avant d'être polie). Le monde parent n'est jamais modifié (lectures seules sur ses tables).
  - **Deux vrais bugs trouvés en exécutant le code pour de vrai, pas en le relisant** : (1) `const { Grid } = await import("@/generators/grid-generator")` — `Grid` n'est pas un export nommé, seulement un global (`window.Grid`, même pattern que `HeightmapGenerator` déjà rencontré en Phase 1) ; le premier essai a planté immédiatement (`Cannot read properties of undefined`). (2) Mon propre script de test supposait `packExport.cells.cells.h` (tableau de hauteurs à plat) alors que `packToJson` produit un tableau d'objets-cellules avec `.h` par cellule — aucun des deux n'aurait été détecté par un simple `tsc --noEmit`, qui **ne couvre même pas `server/`** (le `tsconfig.json` racine est scopé à `"include": ["src"]`, confirmé en le relisant) — seule l'exécution réelle les a révélés.
  - **Vérifié contre de vraies données, via curl sur le vrai serveur Fastify, pas seulement en lisant le code** : `POST /api/maps/8/burgs/8/generate-detail` (burg réel "Crozolym", population 47203) a créé un vrai monde (id 10) — hauteurs parentes de la tranche : min 18 max 21 moyenne 19.9 (70 cellules réelles) ; hauteurs de la carte de détail générée : min 18 max 21 moyenne 20.3 (9041 cellules) — la correspondance quasi exacte confirme que l'héritage de terrain est réel, pas du bruit. `parent_map_id`/`parent_burg_id`/`child_map_id` vérifiés persistés correctement des deux côtés en base après coup. Chemin d'erreur testé aussi : burg inexistant → 404 propre, la file de génération partagée (`generationQueue`) n'est pas restée bloquée après l'échec.
  - **Vrai gap confirmé visible, pas juste théorique** : la carte de détail générée (300×300, densité minimale) contient 1754 burgs et 27 états — beaucoup trop pour une "carte de détail" d'un seul quartier. Ce n'est pas un nouveau bug : c'est exactement le manque déjà identifié dans le plan approuvé et confirmé par l'utilisateur ("cultures/états contraints au burg d'ancrage") — pas encore construit dans ce passage. La génération de cultures/états/burgs tourne aujourd'hui à pleine échelle, sans savoir qu'elle est censée être bornée au voisinage d'un seul burg. Prochaine étape logique de la Phase 4, pas cette session.
  - **Portée explicitement non traitée dans ce passage, pas oubliée** : (1) la contrainte culture/état à la culture/l'état du burg d'ancrage — nécessite de toucher aux algorithmes de génération organique de `Cultures.generate()`/`States.generate()`, un chantier séparé et délicat, pas improvisé à la suite du reste. (2) Le point d'entrée UI ("Generate detail map" sur un burg) — prématuré tant que le résultat produit un nombre de burgs absurde ; exposer cette fonctionnalité à l'utilisateur avant que (1) soit réglé serait trompeur.
  - **Vérifié** : `tsc --noEmit` (côté client seulement — voir ci-dessus, `server/` non couvert), lint, 1123/1123 tests (2 nouveaux, `heightmap-generator.test.ts`, premier fichier de test de ce module), build (aucune régression). Plus, exceptionnellement pour cette session : vraie exécution de bout en bout contre le vrai Postgres et le vrai serveur Fastify (voir ci-dessus) — la vérification la plus forte possible ici, bien au-delà de ce qui est possible pour tout ce qui touche au moteur carte fusionné (Phases 1-3), qui reste bloqué sur une vérification navigateur.
- 2026-09-23 — **Phase 6 Phase 4, suite : contrainte culture/état à l'ancre, sur instruction explicite de continuer et fermer les manques identifiés.** Les deux manques signalés dans l'entrée précédente ("(1) contrainte culture/état, (2) point d'entrée UI") sont maintenant fermés — (1) complètement, (2) voir plus bas.
  - **La contrainte s'obtient en paramétrant l'algorithme existant, pas en le réécrivant** : `Cultures.generate()`/`States.generate()`/`Religions.generate()` prennent déjà "combien en générer" comme entrée (`options.generation.{cultures,states,religions}.limit`) — leur demander exactement 1 fait que l'algorithme organique déjà là, non modifié, remplit toute la zone peuplée avec une seule culture/état/religion de lui-même. Doit être réglé APRÈS `Options.randomize()` (déjà appelé plus haut dans le flux) : confirmé en lisant `randomize()` que `states.limit`/`cultures.limit`/`burgs.limit` y sont eux-mêmes randomisés (`gauss(18,5,2,30)`, etc.) — régler avant aurait été écrasé. `burgs.limit` fixé à un petit nombre explicite (`request.burgsLimit ?? 12`), pas au mode "auto" (`AUTO_BURG_LIMIT`) que `randomize()` aurait choisi : sa formule *augmente* la densité de burgs par point sur une carte plus petite, l'inverse de ce qu'une carte de détail veut.
  - **Reliquat après génération, pas une nouvelle génération** : `relabelToParent()` renomme (et recolore) l'unique culture/état réel généré (index 1 — confirmé en lisant `states-generator.ts` que l'index 0 est toujours le placeholder "Neutrals") avec le nom/la couleur réels du burg d'ancrage, requêtés depuis `map_states`/`map_cultures`/`map_religions` sur le monde parent. Pur changement d'étiquette sur une sortie déjà générée, aucun changement de comment elle a été générée.
  - **Un vrai bug d'algorithme existant déclenché par un cas limite, trouvé en ré-exécutant contre les vraies données, pas en relisant le code** : `provinces.ratio = 0` (mon premier essai, pour "aucune province") a produit **5220 provinces sur une carte de 5631 cellules** — quasi une province par cellule. Confirmé en lisant `provinces-generator.ts` : la formule de croissance du flood-fill est `gauss(20,5,5,100) * ratio ** 0.5` — `ratio=0` l'effondre exactement à 0, ce qui bloque l'expansion des provinces dès la première cellule de départ, un cas limite que le vrai curseur de l'UI ne peut jamais atteindre (il ne descend jamais à 0) et que l'algorithme n'a manifestement jamais été pensé pour gérer proprement. Pas touché à `provinces-generator.ts` (chirurgie sur un algorithme organique délicat, hors de portée) — corrigé en évitant simplement la valeur limite : `ratio = 20` (la valeur par défaut du moteur lui-même). Rejoué contre les vraies données après correction : 9 provinces, un nombre raisonnable. **Piège réel évité en cours de route** : le premier nouveau test après la correction du code source a montré EXACTEMENT le même résultat cassé (5234 provinces) — pas parce que la correction était fausse, mais parce que le serveur Fastify de test tournait déjà et gardait l'ancien module en cache (Node ne réimporte pas un module déjà chargé) ; le vrai test n'a eu lieu qu'après un redémarrage explicite du serveur — sans quoi j'aurais pu conclure à tort que la correction ne marchait pas.
  - **Vérifié contre de vraies données, de bout en bout, pour de vrai** : burg réel "Imblos" (monde 8) → nouvelle carte de détail avec 1 état, 1 culture, 13 burgs, 9 provinces, quelques religions — état et culture renommés exactement "Lesososia" (#fc8d62) et "Hellenic", confirmés identiques à ceux du burg parent par une requête SQL de comparaison directe (jointure monde parent ↔ monde détail). Cartes de test intermédiaires cassées (celles avec 1754 burgs ou 5220+ provinces) supprimées de la base après coup — celles créées cette session uniquement à des fins de vérification, pas du contenu de l'utilisateur.
  - **Vérifié** : `tsc --noEmit`, lint, 1123/1123 tests (inchangé, ce passage est entièrement côté serveur), aucune régression. Toujours vérifié en priorité par exécution réelle contre Postgres/Fastify plutôt que par lecture de code seule, cohérent avec le reste de la Phase 4.
- 2026-09-23 — **Phase 6 Phase 4, fin : le point d'entrée UI ("Generate detail map" sur un burg).** Dernier des deux manques signalés dans l'entrée précédente, plus prématuré maintenant que la contrainte culture/état produit des comptes sains.
  - **État partagé entre les deux moitiés de la fusion mono-DOM (Phase 3), pas un nouveau global `window`** : `src/wiki/db-entities.ts` gagne `setConnectedMapId`/`getConnectedMapId` (simple variable de module, pas `window`) — écrite par `wiki-main.ts` (`loadDatabaseEntities()`, juste après `connectedMap = mapInfo`), lue par `burg-editor.ts` (partie du moteur carte hébergé en shadow DOM) pour savoir s'il existe un monde Postgres connecté du tout. Fonctionne uniquement parce que la Phase 3 a fusionné les deux côtés dans un seul graphe de modules JS — un import ordinaire suffit, pas de pont `postMessage`/bus d'événements nécessaire ici puisqu'aucune frontière de fenêtre ne sépare plus ces deux fichiers.
  - **`API_BASE` dédupliqué, pas dupliqué à nouveau** : `wiki-main.ts` définissait déjà sa propre constante locale ; déplacée vers `db-entities.ts` comme unique source (`wiki-main.ts` l'importe désormais au lieu de la redéclarer) puisque `burg-editor.ts` en avait aussi besoin — un seul endroit à changer si l'URL du serveur API change un jour.
  - **Bouton rendu conditionnellement dans le template, pas caché en CSS** : `burgGenerateDetailMap` n'apparaît dans le HTML du dialogue burg (et n'est écouté) que si `getConnectedMapId() !== undefined` — suit le pattern déjà établi de `burgWikiLink` juste au-dessus (un lien `<a>` construit dynamiquement) plutôt que d'ajouter un mécanisme d'affichage/masquage séparé. Un monde chargé depuis un fichier `.map` local sans connexion Postgres ne voit simplement jamais ce bouton, cohérent avec le fait que la génération de sous-carte est une fonctionnalité côté serveur.
  - **Le clic appelle `requestDetailMap(API_BASE, parentMapId, burg.i)`** (nouvelle fonction dans `db-entities.ts`, POST vers l'endpoint déjà construit et vérifié en Phase 4), désactive le bouton pendant la requête, rapporte succès/échec via `tip()` (déjà utilisé ailleurs dans ce fichier) — pas de nouveau mécanisme de notification inventé.
  - **Vérifié contre le vrai serveur, pas seulement lu** : serveur Fastify redémarré à neuf (leçon du cache de modules Node de l'entrée précédente toujours valable), rejoué exactement la requête que `requestDetailMap()` envoie (même méthode, en-têtes, corps) contre un burg réel ("Testville", monde 9) via curl — 201, 1 état, 1 culture, 13 burgs, 8 provinces, comptes sains. Carte de test (id 14) supprimée après coup, comme les précédentes. **Ce qui reste non vérifié, et ne peut pas l'être depuis cette session** : le rendu/comportement réel du bouton dans un navigateur — qu'il apparaît bien seulement quand un monde est connecté, que le clic déclenche la requête, que le `tip()` de succès/échec s'affiche correctement par-dessus le dialogue burg hébergé en shadow root. Cohérent avec tout le reste du travail touchant le moteur carte fusionné depuis la Phase 3.
  - **Vérifié** : `tsc --noEmit`, lint, 1123/1123 tests, build (aucune régression de taille de chunk). Les deux manques de l'entrée Phase 4 précédente sont maintenant fermés. Phase 4 terminée ; Phase 5 (polish d'intégration, passage de régression complet, nettoyage doc) est la suite, pas encore démarrée.
- 2026-09-23 — **Phase 6 Phase 5 : polish d'intégration — l'onglet carte devient vraiment incorporé, pas un bouton.** Sur instruction explicite de continuer. Le plan approuvé dit seulement "cross-tab features within one page" — vague par construction ; interprété concrètement comme fermer le TODO le plus concret et déjà signalé du plan (le commentaire laissé en Phase 2 : "full inline map embedding lands in Phase 3"), plutôt que d'inventer une fonctionnalité non spécifiée. Appel d'interprétation notable, donc noté ici explicitement plutôt que décidé en silence.
  - **`mountMapEngine(container)` acceptait déjà n'importe quel conteneur depuis la Phase 3, mais rien ne l'appelait pour un onglet carte** — `renderEntityView`'s branche `isMapTab` (`wiki-main.ts`) affichait juste un bouton "Open map" ouvrant l'overlay plein écran existant. Remplacé par un vrai montage inline : une nouvelle `mountMapTabView()` appelle `mountMapEngine(el("map-tab-mount"))` directement dans l'espace de contenu de la page, exactement le même moteur singleton que l'overlay (`renderMapView`) utilise — pas une seconde instance, pas de nouveau mécanisme de boot.
  - **Vrai bug de superposition CSS trouvé et corrigé avant de committer, pas théorique** : premier essai avec `#map-tab-mount` en `position: absolute; inset: 0` directement sur `#map-tab-panel` (`position: relative`) — fonctionne quand il n'y a pas de bannière d'avertissement au-dessus, mais un élément positionné se peint toujours par-dessus un élément en flux normal dans le même contexte d'empilement, peu importe l'ordre DOM : le message `#map-tab-mismatch` (en flux normal, au-dessus dans le HTML) aurait donc été invisible, caché sous la carte plutôt qu'au-dessus. Corrigé en copiant réellement le pattern déjà établi par `#map-panel`/`#map-panel-header`/`#map-panel-mount` (`display: flex; flex-direction: column`, le montage en `flex: 1; min-height: 0; position: relative`) au lieu d'un `inset: 0` improvisé — la bannière garde sa vraie place dans le flux, la carte occupe le reste. `#map-tab-panel` gagne aussi une vraie hauteur (`height: 70vh; min-height: 400px`), remplaçant son ancien style de "carte placeholder" (padding centré, fond `--bg-alt`) qui n'avait de sens que pour le texte "Open map" qu'il contenait avant.
  - **Vrai gap trouvé en écrivant ce code, pas nouveau — le même gap déjà signalé fin de Phase 3, maintenant doublement pertinent** : `PageTabMeta.mapId` ("quel monde Postgres cet onglet affiche") n'est branché nulle part — le moteur reste un singleton global qui affiche toujours le même monde chargé, quel que soit l'onglet. Pas résolu ici (nécessite le même câblage seed/width/height dans `mountMapEngine` déjà reporté depuis la Phase 3, un vrai chantier séparé). Au lieu de l'ignorer silencieusement : quand `activeTab.mapId` diffère du monde effectivement connecté (`getConnectedMapId()`), un message inline explicite l'indique ("showing that below... switching a tab to a specific database world without a full reload isn't wired up yet") plutôt que de laisser croire que l'onglet montre le bon monde.
  - **Comportement différent de l'overlay, accepté, pas un bug** : `#map-panel` (l'overlay plein écran) n'est jamais retiré du DOM, seulement masqué via `[hidden]` — le host shadow-DOM y reste physiquement attaché en permanence. Un onglet carte vit lui dans `#content`, réécrit en entier (`innerHTML =`) à chaque changement d'onglet/navigation — quitter l'onglet carte détache donc réellement le host (toujours vivant en mémoire via la fermeture `bootPromise`, juste orphelin jusqu'au prochain montage). Un rendu SVG/D3 ne dépend pas de rester attaché au DOM entre deux affichages, donc sans conséquence fonctionnelle connue — mais une vraie différence d'implémentation par rapport à l'overlay, documentée ici plutôt que découverte plus tard sans explication.
  - **Vérifié** : `tsc --noEmit`, lint, 1123/1123 tests (inchangé — aucun précédent de test unitaire direct sur `wiki-main.ts`, cohérent avec le reste de la session), build (chunk `wiki` quasi inchangé, 284,83 Ko vs 284,51 Ko avant — l'import dynamique de `map-engine-host.ts` reste bien paresseux, pas de régression du bug de taille de bundle trouvé en Phase 3). **Checklist de vérification navigateur réelle, à faire par l'utilisateur** (aucun outil de navigateur automatisé dans cette session) : ouvrir une page dotée d'un onglet `type: map`, confirmer que la carte s'affiche directement dans l'onglet (pas de bouton "Open map"), que naviguer vers l'onglet wiki puis revenir à l'onglet carte réaffiche la même carte sans la re-générer, que l'overlay plein écran ("View Map" dans la barre latérale) réutilise la même instance sans la re-booter, et — si un monde Postgres est connecté avec un id différent du `mapId` déclaré sur l'onglet — que le message d'avertissement inline s'affiche correctement.
  - **`MIGRATION.md` mis à jour en parallèle** : note ajoutée à sa Phase 4 point 1 enregistrant que la décision "pas de DOM partagé" y a été délibérément rouverte par cette initiative, avec le renvoi vers le mécanisme qui l'a rendue sûre (Phase 1 : shadow DOM + repli `document.getElementById`, audit de collisions d'id).
  - Phase 5 terminée — fermeture du plan approuvé en 5 phases pour cette initiative. Voir plus bas pour la clôture formelle en tant que "Phase 6" de la feuille de route de plus haut niveau (section Phases).
- 2026-09-23 — **Deux vrais bugs de vérification navigateur, trouvés en utilisant un vrai navigateur (Playwright, invoqué manuellement pour déboguer un rapport de bug précis, pas comme gate automatique) après que l'utilisateur a rapporté "+ Create a new world" menant à une page vide.** Exactement le risque que chaque entrée de ce journal depuis la Phase 1 flagair comme "non vérifiable sans navigateur" — confirmé en pratique, et les deux bugs étaient invisibles à la lecture de code seule (aucune erreur réseau, `tsc`/lint/tests tous verts avant comme après).
  - **Bug de race condition, réel mais PAS la cause du rapport de l'utilisateur** : `connectToPersistedWorld()` capturait `isFirstVisit = currentRoute().view === "list"` AVANT son `await fetchAvailableMaps(...)` — si l'utilisateur navigue ailleurs (ex. clique "+ Create a new world") pendant que ce fetch est en vol, la redirection vers `#/choose-world` se déclenche quand même après coup, contredisant le commentaire de la fonction elle-même ("leaves a direct deep link alone rather than yanking it away"). Corrigé en relisant `currentRoute()` au moment de la décision plutôt qu'une capture figée. Un vrai bug, corrigé, mais **insuffisant seul** — l'utilisateur a signalé que ça ne réglait pas son problème, ce qui a motivé la suite.
  - **Bug réel n°1, la vraie cause du "page vide" — `#optionsContainer` poussé ~1700px hors de l'écran visible.** `map.html`'s `<head>` porte un `<style>` inline critique (`#map { position: absolute; }`, entre autres) que `map-engine-host.ts` n'a jamais extrait ni réinjecté dans le shadow root — seules les feuilles de style externes (`STYLESHEETS`) l'étaient. Sans cette règle, le SVG de la carte retombe en position statique et, dimensionné à la taille réelle de la carte, pousse tout ce qui vient après lui dans le flux normal du document — y compris tout `#optionsContainer` (donc le panneau "Generate a new map") — largement sous le viewport visible. Le panneau existait bel et bien en DOM (`display:block`, contenu réel de 16 Ko de HTML), d'où l'echec de toute vérification par lecture de code ou par état DOM seul ; seule une vraie capture d'écran l'a révélé (`getBoundingClientRect().top` ≈ 1700px vs viewport ≈ 700px). Corrigé en parsant `map.html` une seule fois pour en extraire À LA FOIS le `<head>`'s `<style>` et le `<body>` (`parseLegacyDocument()`, `legacyHeadStyles()`), le style étant maintenant injecté dans le shadow root aux côtés du corps.
  - **Bug réel n°2, trouvé en cherchant s'il y avait d'autres erreurs après le premier fix — `styleElementSelect is not defined`, cassant tout `public/modules/ui/style.js` après sa première ligne.** Ce script classique (jamais converti en TS, ~1000 lignes) référence des éléments par leur id comme des variables globales bareword (`styleElementSelect.value`, pas `document.getElementById("styleElementSelect")`) — un vrai comportement natif du navigateur ("named access on the Window object") qui n'existe QUE pour la lumière-DOM du vrai `document`, jamais pour un shadow root. Recherche systématique (script Python comparant les ids de `map.html`/`style-tab.ts` contre les identifiants bareword non déclarés dans `style.js`) : **plus de 130 identifiants distincts** utilisés ainsi rien que dans ce fichier — bien trop pour convertir un par un sans réécrire un fichier legacy que CLAUDE.md dit de ne pas réécrire. Corrigé par un mécanisme général, pas un patch ciblé : `exposeLegacyIdsAsGlobals(shadow)`, qui reproduit le comportement natif du navigateur (`window[id] = element` pour tout élément avec un id, sauf collision avec un global existant) — appelée une seule fois, juste avant que les scripts classiques différés (`DEFERRED_SCRIPTS`, dont `style.js`) ne chargent, moment où tout le contenu (statique ET injecté dynamiquement par `options-tab.ts`) existe déjà. Même philosophie que le monkeypatch `getElementById`/`querySelector` de la Phase 1 : restaurer l'environnement que le code legacy suppose, pas le réécrire.
  - **Vérifié réellement avec un vrai navigateur, pas supposé** : Playwright installé et lancé manuellement (`npx playwright install chromium`, non ajouté au flux de vérification automatique — reste hors du "no-auto-Playwright policy" du projet, utilisé ici comme outil de diagnostic ponctuel) contre le vrai serveur de dev. Avant les fixs : `pageerror` "styleElementSelect is not defined" capturé, `#optionsContainer.getBoundingClientRect().top` ≈ 1704px (hors écran). Après les deux fixs : zéro erreur console, `#optionsContainer.top` ≈ 53px (aligné avec le haut du panneau carte), capture d'écran confirmant visuellement le panneau Options complet et fonctionnel ; onglet Style cliqué ensuite, `styleElementSelect` confirmé résolu comme élément réel avec 41 options, toujours zéro erreur. `tsc --noEmit`, lint, 1123/1123 tests, build, tous verts avant et après (aucun de ces deux bugs n'était détectable par ces outils). Scripts de diagnostic temporaires supprimés après usage, jamais committés.
