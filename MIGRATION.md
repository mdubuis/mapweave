# Migration Postgres/PostGIS + Génération serveur + Leaflet + Wiki unifié

Approuvé le 2026-09-13. Voir `MAPWEAVE.md` pour l'historique des phases précédentes (1-4 : nettoyage,
wiki, liens map↔wiki, timeline) que ce document ne remplace pas — il s'agit d'une migration
ultérieure et bien plus large, avec son propre suivi de phases ci-dessous.

## Contexte

Mapweave tourne aujourd'hui entièrement côté client : moteur de génération procédurale FMG (Voronoi/climat/hydrologie/cultures, ~150 fichiers TS), rendu 100% SVG/D3, wiki Markdown séparé (`wiki.html`) relié à la carte (`index.html`) par un `map_ref` et une iframe. Zéro backend, site statique.

L'utilisateur souhaite migrer vers : un backend PostgreSQL + PostGIS (local, mono-utilisateur, sans authentification), une génération procédurale qui tourne côté serveur plutôt que dans le navigateur, un rendu de carte basé sur Leaflet (couches vectorielles GeoJSON, pas un pipeline de tuiles complet), une vue carte intégrée *dans* le wiki (plus de page séparée), toutes les entités générées navigables dans une arborescence claire du wiki, et une génération déclenchée manuellement plutôt qu'automatique au chargement.

Trois recherches approfondies (rendu SVG, persistance/génération, dépendances navigateur du moteur) ont confirmé la faisabilité et localisé précisément les risques : le moteur de génération est étonnamment portable côté Node (un seul vrai blocage), mais le rendu Leaflet est un chantier majeur (~85-90 fichiers touchent des API SVG directement). Ce document découpe le travail en phases indépendamment livrables, en gardant l'app fonctionnelle à chaque étape plutôt qu'une réécriture big-bang.

**Comment utiliser ce plan** : chaque phase est un jalon validable séparément, dans l'esprit des phases précédentes du projet (voir `MAPWEAVE.md`). Rien n'empêche de s'arrêter après la Phase 0 ou la Phase 3 si le reste s'avère ne pas valoir le coût une fois vécu.

---

## Décisions de conception

**Arborescence wiki = vue live sur la base, pas des fichiers générés.** Avec des centaines à quelques milliers d'entités par carte (burgs, states, etc.), générer un fichier Markdown par entité produirait un dépôt dominé par du bruit généré, en conflit avec l'usage actuel (fichiers `wiki/*.md` = lore écrit à la main, versionné proprement). La solution : le wiki charge deux sources fusionnées — les fichiers Markdown existants (inchangé) + les entités interrogées en direct depuis l'API Postgres, normalisées dans la même forme `WikiEntity`. Une action "développer en fiche de lore" sur n'importe quel nœud généré crée alors un vrai fichier `.md`, réutilisant le flux de création déjà construit en Phase 3 du projet (`#/new?mapKind=...`).

**CRS = SRID 0 (Cartésien), jamais EPSG:4326.** Il n'existe aujourd'hui aucune projection géographique réelle — `pack.cells.p`/`grid.points` sont des `[x,y]` plats dans l'espace `options.map.graph.{width,height}`. C'est exactement le cas d'usage de **`L.CRS.Simple`** dans Leaflet (cartes "à plat", jeux, images). Traiter ces coordonnées comme des degrés de latitude/longitude serait faux.

**Renderer Leaflet = `L.svg()`, pas `L.canvas()`.** Beaucoup de code actuel lit la géométrie DOM directement (`getBBox`, `getTotalLength`, `getPointAtLength` — placement de labels/emblèmes, outil règle, migration de fichiers anciens). `L.svg()` garde de vrais éléments `<path>` par entité, donc ce code s'adapte (`layer.getElement()` au lieu d'une sélection d3 brute) plutôt que d'être totalement réécrit. `L.canvas()` n'a aucun nœud DOM — casserait ce code *et* la délégation de clics simultanément. Un layer Canvas ciblé reste possible plus tard pour la maille brute de cellules (10-100K polygones) si le profilage le justifie.

**Identifiants d'entités = ceux du moteur FMG, pas des clés auto-incrémentées fraîches.** Les tables Postgres doivent être clées `(map_id, id)` où `id` réutilise l'id numérique déjà assigné par le générateur (burg.i, state.i, etc.). Sinon, tous les `map_ref` déjà écrits à la main dans le wiki existant (Phases 3-4 précédentes) cassent silencieusement.

**Pas de synchronisation offline.** Déploiement local mono-utilisateur explicitement choisi → `options.app`/`options.generation` restent en `localStorage` comme aujourd'hui ; IndexedDB ne garde que de l'état UI transitoire. Aucune file d'attente/résolution de conflits à construire.

---

## Phases

| # | Portée | Dépend de | Risque | Casse l'existant si mal fait | Statut |
|---|---|---|---|---|---|
| 0 | Génération manuelle au chargement | rien | Faible | Non — un seul branchement isolé | **Fait** |
| 1 | Schéma Postgres/PostGIS + ETL one-way (CLI), rien de branché à l'app | Docker | Faible | Non — l'ancien flux `.map` reste intact | **Fait** |
| 2 | Serveur API Node, lecture seule sur Postgres | Phase 1 | Moyen | Non — purement additif | **Fait** |
| 3 | Génération portée sur Node, `POST /api/maps` génère côté serveur | Phase 2 | **Élevé** | Seulement si la parité seed-à-seed n'est pas validée | **Fait*** |
| 4 | Le wiki devient la coquille hôte ; carte intégrée (encore en SVG) ; arborescence live | Phase 2 | Moyen | Oui, au système d'ères et aux `map_ref` — voir plus bas | **Fait*** (*jumelage ères/`map_id` volontairement reporté, voir plus bas) |
| 5 | Migration Leaflet, incrémentale, couche par couche | Phase 3 + Phase 4 | **Le plus élevé** (~85-90 fichiers) | Outils interactifs (règle, minimap, labels) les plus exposés | **En cours** (caméra + 7 couches faites : biomes/religions/cultures/provinces/states/rivers/routes ; burgs/marqueurs pas encore ; délégation de clics pas encore câblée) |
| 6 | Retrait du format legacy pour les *nouvelles* cartes seulement | Phase 3 | Faible | Non si bien scopé | À faire |

### Phase 0 — Génération manuelle (à faire en premier, indépendamment de tout le reste) — **Fait**

Le seul point d'auto-génération sans intention explicite est le fallback inconditionnel dans `checkLoadParameters()` (`src/services/url-params.ts:57-58`) — remplacé par un état vide/idle avec un bouton "Générer" (`src/components/idle-state.ts`, nouveau). L'infrastructure de déclenchement manuel existait déjà et est réutilisée telle quelle : le bouton appelle exactement `generateMapOnLoad(size)`, la même fonction que les chemins `?seed=`/`?maplink=` utilisent déjà, juste différée à un clic au lieu d'auto-invoquée. Zéro changement dans le pipeline de génération lui-même.

**Vérification** : ouvrir l'app sans paramètre d'URL et sans carte sauvegardée → écran vide avec bouton, pas de génération auto. `?seed=...`, `?maplink=...`, et "charger la dernière carte" continuent de fonctionner sans changement (ce sont des intentions explicites, elles restent automatiques). Confirmé : `tsc --noEmit`, lint, build, 1018/1018 tests, chargement de la page en local (200, pas d'erreur).

### Phase 1 — Schéma Postgres/PostGIS + validation ETL — **Fait**

`docker-compose.yml` : un service Postgres+PostGIS local (`docker.io/postgis/postgis:16-3.4`), volume nommé local, pas d'authentification au-delà des identifiants par défaut. Schéma dans `server/db/schema.sql`, appliqué automatiquement au premier démarrage du conteneur. Script d'import `server/scripts/import-map.mjs` (package Node isolé sous `server/`, dépendance `pg` uniquement — aucun impact sur le bundle client).

**Ajustement fait pendant cette phase, comme prévu** : la piste initiale ("réutiliser directement les 4 exporteurs GeoJSON de `export.ts`") s'est révélée impraticable telle quelle — ces fonctions lisent les globales navigateur (`pack`/`grid`/`options`), importent des modules DOM (tooltips, viewport, fonts) et projettent les coordonnées en pseudo-lon/lat via `toGeoCoordinates`, incompatible avec la décision SRID 0/Cartésien du schéma. La vraie source exploitée à la place : l'export **"Pack Cells" JSON** déjà existant dans le menu Export de l'app (`getPackDataJson()`/`getPackCellsData()` dans `src/services/io/export-json.ts`) — il expose `cells` (avec `v`, l'anneau d'indices de vertex) et `vertices` (coordonnées `[x,y]` brutes, non projetées) plus tous les tableaux d'entités (burgs/states/provinces/cultures/religions/rivers/routes/markers), sans aucune dépendance navigateur. Zéro changement de code source nécessaire pour produire cet export — la fonctionnalité existe déjà.

**Décision tranchée** : PostGIS fusionne les géométries de territoire lui-même. Le script importe la géométrie brute par cellule dans `map_cells`, puis `UPDATE ... SET geom = ST_Multi(ST_UnaryUnion(ST_Collect(geom))) ... GROUP BY state_id` (idem province/culture/religion) — aucun portage de la logique de traçage de trous de `connectVertices` n'a été nécessaire.

**Validé de bout en bout** avec une fixture synthétique (`server/scripts/fixtures/PackCells.sample.json`, 3 cellules dont 2 adjacentes partageant un état) : les deux cellules adjacentes fusionnent en un seul polygone 2×1 propre (arête interne partagée dissoute), la cellule isolée reste séparée, et une requête `ST_AsGeoJSON` renvoie un `FeatureCollection` directement consommable par `L.geoJSON()` — exactement la mécanique que la Phase 2 exposera via HTTP. Détails et commandes de vérification dans `server/README.md`.

**Piège rencontré et documenté** : sous podman rootless (Fedora), le montage du volume `schema.sql` nécessite le flag SELinux `:Z` (`docker-compose.yml`) — sans lui, Postgres échoue silencieusement à lire le fichier ("Permission denied" visible seulement dans `docker logs`, pas une erreur SQL).

**Reporté sciemment, pas oublié** : les zones (traçage de trous le plus complexe, pas nécessaire pour valider le reste), le meandering des rivières (`Rivers.addMeandering`, rendu en segments droits pour l'instant), et `map_topology.grid` (aucun export de grille utilisé dans ce script).

### Phase 2 — Serveur API Node, lecture seule — **Fait**

Fastify (`server/src/server.mjs`), pas d'authentification (local mono-utilisateur). La logique d'import de la Phase 1 a été extraite dans `server/src/import.mjs` (fonction pure `importPack(client, {...})`, transaction gérée par l'appelant) partagée par le script CLI (`scripts/import-map.mjs`, maintenant un simple wrapper) et le nouvel endpoint `POST /api/maps/import` — aucune logique dupliquée entre les deux chemins.

Endpoints (voir `server/README.md` pour le détail) : `GET /api/maps`, `POST /api/maps/import`, `GET /api/maps/:id`, `GET /api/maps/:id/layers/:layer` (GeoJSON par couche), `GET /api/maps/:id/entities/tree`, `GET /api/maps/:id/entities/:kind/:id`, `DELETE /api/maps/:id` (cascade vérifiée).

**Vérification faite** : chaque endpoint testé manuellement contre la fixture de la Phase 1 (liste, détail, GeoJSON par couche avec un cas de couche inconnue → 400, arborescence, détail d'entité, import via l'API en plus du CLI, suppression avec cascade confirmée sur `map_cells`). Le GeoJSON renvoyé par `/layers/:layer` est directement dans la forme qu'attend `L.geoJSON()`.

**Bug réel trouvé et corrigé pendant la vérification** : l'arborescence d'entités groupait un burg via `province_id ?? state_id` (coalescence nulle) tout en *choisissant* le groupe via un test de vérité (`province_id ? ... : ...`) — comme FMG utilise `0` comme sentinelle "pas de province" (pas `null`), un burg avec `province_id: 0` choisissait le bon groupe (état) mais était ensuite indexé par `0` au lieu de l'id d'état réel, disparaissant silencieusement de l'arborescence. Corrigé pour utiliser un seul test de vérité cohérent pour le choix du groupe et la clé.

**Bug réel trouvé et corrigé plus tard, en testant depuis un vrai navigateur (2026-09-19)** : le serveur n'avait aucun en-tête CORS. `curl`/`psql` (utilisés pour toute la vérification de cette phase) ne sont pas soumis à la politique de même origine, donc ça n'était jamais apparu — mais le wiki (`wiki.html`, servi par Vite sur une origine différente, ex. `localhost:5173`) qui essaie de `fetch()` cette API échouait systématiquement avec `NetworkError`, sans que le message d'erreur affiché ("is the server running?") pointe vers la vraie cause. Corrigé en ajoutant `@fastify/cors` (`server/src/server.mjs`, `origin: true`) — une politique ouverte, cohérente avec le modèle de sécurité déjà posé (local, mono-utilisateur, sans authentification). Vérifié : requête avec un en-tête `Origin` différent renvoie bien `access-control-allow-origin` en retour.

### Phase 3 — Portage de la génération sur Node — **Fait*** (*une vérification reste à faire par un humain, voir plus bas)

Confirmé exactement comme prévu : sur 46 fichiers dans `src/generators/`, un seul point d'API navigateur était réellement sur le chemin du pipeline (`heightmap-generator.ts`'s `fromPrecreated()`), et le reste tourne **sans aucune modification** — le vrai moteur de génération client, pas une réécriture. Une carte complète (8462 cellules, 1117 burgs, 13 états, simulation économique complète) se génère en ~2 secondes sous Node.

**Le seul changement réel apporté à `src/`** : `fromPrecreated()` utilisait `<canvas>`/`Image`, absents sous Node. Rendu le chargeur d'image remplaçable (`setHeightmapImageLoader`, comportement navigateur par défaut inchangé) avec une implémentation serveur basée sur `sharp` (`server/src/generation/heightmap-image-loader.ts`). **Mise en garde documentée, pas cachée** : l'algorithme de redimensionnement de sharp/libvips n'est pas garanti identique pixel-à-pixel à celui du Canvas navigateur — seuls les templates de heightmap **pré-créés** (à base d'image) portent ce risque de divergence marginale ; les templates procéduraux (calcul pur) sont inchangés et identiques bit-à-bit.

**Deux dépendances non-évidentes découvertes seulement en exécutant réellement le code** (invisibles à la simple lecture des imports) :
- `aleaPRNG` (le PRNG à seed) n'est PAS un export ES module — c'est `window.aleaPRNG` posé par un **`<script>` classique vendorisé** (`public/libs/alea.min.js`), invisible à toute analyse par imports. Résolu en utilisant le paquet npm `alea` (même algorithme, déjà une dépendance utilisée directement par plusieurs générateurs).
- `FlatQueue` (utilisé par l'expansion des cultures/états) est le même genre de global vendorisé (`public/libs/flatqueue.js`) — mais ce fichier est UMD et détecte CommonJS, donc le fichier exact utilisé par le navigateur se charge ici sans modification via un simple import relatif.

`src/generators/pipeline.ts`'s `Pipeline<Id, TContext>` s'est révélé portable tel quel. L'équivalent serveur mince à `generate()` (`server/src/generation/generate.ts`) résout une requête en seed/taille/densité, lance le pipeline, puis `packToJson()` (`server/src/generation/pack-to-json.ts`) convertit le `pack` vivant dans la même forme que consomme déjà `importPack()` de la Phase 1 — aucune logique dupliquée.

**Concurrence** : la génération mute des globales partagées (`pack`/`grid`/`options`) — deux générations ne peuvent pas tourner en parallèle dans le même process sans se corrompre. L'endpoint API sérialise via une file de promesses (`server/src/routes/maps.mjs`) plutôt que d'exécuter en parallèle — suffisant en local mono-utilisateur.

**Vérifié directement** :
- **Déterminisme** : la même seed générée deux fois côté serveur produit une sortie identique bit-à-bit.
- **Bout en bout réel** : `npm run generate` sur une carte pleine taille, importée proprement et interrogeable via chaque endpoint de la Phase 2, géométries de territoire et de routes valides.
- **Un second bug réel**, trouvé uniquement en testant avec de vraies données générées (pas la fixture synthétique) : les entrées `points` des rivières/routes de FMG sont des triplets `[x, y, cellId]` (un id de cellule embarqué, pas une coordonnée Z) — GeoJSON lit une coordonnée à 3 éléments comme XYZ, ce que les colonnes PostGIS 2D rejetaient ("Geometry has Z dimension but column does not"). Corrigé en tronquant à `[x, y]`.
- **Requêtes concurrentes** : deux appels `POST /api/maps/generate` simultanés avec des seeds différentes se terminent avec des résultats corrects et indépendants — la file de sérialisation fonctionne.

**Ce qui reste à faire par un humain** (le projet ne lance jamais de vrai navigateur automatiquement) : `server/scripts/compare-with-browser.ts` compare une génération serveur à un export réel du navigateur pour la même seed/taille — c'est la seule étape de vérification du plan qui nécessite un navigateur réel. Voir `server/README.md` pour la commande exacte. Tout le reste ci-dessus a été vérifié directement.

### Phase 4 — Le wiki devient la coquille hôte ; arborescence live — **Fait***

Deux étapes, délibérément séparées de la Phase 5 pour ne jamais avoir "intégration à moitié cassée ET rendu à moitié cassé" en même temps :

1. **Bascule de coquille, même moteur de rendu — fait.** Plutôt que fusionner les deux bundles Vite (risque réel : deux applications complexes partageant un seul DOM, collisions d'id quasi garanties sur un monolithe de 9K lignes), le wiki héberge la carte en iframe — exactement le même mécanisme que `wiki-panel.ts` utilisait déjà pour l'inverse (carte héberge le wiki), simplement inversé. Bouton "View Map" dans la barre latérale du wiki (`src/wiki.html`, `wiki-main.ts`, `wiki.css`) ouvrant `./index.html` en plein écran. Garde anti-imbrication ajoutée à `wiki-panel.ts` (`if (window.self !== window.top) return`) pour qu'`index.html`, une fois chargé À L'INTÉRIEUR de ce panneau, n'essaie pas d'injecter son propre bouton wiki qui ouvrirait un wiki imbriqué à l'infini si on cliquait dessus. Le rendu de la carte elle-même reste inchangé (SVG classique) — seule la coquille change, exactement comme prévu, pour ne pas bloquer sur la Phase 5.
2. **Arborescence live — fait.** `src/wiki/db-entities.ts` (nouveau) normalise `GET /api/maps/:id/entities/tree` en `WikiEntity[]`, avec un slug synthétique déterministe `db-{mapId}-{kind}-{id}` (ex. `db-9-burg-42`) — stable tant que l'id FMG et le `mapId` ne changent pas, donc ciblable par wikilink à la main avant même qu'une fiche `.md` existe. `wiki-main.ts` garde deux tableaux d'entités séparés (`fileEntities`, chargé au build comme avant ; `dbEntities`, chargé à la demande via un nouveau bouton "Connect to map database…") fusionnés par `mergeEntitySources()` avant `buildGraph`/`buildSlugIndex`/`loadEras` — aucun changement nécessaire dans ces trois fonctions ni dans le rendu, comme prévu, puisqu'ils opèrent déjà sur un `WikiEntity[]` plat agnostique de la source. Chaque entité générée affiche un badge "from map #N" et un lien "Write a lore page for this ↗" vers `#/new?title=...`, qui crée une vraie fiche `.md` réutilisant le flux de création déjà existant.

**Décision prise, pas un oubli** : le jumelage ères/`map_id` évoqué comme risque de rupture ci-dessous n'a **pas** été fait dans cette passe. `dbRef` (le pointeur `{mapId, kind, id}` sur une entité générée) est un mécanisme séparé du système d'ères existant (`Era.mapFile`/`map_file`/`?maplink=`), pas branché dessus — documenté explicitement en commentaire dans `db-entities.ts`. Unifier les deux (une ère = une ligne `maps`, avec migration des `map_file:` existants vers `map_id:`) reste un vrai travail, à faire quand la Phase 5 (Leaflet) rendra le rendu de carte lui-même dépendant de Postgres plutôt que du fichier `.map` legacy — prématuré avant ça.

**Vérification faite** : `tsc --noEmit`, lint (biome, 0 erreur), build Vite (OK), 49/49 tests wiki (dont les nouveaux tests unitaires de `flattenTree`/`dbRefOf` dans `db-entities.test.ts`, couvrant l'imbrication état→province→burg, le fallback de titre pour une entité sans nom, et la composition du résumé parent/population/capitale).

**Reste à faire par un humain** (le projet ne lance jamais de vrai navigateur automatiquement) : cliquer "Connect to map database…" dans le wiki, choisir une carte importée en Phase 1/3, vérifier que l'arborescence se peuple et que "Write a lore page for this" crée bien une fiche pré-remplie.

### Phase 5 — Migration Leaflet, incrémentale, par ordre de risque croissant — **en cours**

Deux décisions de scope prises **pendant** cette phase, avant d'écrire du code de rendu, parce que creuser l'implémentation réelle a révélé deux problèmes que le plan initial n'avait pas anticipés :

**1. Pas de gain d'efficacité mémoire/tuiles dans cette phase, et c'est assumé.** Convertir uniquement les couches d'affichage (biomes/états/provinces/etc.) en GeoJSON servi par Postgres ne réduit PAS l'empreinte mémoire navigateur : `pack`/`grid` doivent rester intégralement en mémoire de toute façon, parce que tous les éditeurs (states-editor, burg-editor, provinces-editor, etc.) et toutes les autres couches continuent de muter cet objet directement et de redessiner en local, exactement comme aujourd'hui. Un vrai gain d'efficacité demanderait de rendre les éditeurs eux-mêmes pilotés par l'API (fetch/mutate/save en HTTP au lieu de muter un objet partagé en mémoire) — un chantier bien plus large que "Phase 5", hors scope ici. Cette phase est donc une **migration de moteur de rendu** (Leaflet réel : caméra, gestes, couches vectorielles natives), pas un chantier de performance. Décision validée explicitement avec l'utilisateur avant d'écrire du code.

**2. De vraies couches Leaflet (`L.geoJSON()`, clics natifs par-feature) exigent que le SVG existant partage le même arbre DOM que Leaflet, pas un arbre séparé empilé en CSS.** Deux arbres DOM superposés (un `<div>` Leaflet et un `<svg id="map">` à côté) ne peuvent pas être interactifs tous les deux à la fois : un point de l'écran ne reçoit les événements que d'UN SEUL des deux arbres. La seule architecture qui marche : `<svg id="map">` (avec ses 24 couches non converties, structure interne inchangée) devient l'contenu d'un **pane Leaflet dédié**, lui-même un enfant du même conteneur que les futures couches GeoJSON natives — pour que les clics/drags de l'un et les gestes de pan/zoom de Leaflet coexistent par la bulle d'événements DOM normale, exactement comme d3-zoom et d3-drag coexistaient déjà avant.

**Ce qui est fait, vérifié, et committé dans cette passe** — la fondation caméra, sans laquelle rien d'autre n'a de sens :
- `leaflet` + `@types/leaflet` ajoutés (`package.json`), aucune nouvelle vulnérabilité (`npm audit` ne signale que des CVE pré-existantes sans rapport, vitest/quill).
- `src/components/leaflet-map.ts` (nouveau) : instance `L.Map` unique, `L.CRS.Simple` avec une **échelle linéaire** (`scale(zoom) = zoom`, pas la puissance de 2 par défaut de Leaflet) pour que `map.getZoom()` reste numériquement identique à l'ancien facteur d'échelle `[1, 20]` de `viewport.scale`. `<svg id="map">` est déplacé (au premier appel) dans un pane Leaflet dédié (`legacySvg`), créé comme enfant direct du conteneur Leaflet plutôt que du pane interne `_mapPane` — pour ne PAS hériter de la transformation CSS automatique de Leaflet pendant les animations de pan (qui aurait doublé la transformation avec celle, manuelle, que `zoom.ts` continue d'écrire sur `#viewbox`).
- `src/components/zoom.ts` réécrit pour piloter cette instance Leaflet au lieu de d3-zoom, **signature publique inchangée à 100%** (`zoomTo`, `resetZoom`, `panMap`, `setMapZoom`, `changeMapZoom`, `setZoomExtent`, `constrainZoom`, `setTranslateExtent`, `applyZoomBehavior`, `invokeActiveZooming`) — aucun des ~35 points d'appel dans le reste de l'app n'a changé. Chaque fonction d3 a un équivalent Leaflet direct sur son API publique stable (`map.flyTo`/`setView` pour les transitions, `map.panBy`, `map.setZoom`, `map.setMinZoom`/`setMaxZoom`, `map.setMaxBounds`) — aucun accès aux internes de Leaflet nécessaire. Le mécanisme de synchronisation par frame (écrire `translate/scale` sur `#viewbox`, redessiner `scaleBar`/`coordinates`, notifier la minimap) est **inchangé**, juste réalimenté par `map.latLngToContainerPoint(L.latLng(0,0))`/`map.getZoom()` au lieu de l'event `D3ZoomEvent`.
- **Bug de test réel trouvé et corrigé** : `leaflet`'s propre code (`Browser.js`) lit `document.documentElement.style` à l'import, ce qui plante sous l'environnement de test `node` minimal du projet (`src/test-setup.ts`, sans DOM réel, choisi pour la vitesse). Corrigé en mockant `leaflet` dans `heightmap-editor.test.ts` (le seul autre fichier de test qui importait `zoom.ts` transitivement sans avoir besoin de son comportement réel).
- `zoom.test.ts` (qui, lui, teste vraiment `zoom.ts`) réécrit avec un **faux Leaflet déterministe** (`vi.mock("@/components/leaflet-map", ...)`) plutôt que le vrai Leaflet sous jsdom — jsdom n'a pas de vraie mise en page (taille de conteneur, timing d'animation), ce qui rendait le comportement d'un vrai `L.Map` non déterministe en test. Les 4 tests couvrent le même comportement qu'avant (mise à jour de `viewport`, écriture de la transformation sur `#viewbox`, planification du culling par frame vs. à la fin du geste).
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1024/1024 tests passent, build Vite complet (CSS de Leaflet correctement empaqueté dans le bundle principal — confirmé par grep sur le CSS de sortie).

**Couche `biomes` convertie et vérifiée** — la première vraie couche `L.geoJSON()` de la migration, et un gabarit réutilisable pour les suivantes :
- `src/renderers/leaflet/territory-geojson.ts` (nouveau) : `buildTerritoryFeatureCollection(getType)` — réutilise `getIsolines(pack, getType, { polygons: true })` telle quelle (déjà exactement les anneaux `[x,y]` nécessaires, zéro nouveau code de géométrie) et les enveloppe en `MultiPolygon` GeoJSON, coordonnées brutes (pas `toGeoCoordinates`, cohérent avec la décision SRID 0). Compromis documenté : pas d'équivalent au "water gap" (bordure partielle uniquement côté terre) — les couches converties n'ont pas de bordure du tout pour l'instant.
- `src/renderers/leaflet/territory-layer.ts` (nouveau) : `createTerritoryLayer(paneName, zIndex, idPrefix)` — un `L.geoJSON` persistant, recréé/rafraîchi via `clearLayers()` + `addData()` à chaque `update()` plutôt que reconstruit de zéro (évite de perdre l'instance à chaque redraw). Après chaque rendu, **retague chaque `<path>` Leaflet avec `id="{idPrefix}{featureId}"`** (via `layer.getElement()` sur chaque sous-couche, une fois `addData` retourné — pas via `onEachFeature`, appelé trop tôt, avant que Leaflet ait créé l'élément DOM) — exactement pour que l'ancien code d'éditeur qui cible un territoire par id continue de le trouver — à condition qu'il le cherche comme descendant plutôt que comme enfant direct du conteneur (`#provs > #province5` ne marche plus ; `#province5` seul suffit, l'id étant déjà unique).
- **`src/components/leaflet-map.ts`** : ajout de `getFeaturePane(name, zIndex)` — un pane Leaflet standard (enfant du pane interne de Leaflet, donc pleinement animé/reprojeté par Leaflet lui-même), par opposition au pane `legacySvg` (délibérément un enfant direct du conteneur, sans transformation automatique). Comme `legacySvg` a été ajouté au conteneur *après* le pane interne de Leaflet, il peint par-dessus tout pane créé ici, quel que soit son `zIndex` — `zIndex` n'ordonne les couches converties qu'entre elles.
- **`src/components/layers.ts`** : `LayerParams.parent` gagne une 3ᵉ valeur, `"leaflet"` ; `Layer.getEl()` est élargi à `HTMLElement | SVGElement` (un pane est un `<div>`, pas un `<g>`). `LayersRegistry.init()` ne crée plus de `<g>`/ne réattache rien pour ces couches — il appelle un nouveau hook `LayerParams.ensurePane?.()` puis se contente de la visibilité. **Bug réel trouvé et corrigé pendant cette passe** : `ensurePane` doit tourner dès `init()` (au boot), pas seulement au premier `draw()` — sinon la toute première fois qu'un utilisateur active la couche (`Layers.show("biomes")`), `change()` appelle `setVisible(layer.getEl(), true)` *avant* `draw()`, et `getEl()` ne trouve rien puisque le pane n'a encore jamais été créé → `element.style.display = ...` plante sur `null`.
- `src/renderers/draw-biomes.ts` : `drawBiomes()`/`eraseBiomes()`/`ensureBiomesPane()` réécrits sur `createTerritoryLayer`, plus de manipulation SVG directe.
- `src/controllers/biomes-editor.ts` : les 2 lookups de surlignage au survol (`#biomes > #biome{id}`, combinateur enfant direct) simplifiés en `#biome{id}` — l'id est déjà unique, pas besoin de l'ancêtre. `biomeChangeColor` n'a pas eu besoin de changer : il passe déjà par un `Layers.draw("biomes")` complet, pas par un patch DOM direct — c'est justement pourquoi biomes a été choisi en premier (provinces/cultures/religions font, eux, du patch direct pour la recoloration en direct, d'où le report ci-dessous).
- **Bug systémique de test découvert et corrigé** : dès que `layers.ts` importe une couche qui touche Leaflet (même indirectement), *tout* fichier de test import ant `layers.ts`, même transitivement, hérite du crash `document.documentElement.style` sous l'environnement de test `node` minimal — 7 fichiers supplémentaires touchés cette fois (`biomes-editor.test.ts`, `label-spread-*.test.ts` ×3, `route-editor.test.ts`, `styles.test.ts`, `styles-legacy.test.ts`), aucun ne testant quoi que ce soit lié au rendu. Corrigés avec le même mock ciblé que `heightmap-editor.test.ts` (`vi.mock("leaflet", () => ({ extend: Object.assign, CRS: { Simple: {} } }))`) — mais ce motif va probablement se répéter à chaque nouvelle couche convertie ; un helper de mock partagé serait plus soutenable qu'un copier-coller par fichier, pas fait faute de temps.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1024/1024 tests passent, build Vite complet.
- **Reste à vérifier par un humain** : biomes se dessine et se surligne correctement dans un vrai navigateur — rien de tout ceci n'a été vu s'afficher réellement.

**Couche `religions` convertie et vérifiée** — la première à exiger d'adapter son éditeur en même temps que son rendu :
- Même gabarit que `biomes` (`createTerritoryLayer("religions-leaflet", 101, "religion")`), plus deux différences réelles trouvées en l'adaptant :
  - **Recoloration en direct** : `religionChangeColor` patchait `select("#relig").select(...).attr("fill", newFill)` directement sur l'élément plutôt que de tout redessiner. Remplacé par `Layers.draw("religions")` (même motif que `biomes`, qui le faisait déjà) — un vrai redraw, pas un raccourci, puisque `pack.religions[id].color` est la seule source de vérité que `createTerritoryLayer` consulte.
  - **Suppression** : `removeReligion` supprimait `#religion{id}`/`#religion-gap{id}` directement du DOM. Retirer un noeud DOM d'un `L.GeoJSON` à la main désynchronise le suivi interne de Leaflet (il continue de croire la sous-couche attachée) ; remplacé par un `Layers.draw("religions")` complet après la mise à jour de `pack`, cohérent avec le motif de recoloration ci-dessus.
  - Les autres lookups (`#relig` + `#religion{id}` en enfant direct, pour le surlignage au survol et `highlightReligion`) simplifiés en `#religion{id}` seul, même correction que pour `biomes`.
- **Découverte non anticipée, plus large que `religions` seule** : `highlightReligion` (l'action "localiser sur la carte" de l'éditeur) appelait `element.getBBox()` sur le tracé rendu — sûr avec l'ancien SVG (coordonnées monde fixes, le zoom/pan étant appliqué *en dehors*, sur `#viewbox`), mais **faux** avec un tracé rendu par Leaflet : Leaflet recalcule et réécrit les coordonnées de chaque `<path>` dans l'espace pixel du zoom/pan *courant* à chaque déplacement de la carte, donc `getBBox()` ne renvoie plus une boîte en coordonnées monde. Corrigé en ajoutant `TerritoryLayerHandle.getFeatureBounds(id)` (`src/renderers/leaflet/territory-layer.ts`), qui lit `L.Path.getBounds()` — une `LatLngBounds`, qui *est* l'espace monde sous ce `CRS.Simple` (lat=y, lng=x, pas de projection) — plutôt que la géométrie DOM rendue. **Tout code futur qui positionne quelque chose à partir de la géométrie propre d'un élément converti (`getBBox`, `getBoundingClientRect`, `getScreenCTM`) doit être audité de la même façon** — `label-spread.ts` (item 7 ci-dessous) en dépend fortement pour `burgIcons`.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1024/1024 tests passent, build Vite complet — et cette fois-ci, zéro fichier de test supplémentaire n'a eu besoin du mock `leaflet` (contrairement à `biomes`, qui en avait fait tomber 7).
- **Reste à vérifier par un humain** : rendu, survol, recoloration, suppression, et le bouton "localiser" d'une religion dans un vrai navigateur.

**Couche `cultures` convertie et vérifiée** — même gabarit que `religions`, quasiment sans surprise cette fois :
- `createTerritoryLayer("cultures-leaflet", 102, "culture")`. `cultures-editor.ts` avait exactement la même forme de couplage que `religions-editor.ts` (survol via `#cults > #culture{id}`, recoloration en direct, suppression, `highlightElement`/`getBBox` pour "localiser", plus un marqueur séparé `#cultureCenter{id}` dans `#debug`, inchangé) — les cinq points corrigés de façon identique : lookups simplifiés en `#culture{id}` seul, recoloration et suppression basculées sur `Layers.draw("cultures")`, "localiser" basculé sur `getCultureBounds()`/`highlightArea()` (nouveau `TerritoryLayerHandle.getFeatureBounds` réutilisé tel quel, pas de nouveau code de conversion de coordonnées).
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1024/1024 tests passent, build Vite complet — encore une fois, aucun fichier de test supplémentaire cassé.
- **Reste à vérifier par un humain** : même liste que `religions` (rendu, survol, recoloration, suppression, "localiser") dans un vrai navigateur.

**Couche `provinces` convertie et vérifiée** — la plus riche en points de couplage jusqu'ici, et une vraie découverte au passage :
- `createTerritoryLayer("provinces-leaflet", 103, "province")`. Bonne nouvelle : `changeFill` (recoloration) passait déjà par `Layers.draw("provinces")`, pas par un patch direct — comme `biomes`, aucun changement nécessaire là. Mauvaise nouvelle : deux fonctions (`toggleFog` et `highlightProvinceOnMergeHover`) lisaient l'attribut `d` **complet** du tracé rendu, pas juste sa position — une classe de bug plus large que le `getBBox()` déjà trouvé sur `religions`/`cultures` :
  - `toggleFog` passe ce `d` à `fog(id, path)`, qui l'insère tel quel dans le masque SVG `#fog` (espace monde, puisque le rectangle de fog couvre `options.map.graph` en coordonnées brutes) pour découper un trou "zone non voilée" autour de la province. Avec un `d` en pixels Leaflet courants, le trou aurait été mal placé/mal dimensionné dès le prochain pan/zoom.
  - `highlightProvinceOnMergeHover` passe ce `d` à `highlightOutline()`, qui le dessine directement dans `#debug` (espace monde) pour l'anneau animé de survol pendant une fusion de provinces.
  - Corrigé en ajoutant `TerritoryLayerHandle.getFeaturePath(id)` (`territory-layer.ts`) : convertit `feature.geometry.coordinates` (les coordonnées GeoJSON monde déjà envoyées à Leaflet, conservées sur chaque sous-couche via `layer.feature`) en chaîne `d` SVG avec la même convention `M...L...Z` par anneau que l'ancien `getFillPath` — aucune nouvelle trace de géométrie, juste un format de sortie différent des mêmes données.
- Suppression (`removeProvince`, et le flux `removeAllProvinces`) : même correction que `religions`/`cultures` — patch DOM direct remplacé par un `Layers.draw("provinces")` complet.
- Survol et "localiser" : mêmes corrections que `religions`/`cultures` (lookup simplifié, `getProvinceBounds()`/`highlightArea()`).
- **Écart connu, délibérément non corrigé** : `src/services/fonts.ts`'s `getUsedFonts()` lit `select("#provs").attr("font-family")` pour intégrer la police des provinces dans un export SVG/PNG — `provinces.attrs` du schéma de styles supporte bien `font-family`/`font-size`, mais `#provs` n'existe plus. Portée réelle : narrow et non bloquante — seulement l'export de fichiers, seulement si une police de provinces personnalisée a été définie via l'éditeur de style, dégrade sans planter (la police est simplement absente de l'export, pas de perte de données ni de crash sur la carte en direct). Pas corrigé faute de temps ; à route vers `layers.ts`'s store `styles.provinces.attrs["font-family"]` directement plutôt que de lire un attribut DOM, si quelqu'un tombe dessus.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1024/1024 tests passent, build Vite complet — aucun fichier de test supplémentaire cassé.
- **Reste à vérifier par un humain** : rendu, survol, recoloration, suppression, "localiser", le bouton fog/focus (icon-pin), et le survol pendant une fusion de provinces, dans un vrai navigateur.

**Correctif rétroactif, plus sérieux que ce qui précède : opacité/filtre/masque des 4 couches déjà converties ne s'appliquaient pas du tout.** Découvert en auditant `states` (voir plus bas), mais il touche `biomes`/`religions`/`cultures`/`provinces` aussi — corrigé pour les quatre d'un coup, sans toucher à leurs fichiers `draw-*.ts` individuels.
- **Le mécanisme découvert** : `Styles.write(id)`/`Styles.apply(id)` (`src/generators/styles.ts`) est le système générique qui applique les réglages de l'éditeur de style (opacité, filtre, `stroke`, `fill`, etc., voir `styles-schema.ts`) sur le DOM — il cherche `[data-layer="${id}"]`, puis écrit chaque `attrs` via `setAttribute`. `LayersRegistry.init()` posait bien `dataset.layer` sur les `<g>` SVG classiques, mais **pas** sur les panes Leaflet (`parent: "leaflet"`) — `Styles.write("religions")` etc. ne trouvait donc rien du tout, silencieusement (pas d'erreur, juste aucun effet).
- **Ce n'est pas qu'un problème d'interactivité "si l'utilisateur ouvre l'éditeur de style"** : `default-styles.json` donne à ces couches une opacité **très** différente de 1 par défaut — `religions: 0.7`, `cultures: 0.6`, `provinces: 0.7`, `states.statesBody: 0.4` — et `biomes` porte un `mask: "url(#land)"` (le fill ne doit apparaître que sur la terre, pas l'océan). Sans le correctif, les 4 couches converties auraient dû rendre **entièrement opaques et non masquées** dès le premier affichage, pas seulement après une interaction manuelle avec l'éditeur de style — un vrai écart visuel par défaut, pas un edge case.
- **Corrigé en deux points, aucun n'exigeant de toucher un seul fichier `draw-*.ts`** :
  1. `layers.ts`'s branche `"leaflet"` de `init()` pose maintenant `pane.dataset.layer = layer.id` sur le pane, exactement comme la branche SVG le fait déjà pour son `<g>`.
  2. `styles.ts`'s `writeNode()` : pour un élément qui n'est **pas** un `SVGElement` (un pane Leaflet, un simple `<div>`), `setAttribute("opacity", ...)` ne fait strictement rien — ce n'est pas un attribut HTML reconnu. `opacity`/`filter`/`mask` sont les trois seules clés `attrs` qui ont un équivalent CSS de même sémantique ; pour elles, écrire sur `.style` au lieu de `setAttribute` quand la cible est un `HTMLElement` — et cette opacité/filtre/masque CSS s'applique au pane entier par composition normale du navigateur, donc à tout ce que Leaflet y dessine à l'intérieur, sans qu'aucune fonction `style` de `L.geoJSON` n'ait besoin de connaître `styles[id].attrs` elle-même. Les autres clés (`stroke`, `fill`, `font-family`, ...) restent des no-op silencieux sur un pane, sans équivalent CSS générique — même famille d'écart que le "font-family" de `provinces` déjà documenté plus haut, pas aggravée par ce correctif.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1024/1024 tests passent (dont `styles.test.ts`/`styles-legacy.test.ts`), build Vite complet. `styles.dom.test.ts` (suite navigateur réel, `vitest.browser.config.ts`) n'a **pas** pu tourner dans cet environnement — Playwright n'a pas de Chromium installé ici ; ce n'est pas un échec de test, juste une suite qui n'a jamais pu se lancer. Le raisonnement (élément HTML vs SVG, aucun changement de comportement pour les ~25 autres couches encore en SVG) est solide mais **non confirmé en navigateur réel**.
- **Reste à vérifier par un humain** : ouvrir l'éditeur de style pour `religions`/`cultures`/`provinces`/`biomes`, confirmer que l'opacité par défaut (pas 100%) est bien visible dès le premier rendu, que `biomes` ne peint pas l'océan, et qu'ajuster le curseur d'opacité a un effet visible immédiat.

**Couche `states` convertie et vérifiée** — la plus grosse jusqu'ici, et la seule conçue comme un **hybride** délibéré :
- **Design différent des quatre précédentes, et pourquoi** : `draw-states.ts` a un effet de halo/lueur de bordure optionnel (`shapeRendering: "geometricPrecision"`) — un `<clipPath>` par état découpant un contour flouté à la couleur de l'état assombrie. `LayersRegistry` ne peut piloter la visibilité/l'effacement que d'**une seule** surface par couche ; le halo a besoin d'un vrai `<g>` SVG pour exister. Donc `states` reste `parent: "viewbox"` dans `layers.ts` (comme avant, structure `#regions`/`#statesHalo` inchangée) — SEULE `biomes`/`religions`/`cultures`/`provinces` sont `parent: "leaflet"`. `drawStates()` pilote les deux surfaces lui-même : `createTerritoryLayer("states-leaflet", 104, "state")` pour le fill, plus une fonction `drawHalo()` séparée (quasi identique à l'ancien code) pour `#statesHalo`/`#statePaths` quand `renderHalo` est actif.
- **Le `<clipPath>` a dû changer quand même** : `<use href="#state{id}">` référençait l'ancien tracé de fill pour découper le halo — mais ce tracé vit maintenant dans un `<svg>` Leaflet séparé, et un `<use>` inter-racines-SVG n'est pas quelque chose que ce projet a testé. Remplacé par un `<clipPath>` **auto-suffisant** : `<path d="${fill}">` directement dedans, en réutilisant le `d` que `getIsolines(..., {fill:true, halo:true})` calcule de toute façon pour le halo lui-même — zéro nouvelle trace de géométrie, juste ne plus référencer l'élément Leaflet par id.
- **`states-editor.ts`** (le plus gros fichier de ce type touché jusqu'ici) : mêmes quatre corrections déjà vues sur `provinces` — survol (`stateHighlightOn`, qui utilise directement `highlightOutline`/un `d`, pas un simple trait de surbrillance comme `religions`/`cultures`), "localiser" (`getStateBounds()`), fog/focus (`toggleFog`, `getStatePath()`), survol pendant une fusion d'états (`getStatePath()`). Recoloration (`stateChangeFill`) et suppression (`stateRemove`) passaient **déjà** par un `Layers.draw("states")` complet — comme `biomes`, aucun changement nécessaire là.
- **Découverte tardive, hors de `states-editor.ts`** : `diplomacy-editor.ts`'s `showStateRelations()` (la vue "relations diplomatiques") itère TOUS les tracés enfants de `#statesBody` et remplace directement leur couleur de fill par une couleur basée sur la relation diplomatique — une fonctionnalité entière ratée par l'audit initial de `states-editor.ts` seul. Réécrit pour itérer `pack.states` et cibler chaque `#state{id}` directement (même convention que partout ailleurs), plutôt que de parcourir les enfants d'un conteneur qui n'existe plus sous cette forme. La coloration du "state-gap" (bordure eau) a été supprimée : aucune couche convertie ne rend ce trait, compromis déjà assumé depuis `biomes`.
- **`#statesBody` retiré des enfants déclarés de la couche** (`layers.ts`) — plus rien n'y écrit. Vérifié qu'aucun autre fichier (`auto-update.ts`, `styles-legacy.ts`, les exporteurs) n'en dépend en dehors de la migration de fichiers anciens (qui lit le contenu du **fichier chargé**, pas mon rendu, même raisonnement que pour toutes les couches précédentes).
- **Écart connu, délibérément non corrigé** : l'opacité de `statesBody` (`styles.states.statesBody.attrs.opacity`, 0.4 par défaut) n'a plus d'élément à cibler — contrairement aux quatre couches précédentes, ce n'est **pas** corrigeable par le même mécanisme `dataset.layer`/CSS, puisque `writeNode` cherche `statesBody` comme **descendant** de `#regions` (`[data-group="statesBody"]`), et le pane Leaflet du fill n'est pas dans ce sous-arbre. Router cette opacité spécifique vers le pane demanderait un câblage dédié (lire `styles.states.statesBody.attrs.opacity` directement dans `drawStates()`/`createTerritoryLayer`) — pas fait faute de temps, même famille d'écart que le "font-family" de `provinces`. Le halo, lui, reste un vrai `<g>` SVG et son opacité continue de fonctionner normalement.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1024/1024 tests passent (dont `diplomacy-editor.test.ts`), build Vite complet.
- **Reste à vérifier par un humain** : rendu, survol, recoloration, suppression, "localiser", fog/focus, survol de fusion — même liste que `provinces` — **plus** : activer `shapeRendering: geometricPrecision` (Options → Interface) et confirmer que le halo de bordure s'affiche et suit bien le contour de chaque état ; ouvrir l'éditeur de diplomatie et confirmer que les états se recolorent selon leurs relations, sans planter et sans laisser un état sans tracé.

**Bug sévère trouvé et corrigé, affectant rétroactivement les 5 couches déjà livrées : Leaflet ne rend RIEN tant que la carte n'a pas de vue établie, silencieusement.** Découvert en écrivant le premier test qui exerçait vraiment le rendu Leaflet sous jsdom (`draw-rivers.test.ts` — aucune des 5 couches précédentes n'avait de test de ce type, seulement des mocks évitant Leaflet, ou `layers.test.ts` qui teste la mécanique de `LayersRegistry`, pas le rendu géré par chaque couche).
- **Le mécanisme** : `L.geoJSON(...).addTo(map)` ne plante pas et ne prévient de rien si `map` n'a encore jamais eu de vue (`map.setView`/`flyTo` jamais appelé, `map._loaded` reste `false`) — les tracés ne sont simplement jamais créés dans le DOM (`layer.getElement()` reste `undefined`). Confirmé par un script Node+jsdom isolé, en dehors de vitest, pour éliminer toute variable liée à l'environnement de test. Bonne nouvelle vérifiée dans le même script : si des couches sont ajoutées **avant** que la vue existe, elles "rattrapent" correctement leur rendu dès qu'une vue est enfin établie — ce n'est pas une perte définitive, juste un rendu différé jusque-là.
- **Portée réelle en production** : `getLeafletMap()` (`leaflet-map.ts`) ne posait jamais de vue initiale elle-même — elle comptait sur le fait qu'un appel ultérieur de `zoom.ts` (`resetZoom`/`zoomTo`, typiquement déclenché tôt par le flux normal de génération/chargement de carte) finirait par en établir une. Si **quoi que ce soit** appelle `Layers.draw(...)`/`Layers.init()` pour une couche `"leaflet"` avant ce premier `resetZoom`/`zoomTo`, cette couche restait invisible jusqu'à ce que la vue soit enfin posée — un risque de timing réel, pas hypothétique, pour `biomes`/`religions`/`cultures`/`provinces`/`states` en plus de `rivers`.
- **Corrigé à la source, une fois pour toutes les couches** : `getLeafletMap()` pose maintenant elle-même une vue initiale (`map.setView(L.latLng(0,0), map.getMinZoom(), {animate:false})`) immédiatement après la construction de la carte — avant que quoi que ce soit d'autre puisse s'exécuter. La vraie vue initiale (celle que l'app ajuste normalement à la génération) la remplace l'instant d'après, exactement comme avant ; ce correctif élimine juste la fenêtre de risque où rien n'était encore posé.
- **Vérifié** : reproduit et corrigé via un script Node+jsdom isolé (hors suite de test, pour confirmer que ce n'était pas un artefact de mock) ; `draw-rivers.test.ts` passe maintenant ses 6 cas, y compris ceux qui exercent le rendu Leaflet réel de bout en bout sous jsdom — la première fois qu'un test de ce projet vérifie qu'une couche convertie affiche réellement quelque chose, pas seulement qu'elle ne plante pas.
- **Reste à vérifier par un humain** : rien de spécifique à ce correctif — il rend la production plus sûre, ne change aucun comportement visible attendu.

**Couche `rivers` convertie et vérifiée** — la première hors des cinq remplissages de territoire, et la plus grosse réécriture de la phase jusqu'ici :
- **Géométrie, pas un simple portage** : une rivière est un polygone en ruban à largeur variable (`Rivers.getRiverPath`), pas une `LineString`. La fonction existante lissait ce ruban avec une courbe Catmull-Rom (impossible à représenter en GeoJSON, qui n'a que des segments droits) — plutôt que reparser la chaîne `d` (fragile), `river-generator.ts` a été factorisé : la boucle de calcul des points décalés gauche/droite (déjà de simples `[x,y]`, juste jetés dans un générateur de courbe d3 ensuite) est extraite dans `getOffsetPoints`, partagée par l'ancienne `getRiverPath` (inchangée, toujours utilisée par `erosion-bake.ts` pour le rendu Canvas) et une nouvelle `getRiverPolygonRing` qui relie les mêmes points par des segments droits — `meander()` échantillonne déjà assez dense pour que ça reste visuellement lisse. Zéro nouvelle trace de géométrie, juste un format de sortie différent des mêmes calculs.
- **Couleur partagée, pas par-rivière** : contrairement aux 5 couches précédentes (chacune a sa couleur propre en base), toutes les rivières partagent UNE couleur (`styles.rivers.attrs.fill`), héritée aujourd'hui en CSS depuis `#rivers`. Leaflet écrit toujours un `fill` explicite par tracé (pas d'"hériter" natif) — donc chaque tracé rendu se fait retirer son `fill` juste après coup (`fixupFill`), sauf pendant la coloration par bassin. Ajout de `"fill"` à `CSS_COMPATIBLE_ATTRS` (`styles.ts`, le mécanisme déjà construit pour `opacity`/`filter`/`mask`) pour que ce `fill` hérité vienne bien du pane — sûr aussi pour toutes les autres couches, puisqu'un `fill` explicite par entité (déjà le cas de `provinces`/`states`/etc.) l'emporte toujours sur un `fill` hérité.
- **Longueur sans DOM** : `river-editor.ts`'s `updateRiverLength` lisait `getTotalLength()/2` sur le tracé SVG rendu — impossible sur un polygone Leaflet. Nouvelle `Rivers.getRiverLength(meanderedPoints)` (somme des distances entre points du centre de la rivière) — une mesure géométrique directe, plus précise que l'ancienne approximation par moitié-de-périmètre, pas juste un contournement.
- **Mise à jour incrémentale préservée** : `redrawRiver()` (édition en direct d'une seule rivière) retrouve son équivalent Leaflet — retirer/rajouter juste la sous-couche de CETTE rivière (`geoJsonLayer.removeLayer`/`addData`), sans toucher aux autres, pour ne pas re-rendre 1000 rivières à chaque frame de glisser-déposer d'un point de contrôle.
- **Culling abandonné, assumé** : l'ancien renderer avait son propre système de culling par viewport (`ViewportLayers`/`Scene`, ne matérialisant que les rivières visibles). Leaflet dessine toutes les features fournies, peu importe leur visibilité. Pour l'instant, toutes les rivières sont rendues d'un coup, sans culling — acceptable tant que le nombre de rivières reste raisonnable, à revisiter si une carte à forte densité de rivières montre un vrai problème de performance.
- **Risque d'interactivité réel, mais déjà dans le scope connu (item 2 ci-dessous), pas nouveau** : contrairement aux 5 couches précédentes (non-interactives par design — `pointer-events: none` sur `#regions`/`#cults`/`#relig`/`#biomes`/`#provincesBody` dans `index.css`, confirmé), `#rivers` n'a **pas** cette règle : cliquer une rivière sur la carte pour ouvrir son éditeur (`viewbox-events.ts`'s `PARENT_EDITORS.rivers`) et le survol carte→tableau dans `rivers-overview.ts` (`applyLineHighlighting`, repli DOM `target.closest("[id^='river']")` une fois `pack.cells.r[cellId]` insuffisant) dépendent de VRAIS événements pointeur atteignant le tracé rendu. Or `legacySvg` peint visuellement au-dessus de tous les panes Leaflet (décision déjà documentée) — si un élément SANS `pointer-events:none` se trouve juste au-dessus des mêmes pixels, le clic n'atteindrait jamais la rivière Leaflet en dessous. Non résolu ici : fait explicitement partie de l'item 2 (délégation de clics), déjà identifié avant cette conversion, pas une régression découverte a posteriori. **Ce qui reste garanti de fonctionner** : ouvrir/éditer une rivière depuis le tableau "Rivers Overview" (pas de dépendance au clic carte), tout le survol tableau→carte (survol d'une ligne du tableau colore la rivière sur la carte — sens inverse, ne dépend d'aucun événement pointeur sur la forme rendue), et le survol carte→tableau via `pack.cells.r[cellId]` quand la cellule survolée appartient directement au tracé de la rivière (le cas courant).
- **`services/io/auto-update.ts` (item 7, maintenant confirmé, pas juste supposé)** : les 4 blocs de migration legacy qui lisent `getTotalLength()`/`getPointAtLength()` sur `<path id="river{i}">` sont TOUS gardés par `isOlderThan(...)` et lisent le contenu du **fichier chargé** (son propre SVG embarqué, potentiellement très ancien), jamais mon rendu en direct — confirmé par une relecture complète du fichier, aucun changement nécessaire.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1022/1022 tests passent (nouveau `draw-rivers.test.ts` réécrit pour la nouvelle architecture, y compris un test qui exerce le vrai rendu Leaflet de bout en bout), build Vite complet.
- **Reste à vérifier par un humain** : rendu et couleur d'une rivière ; glisser un point de contrôle en édition et confirmer que la forme suit en direct sans re-rendre les autres rivières ; surbrillance par bassin (bouton dédié dans Rivers Overview) ; survol tableau→carte et carte→tableau ; **et spécifiquement** confirmer ou infirmer si cliquer une rivière directement sur la carte ouvre encore son éditeur (résultat incertain, voir ci-dessus — un résultat négatif n'est PAS une régression de cette passe, juste la confirmation du risque déjà documenté).

**Couche `routes` convertie et vérifiée** — même famille de risque que `rivers` (courbe, pas une ligne droite), plus une complication propre : un style **par groupe**, ouvert et dynamique, pas un style unique ni par-entité.
- **Courbe, comme `rivers`** : `Routes.getPath` dessine chaque route avec une courbe Catmull-Rom (alpha différent par groupe : 0.1 pour roads/trails, 0.5 pour searoutes). Contrairement aux rivières, PAS de largeur variable ici — juste une ligne, donc pas de refactor nécessaire dans `routes-generator.ts` : les points bruts de la route suffisent directement comme coordonnées GeoJSON `LineString`, segments droits à la place de la courbe. `Routes.getPath` reste intact (aucun autre appelant à casser, contrairement à `getRiverPath`) mais n'est plus utilisé pour le rendu en direct — seulement par `Routes.getLength()`, qui construit son propre `<path>` **jamais attaché au document** juste pour l'appeler dessus : un calcul déjà 100% indépendant du rendu live, aucun changement nécessaire (contrairement à `river.length`, qui lisait `getTotalLength()` sur l'élément **rendu**).
- **Style par groupe, dynamique** : `styles.routes.groups` est un `z.record` ouvert (roads/trails/searoutes par défaut, plus tout groupe créé par l'utilisateur via l'éditeur de groupes). Contrairement au fill hérité des rivières, Leaflet supporte nativement `color`/`weight`/`dashArray`/`lineCap`/`opacity` par feature — la fonction `style()` de `route-layer.ts` lit directement `styles.routes.groups[feature.properties.group].attrs` à chaque style, sans détour CSS.
- **Trois fichiers énuméraient les groupes en lisant les `<g>` DOM de `#routes`** (`route-editor.ts`, `route-creator.ts`, `route-groups-editor.ts`) — remplacés par `Object.keys(styles.routes.groups)`, la source déjà "autoritaire" selon le commentaire préexistant du code (`route-groups-editor.ts`). `addGroup()`/`removeGroup()` ne créent/suppriment plus de `<g>` SVG par groupe (Leaflet n'en a pas besoin, tout vit dans UNE couche stylée par feature) — `removeGroup()` doit maintenant explicitement supprimer l'entrée `styles.routes.groups[group]` (avant, supprimer le `<g>` suffisait à le faire disparaître de la liste DOM-dérivée ; la liste est maintenant dérivée du store, donc un groupe non supprimé du store y resterait indéfiniment).
- **Bug réel trouvé et corrigé en écrivant l'édition en direct, applicable rétroactivement à `rivers`** : `route-editor.ts`/`river-editor.ts` attachent un gestionnaire de clic **directement sur l'élément rendu** pendant l'édition ("cliquer sur la route/rivière pour ajouter un point de contrôle"). Mon premier jet de `updateOne` (retirer/rajouter la sous-couche à chaque frame de glisser-déposer) aurait **discrètement cassé cette liaison** dès le premier point déplacé — un NOUVEL élément DOM remplace l'ancien, sans le gestionnaire. Corrigé pour les deux couches : `updateOne` met à jour la géométrie **en place** via `L.Polygon`/`L.Polyline`'s `setLatLngs()` (natif Leaflet) quand la forme existe déjà, au lieu de retirer/rajouter — le nœud DOM, et tout gestionnaire externe qui lui est attaché, survit. Pour `routes` spécifiquement, un changement de **groupe** en cours d'édition a aussi besoin d'un re-style (pas seulement d'une géométrie) — `updateOne` réapplique le style en plus de `setLatLngs`, ce qui a permis de **supprimer** le rebranchement manuel que `route-editor.ts`'s `changeGroup()` faisait avant (`redrawRouteShape` suffit maintenant).
- **Régression réelle mais mineure trouvée et documentée, pas corrigée** : la migration de fichiers anciens "v1.145 svg layer cleanup" (`auto-update.ts`) déduplique les `<g>` orphelins en les retrouvant via `Layers.all`'s `elementId` **courant** — pour une couche "leaflet", cet id a changé (`"routes"` → `"routes-leaflet"`), donc cette étape ne retrouve plus les groupes `roads`/`trails`/`searoutes` **du fichier chargé** (potentiellement très ancien) pour les dédupliquer. Portée réelle : cosmétique et légale uniquement — ces `<g>` ne sont plus jamais lus ni rendus par le code actuel (Leaflet prend le relais inconditionnellement), donc un doublon non nettoyé reste un déchet DOM inerte dans le fichier sauvegardé, jamais un bug visible ou fonctionnel. Test mis à jour pour documenter ce comportement accepté plutôt que de le cacher. `biomes`/`religions`/`cultures`/`provinces`/`rivers` ont le même changement d'`elementId`, mais n'ont pas de sous-groupes déclarés à dédupliquer — cette régression ne touche donc que `routes` en pratique, pas les cinq autres.
- **Interactivité, même risque connu que `rivers`, pas nouveau** : `#routes` n'a pas non plus `pointer-events:none` (confirmé) et `routes` est dans `GRAND_EDITORS` de `viewbox-events.ts` — cliquer une route sur la carte pour ouvrir son éditeur dépend du même risque déjà documenté pour `rivers`.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1022/1022 tests passent (`draw-routes.test.ts` réécrit pour la nouvelle architecture — y compris le changement de groupe en direct — et un test de `auto-update.test.ts` mis à jour pour documenter le gap ci-dessus), build Vite complet.
- **Reste à vérifier par un humain** : rendu et couleur par groupe (roads/trails/searoutes visuellement distincts) ; glisser un point de contrôle en édition (la forme suit, le clic pour ajouter un point continue de marcher) ; changer le groupe d'une route en cours d'édition (la couleur doit changer, la route doit rester éditable) ; créer/supprimer un groupe personnalisé dans l'éditeur de groupes de routes ; l'aperçu temporaire pendant la création d'une nouvelle route (Route Creator) ; même incertitude que `rivers` sur le clic direct carte→éditeur.

**Couche `markers` convertie et vérifiée** — pas un `L.geoJSON` comme les sept précédentes : des points/icônes, pas des polygones ni des lignes, donc `L.marker`/`L.divIcon` plutôt que le gabarit territoire/rivière/route.
- **Design** : `src/renderers/leaflet/marker-layer.ts` (nouveau) — un `L.layerGroup` de `L.marker`, chacun avec une `L.divIcon` dont le HTML est `<svg id="marker{i}" viewBox="0 0 30 30" width=... height=... x=... y=...>...</svg>` : **le même élément `<svg>` qu'avant**, avec le même id, la même fonction `getMarkerContent`/`getPin`/`pinShapes` (déplacée telle quelle depuis `draw-markers.ts`), juste imbriqué dans le `<div class="leaflet-marker-icon">` que Leaflet gère plutôt que positionné à la main dans `#markers`. `x`/`y`/`width`/`height` sur ce `<svg>` restent le coin haut-gauche/la taille **en coordonnées monde**, exactement la même convention que l'ancien renderer — délibéré : `highlightElement()` (survol "localiser" depuis Markers Overview/Markers in Radius) lit ces attributs directement (`element.tagName === "svg" ? getBBox(element) : element.getBBox()`, où `getBBox` ici est le helper maison qui lit `x`/`y`/`width`/`height`, pas la vraie API DOM) — en préservant cette convention, ce code n'a eu **aucun changement** à faire, contrairement à `getBBox()`/`getScreenCTM()` sur les couches précédentes qui, eux, lisaient de la géométrie DOM réellement rendue (donc fausse une fois Leaflet aux commandes).
- **Glisser-déposer, vraie réécriture** : l'éditeur (`markers-editor.ts`) déplaçait un marqueur en réécrivant `x`/`y` à la main sur l'élément pendant un `d3-drag` — inutilisable ici (Leaflet positionne l'icône par transformation CSS sur le `<div>` englobant, pas via les attributs de son contenu). Remplacé par le glisser-déposer natif de Leaflet (`draggable: true` sur le marqueur en cours d'édition, câblé dans `marker-layer.ts` uniquement pendant l'édition ; `wireDrag` retransmet la position finale via un callback `(x, y)` que `markers-editor.ts` utilise pour mettre à jour `selectedMarker.x/y/cell` et redessiner). Bénéfice net : Leaflet gère tout le rendu pendant le glisser (avant, `dragMarker` recalculait `x`/`y` à la main à chaque frame) ; le point de contact avec l'éditeur se réduit à un seul callback de fin de glisser.
- **Redimensionnement au zoom, décision de simplification assumée** : l'ancien renderer recalculait `zoomSize` (option `rescale`) à **chaque frame** de zoom via `ViewportLayers`. Reconstruire une `divIcon` (donc remplacer le nœud DOM du marqueur) à cette fréquence serait coûteux et inutile visuellement. `marker-layer.ts`'s `refreshSizeForZoom()` est appelée une seule fois, **à la fin du geste de zoom** (`zoom.ts`'s `invokeActiveZooming()`, déjà le point où `applyLabelsZoomSize`/le halo des états se recalculent) plutôt qu'à chaque frame — compromis délibéré : la taille des icônes reste figée pendant le geste, se réajuste dès qu'il se termine, au lieu de suivre en continu.
- **Culling abandonné, même raisonnement que `rivers`/`routes`** : plus de viewport culling — tous les marqueurs visibles (non cachés/filtrés) sont toujours rendus, Leaflet gère seul ce qui est effectivement à l'écran.
- **Trois suppressions DOM directes corrigées** (`markers-overview.ts`'s `removeMarker`/`removeAllMarkers`, `markers-in-radius.ts`'s `confirmRemove`) : elles faisaient `document.getElementById('marker{i}').remove()` à la main puis ne redessinaient pas toujours — même classe de bug que `religions`/`cultures`/`provinces`/`states` (désynchronise le suivi interne de Leaflet). Remplacées par un `Layers.draw("markers")` complet après la mutation de `pack.markers` (pour `confirmRemove`, `applyRadius()` juste après faisait déjà ce redraw — la suppression DOM manuelle était redondante, retirée).
- **Interactivité, même risque connu que `rivers`/`routes`, pas nouveau** : `markers` est dans `GRAND_EDITORS`/`GREAT_EDITORS` de `viewbox-events.ts`, et `#markers` n'a pas `pointer-events:none`. Cliquer un marqueur directement sur la carte pour ouvrir son éditeur dépend du même risque de délégation de clics déjà documenté (item 2 ci-dessous) — non résolu ici, pas une régression propre à cette conversion. Ce qui reste garanti : ouvrir un marqueur depuis Markers Overview/Markers in Radius (pas de dépendance au clic carte), tout le survol tableau→carte.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1025/1025 tests passent (`draw-markers.test.ts` réécrit pour la nouvelle architecture — plus de culling/export via `ViewportLayers`, remplacé par des tests sur le filtrage épinglé/masqué/par-liste, le redimensionnement rescale+zoom, et le marqueur en édition), build Vite complet.
- **Reste à vérifier par un humain** : rendu et icône de chaque type de marqueur ; glisser un marqueur en édition et confirmer que la position suit puis se fige à la bonne coordonnée ; le redimensionnement au zoom (option "rescale" dans l'éditeur de style) ; Markers Overview et Markers in Radius (filtrage, épinglage, verrouillage, suppression, "localiser") ; même incertitude que `rivers`/`routes` sur le clic direct carte→éditeur.

**Découverte tardive, plus large que `markers` seule : l'export d'image (SVG/PNG/JPEG) ne contient plus aucune des 8 couches converties.** Pas corrigé dans cette passe — hors du scope annoncé ("rivers, routes, burgs, markers, délégation de clics, minimap"), mais assez sérieux pour être documenté immédiatement plutôt que découvert plus tard en silence.
- **Le mécanisme** : `src/services/io/export.ts`'s `exportToSvg`/`exportToPng`/`exportToJpeg`/`exportToPngTiles` clonent tous `ensureEl("map")` (`cloneEl = ensureEl("map").cloneNode(true)`) puis construisent le fichier exporté à partir de ce clone. Depuis le début de la Phase 5, `biomes`/`religions`/`cultures`/`provinces`/`states`/`rivers`/`routes`/`markers` ne vivent plus DANS `#map` — ils vivent dans des panes Leaflet, enfants du conteneur Leaflet, à côté de `#map` (voir `legacySvg`/`getFeaturePane` dans `leaflet-map.ts`). Un `cloneNode(true)` de `#map` ne peut donc plus les capturer : ils sont silencieusement absents de tout export image, alors qu'ils s'affichent normalement à l'écran.
- **Portée réelle** : sérieuse, pas cosmétique — contrairement aux écarts "font-family"/"statesBody opacity" déjà documentés (narrow, dégradés gracieux), ceci touche une fonctionnalité centrale (Export → SVG/PNG/JPEG/tuiles) et la totalité des couches déjà livrées par cette phase, pas un cas de bord. Un utilisateur qui exporte sa carte aujourd'hui obtient une image sans biomes, sans territoires, sans rivières, sans routes, sans marqueurs.
- **Pourquoi pas corrigé ici** : une vraie correction demande soit de sérialiser chaque couche convertie en SVG à l'export (probablement via un usage à l'export de `getFeaturePath()`/l'équivalent GeoJSON→`d` déjà construit pour `territory-layer.ts`/`river-layer.ts`, pour re-générer des `<path>` dans le clone), soit de cloner directement le DOM des panes Leaflet dans le clone avant sérialisation — les deux sont un chantier à part entière, pas un correctif ponctuel, et n'étaient pas dans le scope demandé pour cette passe.
- **Reste à faire** : décider de l'approche avec l'utilisateur, puis l'implémenter — probablement une nouvelle section dédiée dans cette Phase 5, ou son propre item de suivi séparé.

**Couche `burgs` (`burgIcons`/`anchors`) convertie et vérifiée** — même famille que `markers` (points, `L.marker`/`L.divIcon`), avec deux icônes indépendantes possibles par burg (icône de groupe + glyphe "ancre" pour les ports), et la cascade `label-spread.ts` annoncée depuis le début de cette phase.
- **Design, même astuce de centrage que l'ancien rendu** : chaque `<symbol>` d'icône dans `index.html` est dessiné centré sur son origine locale (0,0) (ex. `<circle cx="0" cy="0" r="5"/>`), et un `<use href="#icon" x="cx" y="cy"/>` sans transform place ce contenu centré exactement à `(cx, cy)`. `burg-icon-layer.ts` (nouveau) reproduit ce mécanisme à l'identique pour chaque `divIcon` : un petit `<svg viewBox="0 0 2·size 2·size">` propre à chaque marqueur, avec `<use x="size" y="size">` en son centre — même maths de `font-size`/`viewBox` que l'ancien `<g font-size="{size}">` partagé, juste porté sur un wrapper individuel plutôt qu'un groupe commun. `id="burg{i}"`/`id="anchor{i}"` et `data-id`/`data-group` sont conservés à l'identique pour la compatibilité des lookups existants.
- **Pas de glisser-déposer à réécrire, contrairement à `markers`** : `burg-editor.ts`'s "relocate" clique sur le fond de carte (`#viewbox`, toujours valide) pour repositionner un burg, il ne déplace jamais l'icône rendue elle-même — un vrai point où cette conversion est **plus simple** que celle des marqueurs, confirmé par l'audit avant d'écrire du code.
- **Pas de `getFeatureBounds()`/`getBounds()` à ajouter non plus** : "localiser" (`zoomIntoBurg`) lit `pack.burgs[id].x/y` directement, jamais de géométrie DOM — contrairement aux cinq couches de territoire, rien à corriger ici.
- **`label-spread.ts`'s `getDisplayedBurgIconBounds()` — la vraie pièce délicate, anticipée avant l'implémentation** : contrairement au `getFeatureBounds()` à une ligne des couches précédentes, cette fonction doit continuer à lire la géométrie **réellement rendue** (`getBoundingClientRect()`) plutôt qu'à calculer une boîte a priori — parce que l'artwork des icônes de burg déborde largement et différemment de sa boîte nominale selon le symbole (l'ancien commentaire du renderer dit "jusqu'à deux em au-dessus de son ancrage"), à l'inverse des marqueurs dont la boîte 30×30 fixe permettait de graver `x`/`y`/`width`/`height` en dur. Corrigé en remplaçant le calcul par CTM de `#viewbox` (`getScreenCTM().inverse()`) par les méthodes publiques de Leaflet (`map.containerPointToLatLng()`, relatif à `map.getContainer().getBoundingClientRect()`) — même substitution déjà faite pour `zoom.ts` en tout début de Phase 5 — et le sélecteur `#burgIcons use[data-id], #anchors use[data-id]` par `svg[id^='burg'][data-id], svg[id^='anchor'][data-id]` (chaque icône est maintenant son propre `<svg>` de haut niveau sous un pane Leaflet, plus un enfant d'un groupe partagé). Cette fonction n'est testée par aucune suite automatisée (confirmé avant de la modifier) — sa correction repose sur ce raisonnement, à valider en navigateur réel (voir plus bas). Le reste du solveur (~950 lignes) est de la géométrie pure, aucun changement nécessaire.
- **Ordre d'empilement des groupes, compromis assumé** : l'ancien rendu peignait les groupes entiers dans l'ordre `options.map.burgs.groups` (groupe suivant = par-dessus). Leaflet ordonne ses marqueurs par défaut selon leur position Y à l'écran (simulation de perspective). Un `zIndexOffset` proportionnel à l'ordre du groupe biaise l'empilement vers l'ancien comportement, sans garantie stricte à l'intérieur d'un même groupe — impact visuel attendu minime (icônes qui se chevauchent rarement en pratique).
- **Trois bugs réels trouvés et corrigés, hors de `draw-burg-icons.ts`/`label-spread.ts` eux-mêmes** :
  - `submap-tool.ts`'s `rescaleBurgStyles()` appelait `ensureEl("burgIcons").querySelectorAll(":scope > g")` pour découvrir quels groupes de burgs sont réellement utilisés sur la carte (afin de rescaler leur taille) — un vrai crash à la création d'une sous-carte (`ensureEl` retourne `null` silencieusement, `.querySelectorAll` sur `null` explose). Corrigé en dérivant l'ensemble des groupes directement de `pack.burgs` plutôt que du DOM — la question était déjà purement une question de données, jamais vraiment une question de rendu.
  - `map-tooltip.ts` (2 endroits) et `burgs-overview.ts`'s repli de survol tableau↔carte lisaient `target.closest("#burgIcons [data-id]")` — mis à jour vers le nouveau sélecteur `svg[id^='burg'][data-id], svg[id^='anchor'][data-id]`, même sélecteur que celui utilisé dans `label-spread.ts`. Comme pour `rivers-overview.ts` déjà documenté plus haut, ce correctif est "prêt pour l'avenir" plutôt qu'immédiatement garanti fonctionnel de bout en bout : dépend du même risque de délégation de clics/survol non résolu (item 2 ci-dessous).
  - `burg-editor.ts`'s `open()` gardait un repli mort (`select("#burgIcons").select(...)`) vers un conteneur qui n'existe plus — confirmé déjà inatteignable avant même cette conversion (`selectedId` est toujours défini directement par `open()`, ce repli n'était jamais consulté) ; supprimé pour ne pas laisser une référence trompeuse à du DOM disparu.
- **Interactivité, même risque connu que `rivers`/`routes`/`markers`, pas nouveau** : `burgIcons` est dans `GRAND_EDITORS` de `viewbox-events.ts`, `#burgIcons` n'a pas `pointer-events:none` (contrairement à `#anchors`, qui l'avait déjà et reste donc non-interactif par choix — reproduit via `interactive:false` sur le marqueur d'ancre). Cliquer un burg directement sur la carte pour ouvrir son éditeur dépend du même risque de délégation de clics déjà documenté — non résolu ici. Ce qui reste garanti : ouvrir un burg depuis Burgs Overview, la génération/placement de nouveaux burgs (clic sur fond de carte, jamais sur l'icône elle-même).
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1024/1024 tests passent (`draw-burg-icons.test.ts` réécrit pour la nouvelle architecture — un fichier de test existant que j'ai bien pensé à chercher AVANT d'implémenter cette fois, contrairement à l'oubli sur `routes`), build Vite complet.
- **Reste à vérifier par un humain, plus que d'habitude à cause de `label-spread.ts`** : rendu et icône de chaque groupe de burg, glyphe d'ancre sur les ports ; Burgs Overview (survol, recolorer via l'éditeur de groupe, suppression, "localiser") ; **surtout** : le placement des noms de ville reste sain (pas de nom qui chevauche son icône, pas de nom qui s'éloigne bizarrement d'un burg) sur une carte avec beaucoup de burgs — un régression silencieuse ici dégraderait le placement de **tous** les types de labels, pas seulement les noms de burg (les icônes de burg sont des obstacles pour tout le monde) ; création d'une sous-carte (Submap Tool) avec "Rescale burg styles" coché.

**Découverte tardive, plus large que `markers` seule : l'export d'image (SVG/PNG/JPEG) ne contient plus aucune des 9 couches converties (biomes/religions/cultures/provinces/states/rivers/routes/markers/burgs).** Pas corrigé dans cette passe — hors du scope annoncé ("rivers, routes, burgs, markers, délégation de clics, minimap"), mais assez sérieux pour être documenté immédiatement plutôt que découvert plus tard en silence.
- **Le mécanisme** : `src/services/io/export.ts`'s `exportToSvg`/`exportToPng`/`exportToJpeg`/`exportToPngTiles` clonent tous `ensureEl("map")` (`cloneEl = ensureEl("map").cloneNode(true)`) puis construisent le fichier exporté à partir de ce clone. Depuis le début de la Phase 5, ces 9 couches ne vivent plus DANS `#map` — elles vivent dans des panes Leaflet, enfants du conteneur Leaflet, à côté de `#map` (voir `legacySvg`/`getFeaturePane` dans `leaflet-map.ts`). Un `cloneNode(true)` de `#map` ne peut donc plus les capturer : elles sont silencieusement absentes de tout export image, alors qu'elles s'affichent normalement à l'écran.
- **Portée réelle** : sérieuse, pas cosmétique — contrairement aux écarts "font-family"/"statesBody opacity" déjà documentés (narrow, dégradés gracieux), ceci touche une fonctionnalité centrale (Export → SVG/PNG/JPEG/tuiles) et la totalité des couches déjà livrées par cette phase, pas un cas de bord. Un utilisateur qui exporte sa carte aujourd'hui obtient une image sans biomes, sans territoires, sans rivières, sans routes, sans marqueurs, sans burgs.
- **Pourquoi pas corrigé ici** : une vraie correction demande soit de sérialiser chaque couche convertie en SVG à l'export (probablement via un usage à l'export de `getFeaturePath()`/l'équivalent GeoJSON→`d` déjà construit pour `territory-layer.ts`/`river-layer.ts`, pour re-générer des `<path>` dans le clone), soit de cloner directement le DOM des panes Leaflet dans le clone avant sérialisation — les deux sont un chantier à part entière, pas un correctif ponctuel, et n'étaient pas dans le scope demandé pour cette passe.
- **Reste à faire** : décider de l'approche avec l'utilisateur, puis l'implémenter — probablement une nouvelle section dédiée dans cette Phase 5, ou son propre item de suivi séparé.

**Délégation de clics (`viewbox-events.ts`) réécrite pour `rivers`/`routes`/`markers`/`burgIcons`** — l'item annoncé "à réécrire, pas à porter" depuis le tout début de cette phase. S'est avéré être **deux bugs indépendants**, pas un seul, découverts par une recherche dédiée avant d'écrire du code (leçon retenue après la découverte tardive de bugs similaires sur `rivers`/`routes`/`states`) :
- **Bug 1, déjà documenté depuis le début : l'écoute de clic vivait sur le mauvais nœud.** `onClick` était lié à `#viewbox` (`select("#viewbox").on("click", onClick)`), qui ne fait partie de l'arbre DOM QUE du SVG legacy (hébergé dans son propre pane Leaflet `legacySvg`, voir plus haut). Un clic sur un élément rendu par une couche convertie (un `<path>` dans un pane Leaflet, ou un `<svg>` dans un `divIcon`) ne remonte JAMAIS à travers `#viewbox` — sous-arbre complètement différent — donc `onClick` ne le voyait tout simplement jamais, quel que soit le contenu des tables `PARENT_EDITORS`/`GRAND_EDITORS`/`GREAT_EDITORS`.
- **Bug 2, découvert seulement par une recherche dédiée avant l'implémentation, non anticipé par le plan initial : même en corrigeant le bug 1, la plupart des clics n'auraient toujours pas atteint la bonne cible.** `legacySvg` peint par-dessus tous les panes Leaflet (compromis déjà documenté) — donc si n'importe quel contenu legacy occupe le même pixel qu'un élément Leaflet-rendu et n'a pas `pointer-events: none`, ce contenu capte le clic en premier, avant même que l'événement DOM n'atteigne l'élément Leaflet en dessous. Une recherche dédiée (grep exhaustif de chaque règle `pointer-events` dans `public/index.css`, croisé avec `src/renderers/draw-ocean.ts`) a trouvé le vrai coupable : `#oceanBase` (`drawOceanBase()`), un rectangle plein `options.map.graph.{width,height}` — donc TOUTE la carte, terre comprise, pas seulement l'eau — ajouté en tout premier enfant de `#viewbox`, jamais masqué à la forme réelle de l'océan (contrairement à `#lakes`/`#landmass`, qui ont un vrai masque), sans règle `pointer-events` dédiée (donc `auto` par défaut). Sur n'importe quel pixel de terre "normal" (pas un lac, pas de la glace, pas exactement sur un trait de côte/frontière, pas un glyphe de label), tout le contenu legacy au-dessus de `#oceanBase` a soit `pointer-events: none` soit n'est simplement pas présent à ce pixel — le clic retombait donc systématiquement sur `#oceanBase`, qui ne correspond à aucune entrée des tables de délégation et absorbait le clic silencieusement. Résultat : avant ce correctif, cliquer une rivière/route/marqueur/burg sur "terrain ordinaire" échouait presque toujours, pas seulement dans des cas de bord — confirmé par une recherche avant d'écrire le moindre code, pas découvert après coup.
- **Corrigé en deux temps** :
  1. `#oceanBase` passe à `pointer-events: none` dans `public/index.css` — aucune régression possible : rien n'était câblé sur un clic à cet élément (absent de toutes les tables), il ne faisait qu'avaler des clics qui n'allaient nulle part.
  2. `onClick()` (`viewbox-events.ts`) vérifie maintenant `target.closest(".leaflet-pane")` en tout premier : si le clic est tombé dans un pane Leaflet **autre que** `legacyPane` (celui qui héberge le SVG legacy), il est résolu directement par une nouvelle table `PANE_EDITORS` clée sur l'id du pane (`rivers-leaflet`, `routes-leaflet`, `markers-leaflet`, `burg-icons-leaflet`) plutôt que par la marche à profondeur fixe dans les ancêtres DOM (sans objet pour la structure de panes/divIcon de Leaflet). Un clic sur `legacyPane`, ou nulle part dans un pane Leaflet, retombe intégralement sur l'ancienne logique de marche d'ancêtres, inchangée (renommée `onLegacyClick`) — les cinq couches de territoire (biomes/religions/cultures/provinces/states) restent volontairement absentes de `PANE_EDITORS`, exactement comme elles l'étaient déjà des anciennes tables. L'écouteur lui-même est maintenant posé sur `getLeafletMap().getContainer()` (le seul ancêtre commun aux deux arbres) plutôt que sur `#viewbox` — `clickSurface()`, exportée pour que `map-placement.ts` puisse s'y accrocher de la même façon un jour.
- **Piège évité de justesse, documenté pour ne pas être retenté** : la première version déplaçait AUSSI l'écoute de clic des outils de placement (`map-placement.ts`'s `toggleMapPlacement`, "cliquer sur la carte pour ajouter un marqueur/burg/label/rivière") vers ce même conteneur Leaflet partagé — mais les quatre créateurs (`marker-creator.ts`, `burg-creator.ts`, `label-creator.ts`, `river-auto-creator.ts`) calculent tous leurs coordonnées monde via `d3.pointer(event, event.currentTarget)`, qui dépend du CTM SVG de `#viewbox` — un conteneur HTML brut n'a pas de CTM, ça aurait cassé le placement de tout nouvel élément sur toute la carte. Revert : `map-placement.ts` reste lié à `#viewbox`, avec un commentaire expliquant pourquoi ne pas "corriger" ça de la même façon — le compromis accepté est que cliquer *exactement* sur une icône Leaflet-rendue pendant qu'un outil de placement est actif ne place rien (clic capté par un sous-arbre DOM différent, sans écouteur) plutôt que d'y placer le nouvel élément — un écart étroit et mineur, pas la régression générale que ce correctif résout par ailleurs.
- **Nouveau fichier de test, zéro couverture existante sur ce sous-système** : `src/components/viewbox-events.test.ts` (6 tests) — clic sur un élément rendu dans chacun des 4 panes convertis-interactifs résout le bon `Controllers.X.open(...)`, clic sur un pane de territoire ne fait rien, clic sur du contenu legacy retombe bien sur l'ancienne logique (vérifié via le cas spécial des labels de burg).
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1030/1030 tests passent, build Vite complet.
- **Reste à vérifier par un humain, en priorité sur cet item précis** : cliquer directement une rivière/route/marqueur/burg **sur la carte** (pas via un tableau Overview) doit maintenant ouvrir son éditeur — c'était l'incertitude explicitement non résolue après chacune des quatre conversions précédentes ; confirmer que ça marche enfin change le statut de cet écart dans les quatre sections ci-dessus, de "risque connu, non résolu" à "résolu". Vérifier aussi qu'un clic sur un lac/la glace/une frontière/le littoral (toujours legacy, toujours interactifs) fonctionne comme avant, et que les outils "cliquer pour ajouter" (marqueur/burg/label/rivière) placent toujours correctement au bon endroit.

**Minimap reconstruite** — dernier item du scope annoncé pour cette passe ("rivers, routes, burgs, markers, délégation de clics, minimap").
- **Pourquoi un portage direct était impossible** : l'ancienne implémentation mirait `#viewbox` en direct via `<use href="#viewbox">` — gratuit (aucun code de rendu propre à la minimap, juste une référence DOM toujours à jour) mais dépendant entièrement du fait que TOUT le contenu visible vivait dans `#viewbox`. Ce n'est plus vrai depuis `biomes` (premier layer converti de cette phase) : les 9 couches passées par Leaflet vivent dans des panes séparés, hors de `#viewbox`. Un simple portage aurait donc affiché une minimap avec un contenu manquant grandissant à mesure que la phase avançait — déjà le cas en pratique avant ce correctif.
- **Nouvelle architecture : une seconde instance `L.Map`, indépendante, pas un miroir DOM** — `src/controllers/minimap.ts` (réécrit) crée sa propre petite carte Leaflet (`crs: LinearSimpleCRS`, maintenant exportée depuis `leaflet-map.ts` pour être réutilisable), sans geste de pan/zoom propre (`dragging`/`scrollWheelZoom`/`doubleClickZoom`/`boxZoom`/`keyboard`/`touchZoom` tous `false`), calée une fois pour toutes sur l'étendue complète de la carte (`fitBounds`/`maxBounds` sur `[0,0]`–`[height,width]`, jamais changé ensuite). Alternative envisagée et écartée : mirer aussi les nouveaux panes Leaflet via `<use>` — rejetée parce que (a) les couches en `divIcon` (`markers`, `burgIcons`) sont du HTML, pas du SVG, donc pas référençables par `<use>` de toute façon, et (b) le contenu SVG des couches `L.geoJSON` restantes est positionné par la transformation interne (privée, non documentée) de Leaflet pour l'animation de zoom — reproduire son inverse aurait couplé ce code à des détails d'implémentation internes de Leaflet plutôt qu'à son API publique, contrairement à toute autre partie de cette phase.
- **Ce qu'affiche la minimap maintenant, et le compromis assumé** : une seule couche d'aperçu — les états, coloriés comme la couche `states` principale (`buildTerritoryFeatureCollection`/`territory-geojson.ts`, réutilisé tel quel, pas de nouveau code de géométrie) — plutôt que de refléter fidèlement n'importe quelle combinaison de couches actuellement visibles sur la carte principale (ce que le miroir DOM faisait gratuitement). Choix délibéré : à l'échelle d'une minimap, la plupart des détails (rivières, routes, icônes de burg/marqueur) seraient de toute façon illisibles ; les territoires d'état donnent le repère visuel le plus utile pour une vue d'ensemble, et c'est la couche affichée par défaut (preset "Political"). Pas un remplacement complet à l'identique — documenté comme tel, pas présenté comme équivalent.
- **Rafraîchissement du contenu, autre compromis assumé** : contrairement à l'ancien miroir DOM (toujours à jour automatiquement, sans code), le contenu de la minimap ne se redessine explicitement qu'à l'ouverture du dialogue (`refreshMinimapContent()`), pas en continu pendant qu'il reste ouvert — recalculer `getIsolines` à chaque frame de pan/zoom (60×/seconde pendant un geste) serait un vrai coût de performance pour un gain rarement utile en pratique (les états changent rarement pendant qu'on regarde la minimap). Le rectangle de viewport, lui, reste mis à jour à chaque frame comme avant (`window.updateMinimap()`, toujours appelé depuis `zoom.ts`) — c'est la partie qui a vraiment besoin d'être fluide. Si un état est recoloré/fusionné pendant que le dialogue reste ouvert, la minimap ne le reflète qu'à la prochaine ouverture — écart mineur, pas corrigé faute d'un point d'accroche déjà existant pour "le pack a changé" (l'ancien code n'en avait pas non plus, il en profitait juste gratuitement via le DOM).
- **Clic pour centrer, plus simple qu'avant** : l'ancien code convertissait le clic en coordonnées monde à la main (`createSVGPoint()` + `getScreenCTM().inverse()`). La nouvelle version encaisse le clic Leaflet natif de la mini-carte (`minimapMap.on("click", ...)`), qui donne déjà `event.latlng` en coordonnées monde (même convention lat=y/lng=x que partout ailleurs dans cette phase) — plus de manipulation de matrice manuelle.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1034/1034 tests passent (`minimap.test.ts`, nouveau, 4 tests — aucune couverture existante avant ce correctif : construction de la mini-carte et coloriage par état, clic → `zoomTo` avec coordonnées bornées à l'étendue du graphe, `updateMinimap()` ne redessine jamais le territoire, fermeture puis réouverture du dialogue sans planter), build Vite complet.
- **Reste à vérifier par un humain** : ouvrir la minimap, confirmer que les états s'affichent avec les bonnes couleurs et la bonne étendue ; cliquer dedans pour centrer la carte principale (bon endroit, pas de décalage) ; panner/zoomer la carte principale et confirmer que le rectangle de viewport suit correctement en fluidité ; redimensionner/rouvrir le dialogue plusieurs fois de suite sans erreur console ; recolorer un état pendant que la minimap est ouverte, confirmer qu'elle ne se met PAS à jour avant réouverture (comportement attendu, pas un bug à signaler).

**Bug critique trouvé et corrigé après coup, via une revue de code dédiée (pas une découverte pendant l'implémentation) : les 9 couches Leaflet de cette phase rendaient verticalement inversées, depuis `biomes`, le tout premier layer converti.**
- **Le mécanisme, confirmé empiriquement, pas juste soupçonné** : `LinearSimpleCRS` (`leaflet-map.ts`) ne surchargeait que `scale`/`zoom` sur `L.CRS.Simple`, en gardant sa `transformation` par défaut (`new L.Transformation(1, 0, -1, 0)`), qui inverse l'axe y — `L.CRS.Simple` suppose par défaut une convention "y augmente vers le haut" (comme une carte géographique). Or toutes les données de cette app (`pack.cells.p`/`grid.points`, et la transformation manuelle `translate/scale` que `#viewbox` continue d'appliquer à ses propres enfants) utilisent la convention opposée, habituelle en SVG/écran : y augmente vers le BAS. Un script de vérification jetable (`L.map(...).latLngToContainerPoint(...)`, comparé point par point à la même transformation `translate(origin.x + k·x, origin.y + k·y)` que `#viewbox` applique à son propre contenu) a confirmé : tout contenu rendu par `L.geoJSON`/`L.marker` (donc les 9 couches converties : biomes/religions/cultures/provinces/states/rivers/routes/markers/burgs) apparaissait à une position en miroir vertical par rapport à `#viewbox` (labels, littoral, lacs, glace — tout ce qui reste en SVG classique) ET par rapport aux coordonnées `pack` réelles.
- **Portée** : maximale — pas un cas de bord. Chaque couche Leaflet-rendue de toute cette phase était mal positionnée verticalement par rapport au reste de la carte depuis le tout premier commit de conversion (`biomes`). Le fait que ça n'ait pas été signalé plus tôt confirme juste qu'aucune vérification humaine en navigateur n'a encore eu lieu depuis — exactement le risque que `LAUNCH.md` répète après chaque couche convertie sans jamais avoir été confirmé.
- **Pourquoi le reste du système fonctionnait quand même** : la caméra (`zoom.ts` : pan, zoom, `resetZoom`, `zoomTo`, la synchronisation de `#viewbox`) ne dépend jamais du signe de `transformation` — elle ne fait que lire/écrire un seul point de référence (`map.latLngToContainerPoint(L.latLng(0,0))`) et s'appuie sur le fait que `project`/`unproject` sont toujours l'inverse exact l'un de l'autre, quel que soit le contenu de `transformation`. Même chose pour les fonctions "localiser" (`getFeatureBounds`/`getBounds`) : elles lisent `L.LatLngBounds.getWest/East/North/South()`, qui travaillent directement sur les valeurs `lat`/`lng` stockées (nos `x`/`y` bruts, jamais projetés) — donc déjà correctes, indépendamment du flip. Seul le rendu visuel des couches Leaflet elles-mêmes (position des `<path>`/marqueurs à l'écran) était affecté.
- **Corrigé** : `LinearSimpleCRS` surcharge maintenant aussi `transformation: new L.Transformation(1, 0, 1, 0)` — une transformation identité pure (aucune inversion), qui fait concorder exactement le rendu Leaflet avec la transformation manuelle de `#viewbox` (vérifié point par point après correction, cette fois avec un résultat exact plutôt qu'inversé).
- **Effet de bord positif, pas un correctif séparé** : ce même bug expliquait aussi pourquoi `label-spread.ts`'s `getDisplayedBurgIconBounds()`/`screenRectToMapBounds()` (réécrit dans cette même passe) aurait produit des bornes avec `y1 > y2` (le haut-écran d'une icône correspond, une fois inversé, au `lat` le PLUS GRAND, pas le plus petit) — violant l'invariant `y1 ≤ y2` que le reste du solveur de placement de labels suppose partout. Corriger le CRS corrige aussi ce problème en cascade, sans toucher à `label-spread.ts` lui-même : une fois le rendu Leaflet non inversé, `containerPointToLatLng()` retourne des `lat` dans le bon ordre naturellement.
- **Nouveau test de régression, permanent** : `src/components/leaflet-map.test.ts` (nouveau, 2 tests) — vérifie qu'augmenter le y monde déplace le point vers le bas à l'écran, et que le rendu Leaflet concorde exactement avec la transformation manuelle de `#viewbox` sur plusieurs points arbitraires. Confirmé que ce test échoue bien (erreur de 200-400px) si on réintroduit l'ancien flip, avant d'être laissé en l'état corrigé — un vrai test de régression, pas un test qui passerait de toute façon.
- **Fichiers de test légués mis à jour** : 8 fichiers de test (`biomes-editor.test.ts`, `heightmap-editor.test.ts`, `route-editor.test.ts`, `styles-legacy.test.ts`, `styles.test.ts`, `label-spread-{burg,path,solver}.test.ts`) mockaient `"leaflet"` avec `{ extend, CRS: { Simple: {} } }` — sans `Transformation`, cassé par ce correctif puisque `LinearSimpleCRS` l'utilise maintenant. Mock étendu avec `Transformation: class {}` dans les huit, aucun changement de comportement testé.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1036/1036 tests passent, build Vite complet.
- **Reste à vérifier par un humain, en priorité absolue avant tout le reste de cette section** : ouvrir une carte existante (générée avant ce correctif) et confirmer que biomes/états/provinces/religions/cultures/rivières/routes/marqueurs/burgs s'alignent maintenant correctement avec le littoral/les labels/les frontières — pas seulement "quelque chose s'affiche", mais que ça s'affiche au bon endroit. C'est la vérification la plus importante de toute cette session : si elle échoue, aucune des vérifications listées plus haut pour `markers`/`burgs`/`rivers`/`routes`/la délégation de clics/la minimap n'a de sens tant que ceci n'est pas confirmé correct.

**Trois bugs supplémentaires trouvés par la même revue de code dédiée, tous corrigés et testés :**
- **Les outils de placement ("cliquer pour ajouter") pouvaient déclencher DEUX actions à la fois.** Avant la réécriture de la délégation de clics, `viewbox-events.ts`'s handler par défaut ET `map-placement.ts`'s handler de placement étaient tous deux liés au MÊME nœud (`#viewbox`) — `d3.on("click", fn)` sur un même nœud remplace toujours le précédent, garantissant qu'un seul des deux tournait à la fois. La réécriture a déplacé le handler par défaut sur `clickSurface()` (le conteneur Leaflet), un nœud DIFFÉRENT de celui où `map-placement.ts` reste lié (`#viewbox`, pour les raisons de CTM déjà documentées) — cassant cette exclusion mutuelle implicite. Résultat concret : avec l'outil "Ajouter un marqueur" actif, cliquer près (ou sur) un label de burg plaçait bien un marqueur, MAIS ouvrait AUSSI l'éditeur de burg en même temps, puisque le clic remonte par bulle à la fois à travers `#viewbox` (déclenchant le placement) et jusqu'au conteneur (déclenchant `onClick` par défaut). Corrigé : `toggleMapPlacement()` suspend maintenant explicitement `clickSurface().on("click", null)` en activant un outil.
- **Corollaire découvert en écrivant le test du bug ci-dessus, pas anticipé** : une fois ce premier correctif en place, arrêter l'outil de placement laissait un AUTRE résidu — `applyDefaultViewboxEvents()` ne faisait que réattacher `onClick` au conteneur, sans jamais nettoyer le handler de placement resté accroché à `#viewbox`. Un clic après avoir arrêté l'outil déclenchait donc encore le CALLBACK DE PLACEMENT (en plus, cette fois, du bon comportement par défaut). Corrigé : `applyDefaultViewboxEvents()` fait maintenant aussi `select("#viewbox").on("click", null)` pour nettoyer ce résidu. Les deux bugs sont couverts par un même test dans `viewbox-events.test.ts` qui clique deux fois (pendant, puis après le placement) et vérifie qu'exactement un seul callback tourne à chaque fois.
- **La formule de redimensionnement au zoom des marqueurs était inversée, et les burgs ne se redimensionnaient jamais du tout au zoom.** L'ancien renderer calculait une taille en coordonnées MONDE (`size/5 + 24/scale` pour `rescale` activé, `size` sinon), automatiquement multipliée par le zoom courant grâce à la transformation `scale(viewport.scale)` que `#viewbox` applique à tout son contenu — donnant une taille apparente à l'écran de `scale·size/5 + 24` (croissante avec le zoom). Le nouveau code (`marker-layer.ts`) réutilisait la même formule mais l'appliquait DIRECTEMENT comme taille CSS finale d'un `divIcon` (déjà en pixels écran, sans multiplication implicite par le zoom) — donnant `size/5 + 24/zoom`, qui **rétrécit** quand on zoome, l'inverse de l'ancien comportement. Pire, `rescale=0` (désactivé) ne redessinait JAMAIS la taille au zoom (retour anticipé sur `!rescale`), alors que dans l'ancien système TOUS les marqueurs grossissaient/rétrécissaient avec le zoom (juste via une formule différente) — un vrai return manquant, pas une différence de formule. Les burgs (`burg-icon-layer.ts`) n'avaient tout simplement aucun mécanisme de redimensionnement au zoom du tout — un point manqué pendant leur conversion, jamais retenté depuis. Corrigé pour les deux : `getMarkerSize` calcule maintenant directement la taille apparente (`zoom·size/5 + 24` si `rescale`, `zoom·size` sinon) ; `refreshSizeForZoom()` recalcule pour TOUS les marqueurs, plus de retour anticipé sur `rescale` ; une nouvelle fonction équivalente ajoutée pour les burgs (`refreshBurgIconsZoomSize`, câblée dans `zoom.ts`'s `invokeActiveZooming()` comme pour les marqueurs), avec la taille de base multipliée par `zoom` dès `update()` (avant, toujours dessinés à une taille "1×", correcte seulement par coïncidence au zoom initial). Les deux fonctions sautent le recalcul si le niveau de zoom n'a pas réellement changé (un pan pur) — évite de reconstruire chaque icône à chaque frame de pan, un vrai coût O(n) inutile relevé par la même revue.
- **Vérifié** : `tsc --noEmit` propre, lint propre, 1041/1041 tests passent (nouveaux tests dans `viewbox-events.test.ts`, `draw-markers.test.ts`, `draw-burg-icons.test.ts`), build Vite complet.
- **Reste à vérifier par un humain** : activer un outil "cliquer pour ajouter" (marqueur/burg/label/rivière) et confirmer qu'un clic ne déclenche plus qu'une seule action ; zoomer/dézoomer et confirmer que les icônes de marqueur ET de burg grossissent visiblement en zoomant (pas l'inverse), y compris avec l'option "rescale" désactivée pour les marqueurs.

**Ce qui reste, explicitement pas encore fait** :
1. `measurers-editor.ts`/`draw-coordinates.ts` (règle, graticule) : **pas d'impact prévu** — leur maths passe par le CTM de `#viewbox`, qui reste valide puisque `#viewbox` continue d'exister et d'être transformé par `zoom.ts`, inchangé.
2. `viewport-renderer.ts` (culling maison) : **pas de suppression prévue** contrairement à l'idée initiale du plan — il reste nécessaire pour les couches qui restent dans `#viewbox`, puisque Leaflet ne peut pas nativement culler un sous-arbre SVG géré à la main qu'il ne connaît pas.
3. L'opacité de `states.statesBody` — écart connu, pas de câblage dédié fait (voir plus haut).
4. **Sérieux** : l'export d'image (SVG/PNG/JPEG/tuiles) ne capture plus les 9 couches déjà converties (voir découverte ci-dessus) — non scopé initialement, à trancher avec l'utilisateur.
5. Le placement de nouveaux éléments (marqueur/burg/label/rivière) exactement sur une icône Leaflet-rendue ne fonctionne pas (voir le piège évité ci-dessus, section délégation de clics) — écart mineur accepté, pas prévu d'être corrigé sans une refonte plus large des outils de placement.
6. La minimap ne reflète qu'une seule couche (états) et ne se rafraîchit qu'à l'ouverture, pas en continu (voir ci-dessus) — écarts assumés, pas des bugs.
7. **Trouvé par la revue de code, pas corrigé, architectural** : les marqueurs et icônes de burg (et, dans une moindre mesure, toutes les couches Leaflet de cette phase) ne peuvent plus jamais peindre PAR-DESSUS `#labels`/`#armies` — puisque `legacySvg` peint toujours par-dessus tous les panes Leaflet (compromis déjà documenté en tout début de cette phase), un marqueur qui se retrouve visuellement sous un label de burg ou une icône militaire reste maintenant caché en dessous, alors qu'avant (DOM `#markers` après `#labels`/`#military` dans `#viewbox`) il passait toujours au-dessus. Pas un correctif ponctuel possible : corriger ceci demanderait de renverser l'ordre de peinture entre `legacySvg` et les panes Leaflet, ce qui casserait à son tour la visibilité de tout le HUD encore en SVG (légende, barre d'échelle, halos d'état, etc.) par-dessus les remplissages de territoire désormais opaques — un compromis strictement pire, pas un vrai correctif. Impact réel probablement mineur (chevauchement direct entre une icône et un label rare en pratique) mais réel, pas hypothétique.

**Toutes les huit couches d'affichage annoncées pour cette passe (rivers, routes, burgs, markers) sont converties, la délégation de clics est corrigée, et la minimap est reconstruite — le scope explicitement demandé par l'utilisateur pour cette session est fait.** Ce qui reste (items 1 à 7 ci-dessus, plus les items déjà notés section par couche) est soit hors scope initial (export d'image), soit un compromis documenté et assumé, soit un point sans impact confirmé — pas du travail oublié.

**Vérification** : chaque couche migrée comparée visuellement à l'ancien rendu SVG sur la même carte ; chaque outil interactif (règle, minimap, placement de labels) testé manuellement pour retrouver son comportement — c'est la phase où une vérification humaine en navigateur est la plus indispensable, aucune suite automatisée ne couvre ce niveau de détail visuel/interactif.

**Checklist de vérification manuelle détaillée** (déplacée depuis `LAUNCH.md`, qui reste volontairement un guide de lancement court — garder ce niveau de détail ici) :

Check this first, before anything else: open any map and confirm biomes/states/provinces/religions/cultures/rivers/routes/markers/burg icons line up correctly with the coastline, labels, and borders — not just that something renders, but that it renders in the *right place*.

- Drag to pan, scroll/pinch to zoom, double-click to zoom in — should feel the same as before.
- `F2` / the "new map" button, the heightmap gallery, and `?seed=`/`?maplink=` URLs should all still load and center correctly.
- Zoom in enough to trigger label resizing — reads `viewport.scale`, now fed by Leaflet rather than d3.
- Open the Style Editor for Biomes/Religions/Cultures/Provinces and check the opacity slider has a visible effect, and that these layers *aren't* fully opaque by default. Biomes specifically should never paint over open ocean.
- **Biomes**: switch to the "Biomes" layer preset — it should render and color exactly as before. In the Biomes editor, hover a biome row to confirm the highlight-on-hover outline still appears on the map, and change a biome's color to confirm it updates. Toggle the layer off and back on to confirm it still reappears correctly.
- **Religions**: switch to the "Religions" preset. Hover a row (outline highlight on the map), change a color (should update), click the "locate" icon on a row (should zoom/pan to that religion's territory), and remove a religion (its territory should disappear from the map, not just the editor list).
- **Cultures**: switch to the "Cultural" preset. Same checks as Religions — hover, recolor, "locate", and remove — plus each culture's center marker (a small circle) should keep tracking correctly.
- **Provinces**: switch to the "Provinces" preset. Same checks as the others, plus: click the fog/focus icon (pin icon) on a province row — it should dim everything outside that province's actual territory, correctly aligned; and start a province merge (Merge button) and hover a candidate province — the animated red outline trace should hug that province's real boundary.
- **States**: default view. Hover, recolor, "locate", remove, fog/focus, and merge-hover — same checks as Provinces. Plus: turn on **Options → Interface → shapeRendering → geometricPrecision** and confirm every state shows a soft blurred border glow that actually follows its territory's real shape; and open the **Diplomacy editor** and confirm every state recolors by its relationship to the selected one.
- **Rivers**: should render and color exactly as before (shared water color). Open **Rivers Overview**: hover a row (highlight red), "locate", and **Basin Highlight** (distinct color per drainage basin, reverts cleanly). Open the River editor (pencil icon, or click the river directly on the map) and drag a control point: shape updates live, other rivers don't flicker.
- **Routes**: roads/trails/sea routes keep their own distinct color/width/dash style. Open the **Route editor** and drag a control point — same live-update check, plus: change the route's group while editing — it should recolor *and remain editable* without reopening. Try **Route Groups editor**: add/assign/remove a group, colors stay in sync.
- **Markers**: same pin shapes/icons/colors as before. Open one (via **Markers Overview**, not by clicking the map) and **drag it** — smooth native Leaflet dragging, lands correctly. Change icon/shape/size/color while editing — updates live and stays draggable. Zoom in/out and confirm marker icons resize (grow when zooming in), settling once the gesture ends. Try **Markers Overview** filters, pin/unpin, lock/unlock, "locate", remove/remove all unlocked. Try **Markers in Radius**: radius circle and in-range list stay in sync.
- **Burg icons**: switch through layer presets and confirm town/capital/other icons render correctly, port burgs show an anchor glyph. Open **Burgs Overview**: hover-highlight, "locate", recoloring via **Burg Groups editor**. **Most important check**: with many burgs, confirm names read cleanly — not overlapping their icon or each other. Also try **Tools → Create a submap** with "Rescale burg styles" checked.
- **Click delegation, the highest-priority check**: click a river, a route, a marker, and a burg icon **directly on the map** — each should open its editor. Confirm always-click-through layers still work (lake, ice cap, coastline point, state/province border). Confirm "click to place" tools land correctly: **Add Marker**, **Add Burg**, **Add Label**, river auto-creator — click on empty land, not just near an existing icon (clicking exactly on an existing icon while placing a new one is a known, accepted gap — nothing should place there, but it shouldn't error either).
- **Minimap**: open from its toolbar button. Independent Leaflet map — confirm states render with real colors/shape. Click inside it: main map centers there. Pan/zoom the main map: the dashed viewport rectangle tracks it. Close/reopen a few times — never blank/broken. Known, accepted: only shows states (not rivers/routes/icons), only refreshes on (re)open, not live.

If any of that feels off, see the file list a few paragraphs up (`src/components/zoom.ts`, `leaflet-map.ts`, `viewbox-events.ts`, `src/renderers/leaflet/`, the `draw-*.ts` renderers, `label-spread.ts`, `minimap.ts`) for what changed. **Also worth knowing**: image export (SVG/PNG/JPEG/tiles) currently omits all 9 converted layers entirely — a known gap, not a surprise to re-report.

### Phase 6 — Retrait du format legacy (nouvelles cartes seulement)

Une fois la Phase 3 validée, les nouvelles cartes n'ont plus besoin du format positionnel `\r\n` de `save.ts` comme vérité — reformuler l'export `.map` comme une fonctionnalité "télécharger une sauvegarde/un instantané partageable" généré depuis Postgres à la demande, pas la source de vérité. Les anciens fichiers `.map` restent chargeables pour toujours via `load.ts` et sa logique de réparation existante — ne jamais tenter de faire disparaître ce chemin, c'est le seul accès aux données des utilisateurs existants.

---

## Schéma Postgres/PostGIS (concret)

Toutes les colonnes géométrie : SRID 0, indexées GiST. Tables d'entités clées `(map_id, id)` où `id` réutilise l'id assigné par FMG lui-même.

- **`maps`** — `id` PK, `name`, `seed`, `meta` (jsonb), `facts` (jsonb — graph size, geography, climate, cultures.set, lore, units, style preset, coastline, etc. — tout le bloc `facts` de `future-data-model.md`), `layers` (jsonb), `style` (jsonb), `created_at`, `updated_at`.
- **`map_cells`** — `(map_id, cell_id)` PK, `geom geometry(Polygon,0)`, `height`, `biome_id`, `state_id`, `province_id`, `culture_id`, `religion_id`, `population`, `neighbors int[]`.
- **`map_states`** — `(map_id, state_id)` PK, `name`, `color`, `culture_id`, `capital_burg_id`, `form`, `geom geometry(MultiPolygon,0)`.
- **`map_provinces`** — `(map_id, province_id)` PK, `state_id` FK, `name`, `color`, `geom geometry(MultiPolygon,0)`.
- **`map_cultures`** — `(map_id, culture_id)` PK, `name`, `color`, `geom geometry(MultiPolygon,0)`.
- **`map_religions`** — `(map_id, religion_id)` PK, `name`, `type`, `color`, `geom geometry(MultiPolygon,0)`.
- **`map_burgs`** — `(map_id, burg_id)` PK, `name`, `cell_id`, `state_id`, `province_id`, `culture_id`, `religion_id`, `population`, `type`, `capital bool`, `port bool`, `geom geometry(Point,0)`.
- **`map_rivers`** — `(map_id, river_id)` PK, `name`, `type`, `discharge`, `width`, `geom geometry(LineString,0)`.
- **`map_routes`** — `(map_id, route_id)` PK, `group_name`, `geom geometry(LineString,0)`.
- **`map_markers`** — `(map_id, marker_id)` PK, `type`, `name`, `icon`, `geom geometry(Point,0)`.
- **`map_zones`** — `(map_id, zone_id)` PK, `type`, `geom geometry(MultiPolygon,0)`.
- **`map_annotations`** — `(map_id, id)` PK, `kind` (`note`|`ruler`), `data jsonb`.
- **`map_topology`** — `(map_id)` PK, `grid jsonb`, `pack jsonb` — tableaux bruts d'adjacence/vertex non capturés par la géométrie de `map_cells`, conservés pour fidélité exacte (les chemins Keep/Risk/Resample de l'éditeur de heightmap mutent ceci hors pipeline — ne pas supposer que c'est reproductible depuis seed+facts seuls).

## Surface API

- `GET /api/maps` — liste (id, name, seed, createdAt).
- `POST /api/maps` — génère (corps calqué sur `options.generation`), tourne le pipeline côté serveur, réponse synchrone (pas de file d'attente nécessaire en local mono-utilisateur, sauf si le temps de génération l'impose).
- `GET /api/maps/:id` — `meta`/`facts`/`layers`/`style`.
- `GET /api/maps/:id/layers/:layer.geojson` — un `FeatureCollection` par couche.
- `GET /api/maps/:id/entities/tree` — hiérarchie imbriquée pour la barre latérale du wiki.
- `GET /api/maps/:id/entities/:kind/:id` — détail d'une entité (vue wiki + préremplissage `map_ref`).
- `PATCH /api/maps/:id/entities/:kind/:id` — édition manuelle post-génération.
- `DELETE /api/maps/:id`.

---

## Fichiers critiques

- `src/generators/pipeline.ts`, `src/generators/generation-pipeline.ts` — le runner à invoquer côté serveur (Phase 3).
- `src/services/io/export.ts` — les exporteurs GeoJSON à porter dans l'ETL Postgres (Phase 1).
- `docs/architecture/future-data-model.md`, `docs/architecture/configuration.md` — les contrats de schéma/scope de config à refléter dans les tables et payloads API.
- `src/wiki-main.ts`, `src/wiki/entities.ts`, `src/wiki/eras.ts` — logique de chargement/routage/ères à rendre asynchrone et hybride (Phase 4).
- `src/components/layers.ts`, `src/components/zoom.ts`, `src/components/viewbox-events.ts` — API publiques à réimplémenter contre Leaflet (Phase 5).
- `src/services/url-params.ts`, `src/components/idle-state.ts`, `src/components/lifecycle.ts` (`regeneratePrompt`/`regenerateMap`) — le changement de la Phase 0 (fait).
- `vite.config.ts` — les deux points d'entrée à fusionner en Phase 4.
- `docker-compose.yml`, `server/db/schema.sql`, `server/scripts/import-map.mjs`, `server/README.md` — la Phase 1 (fait).
- `server/src/import.mjs`, `server/src/db.mjs`, `server/src/routes/maps.mjs`, `server/src/server.mjs` — la Phase 2 (fait).
- `src/services/io/export-json.ts`'s `getPackDataJson()`/`getPackCellsData()` — la source de données réellement utilisée par l'ETL (voir Phase 1), pas les exporteurs GeoJSON de `export.ts`.
- `server/src/generation/browser-shim.ts`, `generate.ts`, `pack-to-json.ts`, `heightmap-image-loader.ts`, `server/scripts/generate-map.ts`, `compare-with-browser.ts` — la Phase 3 (fait).
- `src/generators/heightmap-generator.ts` — seul fichier de `src/` modifié pour la Phase 3 (chargeur d'image rendu remplaçable, comportement navigateur inchangé).

## Notes de risque global

- Le plus gros poste de risque et d'effort est de loin la **Phase 5 (Leaflet)** — environ 85-90 fichiers touchent des API SVG directement, contre un seul vrai blocage pour le portage serveur de la génération. À budgéter en conséquence : c'est réalistement le chantier le plus long du plan, largement devant la mise en place de Postgres elle-même.
- Chaque phase a une vérification qui lui est propre ; à partir de la Phase 5, une vérification manuelle en navigateur devient indispensable (le projet a pour règle de ne jamais lancer Playwright automatiquement) — prévoir des passes de test manuel dédiées, pas seulement `tsc`/lint/build/tests unitaires.
