{
  "name": "mapweave-server",
  "private": true,
  "type": "module",
  "description": "Backend for the Mapweave migration (Postgres/PostGIS, see ../MIGRATION.md). Isolated from the client app's package.json — nothing here affects the browser bundle.",
  "scripts": {
    "import-map": "node scripts/import-map.mjs",
    "start": "tsx src/server.mjs",
    "generate": "tsx scripts/generate-map.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "alea": "^1.0.1",
    "fastify": "^5.2.0",
    "pg": "^8.13.1",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
